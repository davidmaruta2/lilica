# Lilica Agent Instructions

This is the Lilica React Native care-organising app for Luxford Interactive. Work in this repository and read these files before changing code:

- `docs/PROJECT_BRIEF.md`
- `docs/LUMEN_HANDOFF.md` (canonical current handoff despite its legacy name - **read its "READ THIS FIRST" section before anything else**)
- `docs/REVISION_LOG.md`
- `docs/SUPABASE_OPERATIONS.md` before any backend or authentication operation
- `docs/CORE_SYSTEM_CONTRACT.md` for approved architectural direction

## Resume here (15 September 2026)

Branch: `prephase22-remove-supported-person-faq-help`, pushed, working tree clean. This branch is ahead of `origin/master`, which is stale/behind - do not treat `master` as current, and do not merge to it without explicit product-owner instruction. Latest commit: run `git log -1` (this handoff pass sits on top of `f6285fc`, the last substantive code commit).

**Concurrent-session note:** while this handoff was being written, a commit (`cb9f696`, authored `Codex <codex@local>`, message "Implement Phase 8 occurrence engine") landed on this SAME branch from what appears to be a different, concurrently-running agent process sharing this working directory - its actual diff was unrelated to its message (it picked up an in-progress doc edit). No corruption resulted, but this confirms another agent/process can write to this exact repo/branch without warning. Always re-run `git log --oneline -10` and `git status` fresh before trusting any prior session's account of the repo state - never assume it is still accurate.

**Care Circle invitation system is COMPLETE - physical QA PASSED 15 September 2026** by the product owner on a real device with two genuine accounts, covering the full multi-person invitation flow (organiser multi-select, grouped invitation creation/confirmation/email/Share/code, acceptance, and correct person-switcher reconciliation). Do not re-open this area without a new, explicit, specific product-owner defect report.

**No EAS build or `eas.json` exists, at all, ever.** There is no build pipeline for anything to be "queued" into, and OTA (`expo-updates`) is not configured and has nothing to be "awaiting" - both are meaningless before a first native build exists. When the product owner is ready for that first build: never run `eas build` without their explicit, express, build-specific consent; keep `app.json`'s `ios.supportsTablet: false` (phone-only); enable OTA as part of that first build, not afterward. No one has asked for a first build yet - do not create one speculatively.

**Immediate next step: Phase 22 - Premium Visual Optimisation & Experience Polish**, the next agreed roadmap item. It has a mandatory, NOT-yet-satisfied gate: produce a visual/experience AUDIT REPORT ONLY first (no code, theme, or component changes), get it reviewed and approved by GPT and the product owner, then implement. See "Roadmap Pointer" below for the exact wording. This is the correct next action for an incoming agent - not writing Phase 22 code directly.

## Current State

- Phase 5 authentication, Phase 6 multi-person ownership, Phase 7 record persistence/cache/sync and Phase 8's core Record -> Occurrence engine are implemented, validated and physically approved (product-owner QA passed 10 September 2026).
- Phase 9 (everyday add/record management: a stable-identity Assigned-to control, and everyday-vs-onboarding copy for the already-existing category-gateway screen) is implemented and validated; physical-device QA is outstanding - see `docs/PHASE_9_QA.md`.
- Calendar (a month grid and day list projecting existing records, opening into the same established record editor) is implemented and validated; physical-device QA is outstanding - see `docs/PHASE_10_QA.md`. **Roadmap numbering note:** this work was implemented and committed under the working label "Phase 10", but the canonical roadmap in `docs/CORE_SYSTEM_CONTRACT.md` defines it as **Phase 11 - Calendar Projection** (Phase 10 there is Home Projection, effectively already delivered by the earlier Home redesign work). Historical commits and doc entries using "Phase 10" for Calendar are not rewritten; numbering resumes canonically from here.
- To Do (Phase 12 - actionable-work projection: open task/bill/home-or-car-matter records grouped into Overdue/Today/Upcoming, with Mine/Unassigned/All assignment filters on Phase 9's stable membership identity) is implemented and validated; physical-device QA is outstanding - see `docs/PHASE_12_QA.md`.
- Person (Phase 13 - durable-knowledge projection: contacts, care/health information, home information, documents and bills/renewals grouped from existing records, with no medical/legal inference) is implemented and validated; physical-device QA is outstanding - see `docs/PHASE_13_QA.md`. The tab previously showed `AccountScreen` under stale "person" copy it never delivered; `AccountScreen` is now reached from Person's own "Account" link.
- Reminders/notifications (Phase 14 - local-only reminder engine: a "Remind me" toggle on appointment/task/bill/homeMatter records, quiet hours and a master switch in Account, strict separation of notification-delivery state from record truth) is implemented and validated; physical-device QA requires a development build (Expo Go does not reliably support local notifications since SDK 53) - see `docs/PHASE_14_QA.md`. Server/push delivery and device-token registration are deliberately deferred - see `docs/PHASE_14_ARCHITECTURE.md`'s "Deferred: server/push" section.
- Care Circle (Phase 15 - real invitations to Contributor/Viewer roles, explicit per-domain permission grants reviewed before sending, an invitee-facing Invitations screen that auto-surfaces once per session, a real Assigned-to selector, server-enforced assignment-visibility, immediate-effect removal/leaving, sole-organiser safety) is implemented and validated, and its migration is deployed to `lilica-development` - see `docs/PHASE_15_ARCHITECTURE.md`. Physical two-real-account device QA is outstanding - see `docs/PHASE_15_QA.md`. Its server-side domain-grant enforcement was re-verified end-to-end on 12 September 2026 (see that doc's addendum) - a suspected organiser-only RLS gap was checked and found not real. Real transactional email delivery (Resend) and native Share were added 14 September 2026, and are implemented and validated - see that same doc's addenda. Still not built (documented, not faked): an "assignee no longer has access" UI treatment and in-app role-change/organiser-handoff controls.
- **Care Circle invitation & joining flow completion (14 September 2026, `\downloads\carecircle.txt`): implemented, validated, migration applied to `lilica-development`, not yet committed.** Adds a human-friendly invitation CODE (`care_space_invitations.invite_code`, server-generated, unique, an additive column - never a second security model) as a manual alternative to the email/deep-link route, resolved via a new minimal-preview RPC (`resolve_invitation_by_code()`); a "Join a Care Circle" entry point for both a genuinely new user (a small onboarding routing fork, `src/screens/JoinOrSetupScreen.tsx`) and an existing user (`CareCircleScreen`'s own "Join another Care Circle"), both converging on the SAME `JoinCareCircleScreen` component and the SAME `accept_invitation()`/membership/permission authority every other route already used - unchanged. Also corrected a real physically-tested UX bug: the Care Circle screen used to show the same just-created invitation twice at once ("Invitation created" panel + a duplicate "Pending invitations" row); CREATED and DELIVERED are now two genuinely distinct, non-overlapping states, with truthful per-device delivery-state tracking (a new optional `LocalCareSpaceState.invitationDeliveryStatus` field, no schema change - the server itself has no delivery-state column). See `docs/PHASE_15_ARCHITECTURE.md`'s 14 September 2026 addenda for the full model.
- The record-open "flash" bug (11 September 2026) was root-caused to a whole-screen navigation just to host the editor, and fixed architecturally with `src/components/RecordQuickEditor.tsx` - an overlay reusing the same `RecordSheet`/`RecordEditor`/`RecordDetail` pairing, mounted over whichever screen already shows. An existing record now opens a shared, read-only `RecordDetail` first (view/edit separation); Edit reaches the unchanged `RecordEditor`. Dismissing that sheet by Done, backdrop tap, or swipe-down now saves pending valid content through the same validated save path the Save/Add button uses, rather than discarding it.
- Corrective Task 10 (11 September 2026, `docs/CORRECTIVE_TASK_10_PEOPLE_IA.md`) renamed the fourth tab from Person/Care Circle to **People**, centred on Supported people / Key contacts / the real Care circle (Phase 15 membership list) / an Ask Lilica placeholder - no longer duplicating Home's own bills/home/documents/care-note listing. A visual pass (`tabAccent`/`ScreenBackdrop` in `src/theme.ts`/`src/components/ScreenBackdrop.tsx`) applies to Calendar/To Do/People; Home was deliberately reverted to its original plain background.
- **Phase 16 (Document Maturity - foundation): implemented, validated and its migration now APPLIED to `lilica-development`** (applied as Phase 17's own first step on 12 September 2026) - real persisted, RLS-protected typed record links (`record_links`); durable cloud-side attachment metadata (`record_attachments`) and a private Storage bucket with domain-grant RLS; the client upload/retrieval library (`src/recordLinks.ts`, `src/attachments.ts`); and a fix for a real pre-existing bug (synced attachment metadata never reached a pulled record on a second device - `src/recordSync.ts`). See `docs/PHASE_16_ARCHITECTURE.md`.
- **Phase 17 (Document Experience & Integration): implemented, validated, committed and pushed (`f1baa0c`), physical QA confirmed by the product owner on 12 September 2026.** Wires the Phase 16 foundation into the real app: RecordEditor's document-only "Related to" picker and "Does anything need doing?" task-creation flow; RecordDetail's bidirectional, context-aware related-record presentation and "View document"; in-sheet link navigation in `RecordQuickEditor`/`FirstThingScreen`; upload queuing on save plus an app-wide retry effect in `App.tsx` (covers restart-resume and pre-Phase-16 legacy documents); deletion cleanup (`removeAllLinksForRecord`, `cleanupDocumentAttachments`). See `docs/PHASE_17_ARCHITECTURE.md` for full detail.
- **Phase 18 (Privacy, Settings, Export & Account Lifecycle): implemented, validated, committed and pushed (`e3dc819`).** A new Settings entry, Privacy & data (`src/screens/PrivacyDataScreen.tsx`): real display-name editing (reuses `AuthProvider.saveProfile`); `export_my_data()` - a single read-only RPC building one JSON document from the caller's own authorised care spaces (same domain-grant filter every other read uses), written to a local file client-side and shared via the OS share sheet, never persisted server-side; "Clear data from this device" (warns of genuinely pending work first, then signs out so next sign-in rebuilds from the cloud); "Leave [care space]" for non-organiser roles (wires Phase 15's previously-unused `leave_care_space()` into real UI); "Delete account" - see Phase 18B immediately below, which replaced the original precheck-only terminal state with the real deletion. Also closed a real Phase 17-acknowledged gap: document delete/link/attachment cleanup now goes through a durable AsyncStorage-backed retry queue (`src/documentCleanupQueue.ts`), surviving an app kill mid-cleanup, replacing Phase 17's best-effort fire-and-forget. See `docs/PHASE_18_ARCHITECTURE.md`/`docs/PHASE_18_QA.md`.
- **Phase 18B (Account Deletion & Complete Data Export): implemented, validated, committed and pushed (`e3dc819`).** Resolves Phase 18A's own reported blocker: `care_space_memberships.user_id` and a second, previously-missed `care_spaces.bootstrap_owner_id` reference both become nullable, with a `former_display_name` snapshot and a new `membership_status = 'former'` value - a membership is detached, never deleted, so every FK across records/occurrences/assignments/links/attachments stays valid, and every existing access check (already `'active'`-only) denies a former membership with zero new RLS. `delete_my_account()` (security definer, `auth.uid()`-only, idempotent, server-side sole-organiser re-check) performs the real deletion; `resolve_membership_identities()` lets the client resolve truthful historical attribution. Export now downloads actual authorised document files fresh from the current cloud copy (never local-only), sharing `data.json` plus each file individually - a `.zip` archive was considered but not built, per direct product-owner decision to avoid a new dependency. See `docs/PHASE_18_ARCHITECTURE.md`'s Phase 18B section for full detail and named limitations. Physical-device QA (two-account deletion/export scenarios, `docs/PHASE_18_QA.md`) remains outstanding. Do not begin Phase 19 without its own separate, explicit, bounded implementation prompt.
- **Settings navigation correction (12 September 2026): committed and pushed (`74b1a61`), physically confirmed working.** Settings is now a right-hand application drawer with its own internal navigation hierarchy - subsections render within the drawer, never as separate top-level screens. See `docs/PHASE_18_ARCHITECTURE.md`'s "Settings navigation correction" addendum.
- **People screen final visual implementation (12 September 2026): committed and pushed (`1c1c682`), physically confirmed matching the approved mock.** Key contacts is a bounded overview (at most four, "View all (N)" for the rest via new `src/screens/ContactsListScreen.tsx`); Care circle is a summary with one "Manage" action and a bounded member preview. Presentation only - see `docs/CORRECTIVE_TASK_10_PEOPLE_IA.md`'s addendum.
- **To Do background QA correction (13 September 2026): committed and pushed (`e3dc819`), physically inspected and approved.** `src/components/ScreenBackdrop.tsx` gained an opt-in `stretch` mode (gradient sized to the actual screen height, not a fixed box) and `ToDoScreen.tsx` a wider `stops` distribution - fixes a real accidental-cream-rectangle/late-fade defect. Home/Calendar/People untouched. Visual only - see `docs/REVISION_LOG.md`.
- **Settings drawer visual/safe-area fix, profile pictures, Care Circle avatar details (13 September 2026): committed and pushed (`d79b62d`).** Fixed a real Modal safe-area bug plus a visual-quality upgrade to the Settings drawer; added real profile pictures (new private `profile-avatars` bucket, `src/profileAvatar.ts`, shown in Account and for the user's own "You" tile in People's Care circle preview); added a soft Care Circle avatar detail popup (`src/components/MemberDetailPopup.tsx`). See `docs/REVISION_LOG.md`.
- **Phase 19 (Production Hardening And Release Readiness): audit/hardening work completed, no release-blocking defect found, not yet committed.** A real, committed, fully-cleaned-up two-account collaboration and account-deletion rehearsal against `lilica-development` closes out Phase 18B's own outstanding server-side two-account QA item; RLS/permission adversarial coverage, dependency/security, accessibility (static), and store/release-config reviews all completed with no A-severity findings. Physical-hardware-only items (offline/restart, screen-reader, device matrix, reminders on a dev build, performance feel) are honestly marked NOT RUN rather than fabricated. See `docs/PHASE_19_ARCHITECTURE.md` and `docs/PHASE_19_QA.md`. **Production release remains a separate, explicit product-owner go/no-go - not implied by this phase's completion.**
- The self/someone-else onboarding fork (`Whose wellbeing are you looking to support with Lilica?`) is implemented and validated; physical-device QA is outstanding - see `docs/CARE_FORK_QA.md`.
- Supabase provides email/password Auth, organiser profiles, supported people, care spaces and organiser memberships.
- Signup and password recovery use six-digit email OTPs. Recovery proceeds through Check your email, Enter your code and Choose a new password.
- Privacy, interests, attachment bytes and setup progress remain device-local. Records now use a care-space cache/outbox and durable Supabase rows; attachment metadata only is cloud persisted.
- Stable assignment/contact entities exist as a server-side Phase 8 foundation only. Assignment UI, invitations, collaboration, granular permission management and production infrastructure have not started.
- The approved Welcome experience, safe-area correction and shared keyboard-aware form layout are protected behavior.

## Working Rules

- Keep Expo SDK 57 unless explicitly instructed otherwise.
- Preserve user changes and inspect `git status --short` before editing. Never reset cumulative work casually.
- Never expose or commit `.env.local`, `lilica-development.txt`, Supabase personal tokens, database passwords, service-role keys or SMTP credentials.
- Use only `lilica-development` (`ldocquqbcabdbscghojc`) for current hosted work. No Lilica production project exists.
- Never run a blind full `supabase config push`. Preview with `supabase config diff`, then apply only declared, reviewed properties from a narrowly scoped config.
- Preview database migrations with `npx supabase db push --linked --dry-run`; never run `supabase db reset --linked`.
- Keep organiser identity separate from supported-person identity and membership-relative relationships.
- Persist only real user-created records. Derived due/overdue state must remain deterministic.
- Preserve the Phase 8 occurrence/recurrence/history and assignment-identity contracts, and the Phase 15 Assigned-to control's domain-eligible-active-member scope (see `docs/PHASE_15_ARCHITECTURE.md`). Do not add AI, OCR, banking, monitoring, clinical features, or further permission/role UI beyond Phase 15's Organiser/Contributor/Viewer model without explicit approval.
- All text-entry screens must use the shared keyboard-aware `Screen` or `RecordSheet` path. Do not add fixed CTAs that overlay fields or device-specific keyboard margins.
- Use `react-native-safe-area-context`; do not introduce a competing safe-area system.

## Validation And Handoff

Run `npm run validate` for app changes and `npm run validate:all` for database changes. Physical-device claims require actual device testing; automated layout tests are not physical proof.

Phase 8 is implemented, committed and physically approved. Phase 9, Calendar (canonical Phase 11, committed under the working label "Phase 10" - see the roadmap numbering note above), Phase 12 (To Do) and Phase 13 (Person) are implemented, validated and committed, awaiting physical QA. Phase 14 (reminders/notifications) is implemented, validated and committed (`b59ef1a`), awaiting a development-build physical QA pass (`docs/PHASE_14_QA.md`).

**Phase 15 (Care-Circle Invitations And Collaboration) is implemented, validated and committed, with its migration deployed to `lilica-development`.** Its Hard Start Gate (Phase 14 physical QA sign-off) was explicitly waived by the product owner on 11 September 2026 before this phase began - that decision, not automated-test success, is what authorised it. Phase 15 still requires genuine two-real-account physical testing per its own brief (section 79) - do not treat one account pretending to be two as adequate QA; see `docs/PHASE_15_QA.md`. Phase 16 and Phase 17 are both implemented, validated, committed and pushed (`f1baa0c`), with Phase 16's migration applied to `lilica-development` and physical QA confirmed by the product owner. Phase 18 and Phase 18B are likewise implemented, validated, committed and pushed (`e3dc819`), migrations applied to `lilica-development` - their own two-account/device physical QA remains outstanding (see `docs/PHASE_18_QA.md`), though Phase 19's own real database rehearsal has since closed out the server-side portion of that item. **Phase 19 (Production Hardening And Release Readiness) is implemented and validated - see `docs/PHASE_19_ARCHITECTURE.md`/`docs/PHASE_19_QA.md` - with no release-blocking defect found; not yet committed, and production release requires a separate, explicit product-owner go/no-go.**

- **Phase 20 (20A audit + 20B: Recent Activity, Unified Search, Care Summary): COMPLETE, physically tested and approved by the product owner, committed and pushed (`1bcbf2a`).** Exactly three candidates approved from Phase 20A's audit - see `docs/PHASE_20_ARCHITECTURE.md`/`docs/PHASE_20_QA.md`. Every other Phase 20A candidate (contact import, medication structure, collaboration push notifications, contextual record comments, calendar export, profile-field expansion, former-member management refinements, human-readable export, general chat, generic Notes) remains explicitly not implemented - see `docs/PHASE_20_GAP_AUDIT.md`'s own preserved backlog.
- **Phase 21B (Billing, Subscription & Entitlement): implemented, validated, migration applied to `lilica-development`, not yet committed.** Final approved commercial model: 60 days completely free, then £8.99/year. Server-side entitlement enforcement is complete and proven by pgTAP (`care_space_has_active_entitlement()`, called consistently from every gated RPC - see `docs/PHASE_21_ARCHITECTURE.md` for the exact mutation inventory and the documented resolution of a real tension between two of the brief's own instructions regarding `remove_member()`). Client-side (`src/entitlement.ts`, `src/billing.ts`, `SubscriptionScreen.tsx`) is implemented and unit-tested with RevenueCat's SDK mocked. **Purchases cannot be tested in this environment** - no RevenueCat project, store products, or development build exist yet (Expo Go cannot run native purchases at all). Do not claim physical purchase/webhook QA has occurred; see `docs/PHASE_21_QA.md` for exactly what remains outstanding.
- **Phase 21C (Expired Trial / Read-Only UX + Phase 21 Closure): implemented, validated, no schema change (confirmed via `db diff`), not yet committed.** Closes the proactive-UX gap Phase 21B's own completion report identified: a centralised `isReadOnly`/`guardMutation` mechanism in `App.tsx`, one reusable `src/components/ReadOnlyGate.tsx` notice (distinct owner/collaborator copy - a collaborator is never shown the owner's billing detail or told their own subscription would help), gated at exactly five entry points (Home/To Do Add, Person Add links, the shared existing-record Edit trigger, Care Circle's Invite), and a friendly server-rejection fallback (`hasEntitlementHeldMutations()`) for the rare remaining race. Reuses Phase 21B's server architecture and RPCs entirely - no new table, RPC, or billing mechanism. The commercial model is unchanged: 60 days free, then £8.99/year. See `docs/PHASE_21_ARCHITECTURE.md` section 23 and `docs/PHASE_21_QA.md`'s matching section. **Phase 21 is now engineering-complete; real store purchase QA (RevenueCat project, store products, development build, sandbox purchase/renewal/cancellation/refund/restore) remains explicitly outstanding - do not claim it is physically verified.** Do not begin Phase 22 without its own separate, explicit, GPT-and-product-owner-approved prompt.

- **Home/Search interaction refinement (`search4.txt`), 14 September 2026: implemented, validated, committed and pushed.** A bounded UI/interaction refinement to match an approved mock, not a redesign: Home's avatar grew to 56px with a larger initial and the supported person's first name now shows directly beneath it; the search affordance moved out of the header into its own real search-bar-styled element next to the avatar (a magnifying-glass glyph plus "Everything for [Name], in one place.", no gap between glyph and text per product-owner correction), still opening the same existing dedicated Search screen (Phase 20B) - not an inline-expanding field. The horizontal dashboard strip gained left/right scroll chevrons (`src/screens/HomeScreen.tsx`) with real enable/disable state tied to actual scroll position, never replacing swipe. `src/screens/SearchScreen.tsx` needed no code change - inspection confirmed it already met every requirement (dynamic title, autoFocus, plain-callback back navigation); only tests were added to lock that in. See the diff/QA detail in the search4 completion report (not committed as a tracked file, per this repo's own convention for loose brief/report `.txt` files).
- **Standing rule (14 September 2026): no em-dashes or en-dashes anywhere in this repository** - UI copy, code comments, docs, commit messages. Use a plain hyphen instead (with a single space either side when it separates a clause, no space when it joins two values directly, e.g. `9pm-8am`). A full repository sweep was completed 14 September 2026 removing every existing em/en-dash; do not reintroduce them when writing new code, docs, or copy.
- **Standing requirements before the first real EAS build (added 14 September 2026, none actioned yet - no `eas.json`/EAS project exists):** (1) no `eas build` is ever run without the product owner's explicit, express consent for that specific build; (2) iOS is phone-only - `app.json`'s `ios.supportsTablet` is `false` (corrected from `true`), so TestFlight never asks for iPad screenshots - do not revert without explicit direction; (3) OTA (`expo-updates`) must be enabled starting with the very first build, not added later, so future JS-only fixes can ship without a new store build. Full detail: `docs/PHASE_19_ARCHITECTURE.md`'s "Product-owner standing requirements ahead of the first real EAS build" addendum.

## Care Circle invitation and multi-person access flows (14-15 September 2026): COMPLETE, physical QA PASSED, committed and pushed (`f6285fc`/`938241a`)

The full invitation/collaboration closure sequence, all implemented, validated and now physically confirmed working by the product owner on a real device with two genuine accounts:

- Multi-person invitation scope: an organiser can select MULTIPLE supported people ("Who can they help with?") in one invitation - `care_space_invitation_groups` orchestrates the existing, unmodified per-care-space `invite_member()`/`accept_invitation()`/`decline_invitation()`/`revoke_invitation()` via `invite_member_group()`/`accept_invitation_group()`/`decline_invitation_group()`/`revoke_invitation_group()`. One user-facing invitation, separate secure per-care-space memberships underneath - never a global membership. Real root cause of the originally-reported "organiser only sees one person" bug: the organiser-eligible-people list depended on a one-shot, un-retried `reconnectCareSpaces()` call fired once per session; fixed by refreshing it every time Care Circle is opened.
- Invitation-code rate-limiting security correction: `resolve_invitation_by_code()` is now the ONE atomic, client-callable entry point (auth + rate-limit accounting + threshold check + lookup in one call) - the prior two-RPC split was a real, client-bypassable defect, fixed by removing the separate helper entirely.
- "Join a Care Circle" discoverability and navigation: a clearly visible action on the Care Circle screen itself (and the Settings drawer), for every authenticated user regardless of role; a real pre-existing navigation bug (tapping it from the People-tab route had no visible effect due to an else-if ordering conflict in `App.tsx`) was found and fixed; Back/Cancel correctly returns to Care Circle, never Home.
- "Send invitation" renamed to "Continue" on the invite-creation form (direct product-owner correction - that tap only creates the invitation; the actual send happens on the next screen).

25 new pgTAP assertions (`supabase/tests/database/multi_person_invitation_groups.test.sql`, 487/487 local suite green); multiple new/updated Jest suites across the client. Full technical detail across every addendum in `docs/PHASE_15_ARCHITECTURE.md`; closure summary in `docs/REVISION_LOG.md`'s 15 September 2026 entry. **Do not re-open this area without a new, specific, explicit product-owner defect report.**

## Roadmap Pointer (added 13 September 2026, documentation only; status corrected 15 September 2026)

Phase 19 - Production Hardening & Release Readiness is implemented, committed and pushed. On 13 September 2026 the product owner approved a three-phase roadmap extension beyond it: Phase 20 - Competitive Gap Review & Carer-Focused Enhancements (COMPLETE, see above in this file), Phase 21 - Billing, Subscription & Membership/Entitlement (engineering-complete, committed and pushed, real store-purchase QA still outstanding - see above), and **Phase 22 - Premium Visual Optimisation & Experience Polish, which is the current, immediate next item and has NOT started.** Phase 22 has an explicit, mandatory audit/approval gate that is NOT yet satisfied: it must produce a visual/experience audit/report ONLY (no code/theme/component changes) and get GPT+product-owner approval of the exact optimisation plan BEFORE any implementation. An incoming agent's correct first action toward Phase 22 is producing that audit, not writing Phase 22 code.
