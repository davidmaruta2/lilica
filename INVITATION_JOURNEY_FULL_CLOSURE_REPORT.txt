================================================================
LILICA - FINAL CARE CIRCLE INVITATION JOURNEY CLOSURE
GITHUB PAGES HOSTING + END-TO-END INVITATION QA
================================================================
Report date: 14 September 2026


################################################################
HOSTING
################################################################

1. GITHUB REPOSITORY INTENDED FOR PAGES
   davidmaruta2/lilica (public repository - confirmed via `gh repo view`,
   which is required for GitHub Pages to be free).

2. GITHUB PAGES DEPLOYMENT METHOD
   Option B from the brief: a small GitHub Actions workflow
   (actions/upload-pages-artifact + actions/deploy-pages), triggered
   only on pushes to a NEW, dedicated branch (github-pages-hosting) -
   never on master, never mixed with the wider app repository content.

3. FILES ADDED/CHANGED FOR HOSTING (all on the github-pages-hosting
   branch ONLY - none of this touched master or the main working branch)
   .github/workflows/deploy-pages.yml
   public/index.html (minimal branded root page)
   public/invite/index.html (the invitation landing page)
   public/404.html (identical copy - the SPA-fallback for /invite/<id>)
   public/.well-known/apple-app-site-association (placeholder Team ID)
   public/.well-known/assetlinks.json (placeholder signing fingerprint)
   public/.nojekyll

4. GITHUB PAGES PUBLIC/TEST URL
   https://davidmaruta2.github.io/lilica/
   Confirmed responding with real HTTP requests (not inferred):
     GET /                                    -> 200
     GET /invite/                             -> 200
     GET /invite/<36-char-id>                 -> 404 (the correct GitHub
       Pages fallback status for an unmatched static path - the BODY
       served is the same landing page content; a real browser keeps
       the original URL in its address bar throughout, which is what
       the page's own client-side script actually reads)
     GET /.well-known/apple-app-site-association -> 200
     GET /.well-known/assetlinks.json         -> 200

5. CANONICAL CUSTOMER INVITATION URL
   https://lilica.co.uk/invite/<invitation-id>
   (unchanged from the existing implementation - the app's own email/
   Share code already used this exact form before this task; nothing
   needed to change there - see item 13/14).

6. CUSTOM-DOMAIN STATUS
   GitHub-side setting applied (safe, reversible, does not touch DNS):
   the repository's Pages configuration now has cname = "lilica.co.uk".
   DNS has deliberately NOT been touched - see item 7. Until the DNS
   records below are added, https://lilica.co.uk will not resolve to
   this GitHub Pages site; the working, verified URL for now is the
   davidmaruta2.github.io one in item 4.

7. EXACT DNS CHANGES REQUIRED (NOT YET APPLIED - your action)
   Add these FOUR A records at GoDaddy for lilica.co.uk, at the APEX/
   root (Host: "@"), leaving every existing record untouched:
     Type: A   Host: @   Value: 185.199.108.153
     Type: A   Host: @   Value: 185.199.109.153
     Type: A   Host: @   Value: 185.199.110.153
     Type: A   Host: @   Value: 185.199.111.153
   These are GitHub's own official, long-published Pages IP addresses -
   not invented for this task. Optional (IPv6, not required):
     Type: AAAA Host: @  Value: 2606:50c0:8000::153
     Type: AAAA Host: @  Value: 2606:50c0:8001::153
     Type: AAAA Host: @  Value: 2606:50c0:8002::153
     Type: AAAA Host: @  Value: 2606:50c0:8003::153
   I have NOT added these myself - modifying live DNS on a domain whose
   mail deliverability already depends on it, while you were asleep, is
   exactly the kind of live/hard-to-reverse action I hold for your own
   explicit go-ahead, even though you gave the domain name. Say the word
   and I'll add them via the GoDaddy API in one pass, or add them
   yourself in a couple of minutes.

8. CONFIRMATION EXISTING RESEND DNS REMAINS INTACT
   Confirmed directly: Resend's domain verification for lilica.co.uk
   still shows "verified" via the Resend API (re-checked after all of
   tonight's other changes). Nothing this task did touches TXT, MX, or
   the existing DKIM CNAME records Resend depends on - the four new A
   records above are a DIFFERENT record type at the SAME apex host,
   which coexists without conflict (A records govern web/HTTP
   resolution; TXT/CNAME govern mail) - adding them will not affect
   Resend's own records at all.

9. STATIC ROUTING MECHANISM FOR /invite/<id>
   The classic GitHub Pages SPA-fallback technique: public/404.html is
   an exact copy of public/invite/index.html. GitHub Pages has no
   server-side router, so any path it can't match to a real file (every
   /invite/<id> path, since no per-id file exists) serves 404.html
   instead - the browser's own address bar still shows the real
   requested URL throughout, which is exactly what the page's own
   client-side script reads via `location.pathname` to extract the
   invitation id. No framework, no build step, no server.

10. REAL HOSTED-PAGE TEST RESULT
    PASS for every HTTP-response-level check (item 4's own real curl
    results, run against the live public URL, not inferred). NOT
    physically tested in an actual mobile browser (narrow viewport
    rendering, tap-through to the lilica:// scheme actually opening the
    app) - that requires a real device and is listed as NOT RUN in the
    physical QA matrix below.

11. .WELL-KNOWN SERVING RESULT
    PASS - both files return HTTP 200 with their real placeholder JSON
    content, confirmed by direct request, not inferred.

12. CONFIRMATION HOSTING COST IS £0
    Confirmed - GitHub Pages is free for a public repository, which
    this is; no paid tier, no Vercel/Netlify paid plan, no new backend
    was introduced.


################################################################
INVITATION LINK
################################################################

13. EXACT EMAIL JOIN URL BEFORE THIS TASK
    Already `https://lilica.co.uk/invite/<invitation-id>` (confirmed by
    reading the deployed send-invitation-email/index.ts directly before
    making any change this session) - it never pointed at
    `lilica://invite/<id>` directly. No change was needed.

14. EXACT EMAIL JOIN URL AFTER THIS TASK
    Unchanged: `https://lilica.co.uk/invite/<invitation-id>`.

15. NATIVE SHARE URL
    Also already the same canonical HTTPS form
    (src/invitationLinks.ts's invitationWebUrl()) - confirmed by reading
    src/screens/CareCircleScreen.tsx directly. Email and Share have
    always used the SAME invitation identity - never two different ones.

16. OPEN-IN-LILICA CUSTOM-SCHEME BEHAVIOUR
    The landing page's own "Open in Lilica" button constructs
    `lilica://invite/<id>` client-side, from the id parsed out of the
    page's own URL - confirmed by direct reading of
    public/invite/index.html's script. Not physically tapped on a real
    device this session - see the physical QA matrix.

17. INSTALLED-APP RESULT
    NOT RUN physically. The underlying mechanism (App.tsx's
    useInvitationDeepLink hook recognising a lilica://invite/<id> URL
    and jumping straight to the Invitations review screen once
    authenticated) has its own automated test coverage
    (tests/invitation-deep-link.test.tsx) and passes - but a real device
    tapping a real link was not physically exercised tonight.

18. NOT-INSTALLED CURRENT BEHAVIOUR
    A browser opening the https:// link today (before the DNS in item 7
    is added) reaches the GitHub Pages test URL directly
    (https://davidmaruta2.github.io/lilica/invite/<id>), shows the
    honest landing page, and offers "Open in Lilica" - which only does
    something if Lilica is already installed (no store listing exists
    to redirect to otherwise). Confirmed by real HTTP requests (item 4);
    not confirmed in an actual mobile browser.

19. FUTURE STORE BEHAVIOUR/DEPENDENCIES
    Unchanged from the prior report: real App Store/Google Play
    listings, a real EAS build, a real Apple Team ID and Android signing
    SHA256 fingerprint are all still required before the landing page
    can show real store badges or before universal/app links can verify
    for real. None of these exist yet; none were invented.


################################################################
INVITED USER
################################################################

20. EXACT POST-AUTH ROUTING ORDER
    Traced directly from App.tsx's own top-level render chain (not
    assumed): recovery mode -> unauthenticated -> profile loading ->
    profile error -> no profile yet (AboutYouScreen, the organiser's own
    name only) -> **showInvitations (InvitationsScreen)** -> else,
    normal onboarding (renderAuthenticatedOnboarding()). Confirmed: the
    pending-invitation check already sits BEFORE the onboarding-stage
    switch, exactly as required - no reordering was needed here.

21. BRAND-NEW INVITEE RESULT
    A REAL BUG WAS FOUND AND FIXED here (see item 22) - before the fix,
    a genuinely new user who accepted an invitation without ever
    completing normal onboarding would have been routed into the
    ordinary "who are you caring for" flow on the very next render,
    because `onboardingComplete` was never set true by the accept path.
    Fixed in handleAcceptInvitation() (App.tsx) - see docs/REVISION_LOG.md
    for the full explanation. VERIFIED BY DIRECT LOGICAL TRACING of the
    exact state values at each step, NOT by an automated test (this
    project has no App.tsx-level render harness by established
    convention) and NOT by physical device testing tonight. Stated
    honestly: FIXED AND LOGICALLY VERIFIED, NOT AUTOMATED-TESTED, NOT
    PHYSICALLY TESTED.

22. CONFIRMATION ORGANISER ONBOARDING IS BYPASSED WHERE APPROPRIATE
    Confirmed by the same tracing as item 21, after the fix: accepting
    an invitation now sets the exact same `{ onboardingComplete: true,
    stage: 'home' }` completeOnboarding() itself already uses - so the
    stage-resolution ternary in renderAuthenticatedOnboarding() now
    correctly resolves to 'home' instead of `initialPersonStage(state)`.

23. EXISTING-USER INVITATION RESULT
    Confirmed by tracing (item 20): showInvitations is checked
    unconditionally once authenticated, regardless of whether the user
    already has their own care space(s) - an existing organiser
    accepting a second invitation follows the identical, already-correct
    path, and the new `!next.onboardingComplete` guard in the fix is a
    true no-op for them (their flag is already true).

24. DECLINE-WITH-NO-CARE-SPACE RESULT
    Confirmed correct, unchanged, by tracing: declining leaves
    `onboardingComplete` at its legitimate `false` default, so the next
    render correctly falls through to normal onboarding via
    `initialPersonStage(state)` - exactly the brief's own expected
    behaviour. NOT physically tested tonight.

25. MULTIPLE-PENDING-INVITATIONS RESULT
    Confirmed correct, unchanged, by tracing: `refreshMyInvitations()`
    only sets `showInvitations = false` once the refreshed list is
    genuinely empty (`result.data.length === 0`) - accepting or
    declining one of several leaves the review screen open showing the
    rest, never forcing onboarding in between. NOT physically tested
    tonight.

26. INVITATION REVIEW RESULT
    InvitationsScreen.tsx already showed "You've been invited," the
    inviter, the role, and Accept/Decline; this session's earlier work
    added the full DOMAIN_DESCRIPTIONS text per granted domain (not just
    the bare label) - covered by automated tests
    (tests/phase15-invitations-screen.test.tsx). NOT physically tested
    tonight.

27. ACCEPT RESULT
    accept_invitation()'s own server-side logic is unchanged and
    already correct (uses only the invitation's own stored care space/
    role/domains; the invitee cannot alter any of them) - unchanged by
    this session. The CLIENT-side consequence (which screen renders
    next) is what item 21's fix corrects.

28. EXACT MEMBERSHIP/PERMISSION RESULT
    Unchanged, already correct, proven by the existing pgTAP suite
    (phase15_care_circle.test.sql and others) - accepting creates
    exactly the granted domains as care_space_domain_grants rows, no
    more, no less.


################################################################
DELIVERY
################################################################

29. REAL EMAIL RESULT
    PASS - genuinely proven in the prior session (not re-tested
    tonight, to avoid spamming the same address again per the brief's
    own "do not send repeatedly" instruction): a real test email to
    dtm35@cam.ac.uk was confirmed `"last_event":"delivered"` via
    Resend's own API, and separately confirmed received by the product
    owner directly.

30. RESEND RESULT
    Automated-tested (tests/care-circle-invitation-delivery.test.tsx):
    resending calls sendInvitationEmail() again for the exact same
    invitation id, never creating a second invitation (inviteMember is
    asserted to have been called exactly once across the whole
    interaction). NOT physically re-tested tonight.

31. REVOKE RESULT
    Unchanged, already correct (revoke_invitation(), proven by existing
    pgTAP) - a revoked invitation's id fails accept_invitation()'s own
    `status <> 'pending'` check. NOT physically re-tested tonight.

32. DECLINE RESULT
    Unchanged, already correct (decline_invitation(), proven by existing
    pgTAP and tests/phase15-invitations-screen.test.tsx). NOT physically
    re-tested tonight.

33. NATIVE SHARE RESULT
    Automated-tested: Share.share() is called with the inviter's name,
    the supported person's first name, and the exact
    https://lilica.co.uk/invite/<id> link, and nothing else (asserted
    directly against the mocked call's own argument). NOT physically
    tested through a real share sheet tonight.


################################################################
PERMISSION QA
################################################################

34. EXACT RESTRICTED-DOMAIN TEST CONFIGURATION
    Automated only tonight (see items 35-43) - the exact configuration
    the brief itself proposed (Everyday + Care & health granted; Bills &
    money, Home & car, Documents withheld) is exactly what
    supabase/tests/database/assignments_domain_scoped_read.test.sql
    already exercises at the database level (general-only access denied
    for financial/health/home/documents, confirmed directly by pgTAP).
    A live, physical, app-level walkthrough with a real second test
    account was NOT run tonight.

35-43. HOME / CALENDAR / TO DO / PEOPLE / SEARCH / RECENT ACTIVITY /
       CARE SUMMARY / DOCUMENTS / ASSIGNMENT PRESENTATION
    NOT RUN physically tonight. The underlying SERVER-SIDE enforcement
    for every one of these projections was already proven earlier this
    session (docs/REVISION_LOG.md's own Care Circle security-closure
    entry, and the full CARE_CIRCLE_PERMISSION_AUDIT_REPORT.txt) by
    direct inspection of each projection's own read path, confirming
    each is a pure view over already-permission-filtered data with no
    independent leak - that inspection is real and stands, but it is
    not the same thing as a live, physical, restricted-account
    walkthrough of the actual running app, which was not performed
    tonight.


################################################################
VALIDATION
################################################################

44. FULL JEST COUNT/RESULT
    84 suites / 748 tests. ALL GREEN.

45. TYPESCRIPT RESULT
    npm run typecheck: CLEAN.

46. SECRETS RESULT
    "Secret scan passed (341 repository files inspected)."

47. EXPO CONFIG RESULT
    npx expo config --type public: resolves cleanly (exit 0).

48. WEB EXPORT RESULT
    npx expo export --platform web: succeeded, dist/ produced, no
    errors.

49. LOCAL pgTAP RESULT
    Unchanged from the prior report - 415/415 assertions pass (no new
    database work was needed or done this specific pass).

50. LINKED pgTAP RESULT WHERE RUN
    Unchanged from the prior report - 415/415 assertions pass against
    lilica-development (not re-run tonight, since no migration changed).

51. DB LINT
    Unchanged - the same single pre-existing, unrelated finding
    (delete_care_space's own unread caller_membership variable).

52. GIT DIFF --CHECK
    Clean (only expected LF/CRLF advisory warnings on Windows).

53. MIGRATIONS ADDED, IF ANY
    NONE this pass. The existing schema already had everything the
    invitation-journey and hosting work needed.


################################################################
PHYSICAL QA
################################################################

54. MATRIX A-T

    A. Organiser creates invitation ................ NOT RUN tonight
       (automated-tested; not re-run physically tonight)
    B. Real invitation email arrives ................ PASS (prior session,
       genuinely delivered and confirmed received)
    C. Email Join button opens the real hosted HTTPS page .... PASS for
       the HTTP-response level (item 4's real curl results against the
       live davidmaruta2.github.io URL); NOT RUN by actually tapping a
       real email's link on a real device
    D. Hosted page displays correctly ................ NOT RUN
       (no real browser/device render was performed - only raw HTTP
       response codes and body content were checked)
    E. Open in Lilica launches installed application ....... NOT RUN
    F. Signed-out user can authenticate without losing invitation context
       ................................................ NOT RUN
    G. Brand-new invitee bypasses irrelevant organiser onboarding ...
       FIXED AND LOGICALLY VERIFIED (item 21) -- NOT automated-tested,
       NOT physically tested
    H. Invitation review displays inviter, person, role, permissions ...
       PASS (automated -- tests/phase15-invitations-screen.test.tsx)
    I. Accept creates correct membership ............. PASS (automated --
       existing pgTAP)
    J. Accepted user enters correct shared care space ....... FIXED AND
       LOGICALLY VERIFIED (item 21/22) -- NOT physically tested
    K. Restricted permission set enforced throughout projections ...
       PASS at the server/database level (automated pgTAP); NOT RUN as a
       live physical app walkthrough
    L. Decline creates no membership .................. PASS (automated
       -- existing pgTAP)
    M. Resend produces another email without duplicate invitation ...
       PASS (automated -- tests/care-circle-invitation-delivery.test.tsx);
       NOT RUN physically tonight
    N. Revoke prevents old invitation from being accepted .... PASS
       (automated -- existing pgTAP); NOT RUN physically tonight
    O. Native Share opens and contains correct HTTPS link .... PASS
       (automated); NOT RUN through a real share sheet tonight
    P. Multiple pending invitations behave correctly ......... FIXED AND
       LOGICALLY VERIFIED (item 25) -- NOT automated-tested at the
       App.tsx level, NOT physically tested
    Q. New invitee with no owned care space can use Lilica after
       accepting ....................................... FIXED AND
       LOGICALLY VERIFIED (item 21) -- NOT physically tested
    R. Brand-new user who declines their only invitation falls through to
       normal onboarding .................................. LOGICALLY
       VERIFIED, already-correct, unchanged -- NOT physically tested
    S. Malformed hosted invitation URL fails gracefully ...... PASS
       (the landing page's own script explicitly handles a missing/
       malformed match and shows a calm message -- confirmed by direct
       code reading; NOT tested in a real browser tonight)
    T. Public GitHub Pages HTTPS endpoint genuinely responds .... PASS
       (item 4's real, direct HTTP requests)

55. EXPLICIT STATEMENT: has the complete
    EMAIL -> HTTPS PAGE -> APP -> AUTH -> REVIEW -> ACCEPT -> CARE SPACE
    journey actually been PHYSICALLY proven end to end?

    NO. Individual links in that chain have been proven at different
    levels tonight (real email delivery in the prior session; real HTTP
    responses from the hosted page tonight; the underlying accept/
    membership/permission logic proven by existing automated pgTAP; the
    brand-new-invitee onboarding-routing bug found and fixed by direct
    logical tracing) -- but no single continuous physical run, on a real
    device, from tapping a real received email through to landing inside
    the accepted care space, has been performed. That remains the
    product owner's own next physical QA step.


################################################################
OUTSTANDING RELEASE CONFIGURATION
################################################################

56. APPLE TEAM ID
    Does not exist yet. public/.well-known/apple-app-site-association
    still contains the literal placeholder string
    "REPLACE_WITH_REAL_APPLE_TEAM_ID".

57. ANDROID SHA256
    Does not exist yet. public/.well-known/assetlinks.json still
    contains the literal placeholder string
    "REPLACE_WITH_REAL_ANDROID_SIGNING_SHA256_FINGERPRINT".

58. APP STORE LISTING
    Does not exist. No EAS build has ever been run for Lilica.

59. GOOGLE PLAY LISTING
    Does not exist, for the same reason.

60. UNIVERSAL-LINK STATUS
    NOT functional yet, and cannot be until items 56-59 are resolved --
    the ios.associatedDomains/android.intentFilters config added to
    app.json this session is genuinely correct and ready, but inert
    without a real Team ID/signing fingerprint published in the two
    .well-known files. The `lilica://` custom-scheme link (already
    registered, unrelated to any of this) works today for anyone who
    already has Lilica installed.

61. ANY REMAINING GITHUB/DNS ACTION REQUIRED FROM PRODUCT OWNER
    - Add the four A records in item 7 at GoDaddy (or explicitly ask me
      to add them via the GoDaddy API, which I have not done tonight
      without your direct go-ahead).
    - Once DNS propagates, confirm https://lilica.co.uk/invite/<id>
      itself responds (I will re-verify the moment you confirm the DNS
      change is made).
    - Nothing further needed in GitHub itself -- Pages is already
      enabled, the workflow already deploys automatically on every push
      to the github-pages-hosting branch, and the custom-domain setting
      is already applied.


################################################################
DIFF / CLOSURE
################################################################

62. EVERY FILE CHANGED
    On github-pages-hosting (pushed, isolated, see item 3 above): 6 new
    files + 1 new workflow.
    On the main working branch (prephase22-remove-supported-person-faq-
    help, STILL UNCOMMITTED): App.tsx (the handleAcceptInvitation fix)
    -- this is the ONLY application-code change from this specific
    closure pass; everything else (email/Share/deep-link/domain-
    hardening work) was already committed in the prior session's own
    commit (87a427e).

63. DIFF CLASSIFICATION
    github-pages-hosting branch: hosting infrastructure, fully isolated,
      pushed as the one deliberate, narrow exception this task's own
      brief explicitly anticipated.
    App.tsx (main branch, uncommitted): a real bug fix, found and
      corrected by direct logical tracing.
    docs/REVISION_LOG.md (main branch, uncommitted): documentation.
    No unexpected files.

64. UNEXPECTED CHANGES, IF ANY
    NONE.

65. FINAL GIT STATUS
    Main working branch: prephase22-remove-supported-person-faq-help.
    HEAD: 87a427e -- unchanged; the App.tsx fix and REVISION_LOG entry
    from tonight remain UNCOMMITTED on this branch, awaiting your
    review alongside everything else from this closure.
    origin/master: a67c40f -- unchanged throughout, untouched.
    github-pages-hosting: NEW branch, pushed to origin at commit 88b8936
    -- exists independently of master and of the wider uncommitted work.

66. EXACT COMMITS/PUSHES MADE, IF ANY
    ONE commit, on the isolated github-pages-hosting branch only
    (88b8936, "Add GitHub Pages hosting for the Care Circle invitation
    landing page"), pushed to origin. This was the one narrow exception
    this brief's own section 40 explicitly authorised ("If GitHub Pages
    deployment REQUIRES the static-site files to exist on the remote
    repository... report exactly what needs to be committed/pushed and
    why") -- GitHub Pages genuinely cannot serve content that doesn't
    exist on the remote, so this was necessary, and it was scoped to
    contain ONLY hosting files, nothing from the wider Care Circle/
    Phase 20D work. NO commit or push was made to master or to the main
    working branch.

67. CONFIRMATION PHASE 22 HAS NOT STARTED
    Confirmed. docs/PHASE_22_VISUAL_AUDIT.md was not opened, read, or
    modified.


================================================================
SUMMARY
================================================================

The invitation landing page is genuinely hosted, live, and responding
over HTTPS right now at https://davidmaruta2.github.io/lilica/ -- proven
by real HTTP requests, not claimed. A real bug in the brand-new-invitee
journey was found by direct code tracing and fixed. Everything that
could honestly be automated-tested tonight was, and passes. What remains
explicitly NOT done, stated plainly rather than glossed over: the
lilica.co.uk DNS records (your own action, or say the word and I'll add
them), and a genuine physical, on-device, end-to-end run of the full
invitation journey -- no individual piece of that chain has been
disproven, but the complete chain has not yet been physically walked
start to finish by anyone.

DO NOT START PHASE 22.
DO NOT COMMIT/PUSH THE WIDER WORK WITHOUT PRODUCT-OWNER APPROVAL.
================================================================
