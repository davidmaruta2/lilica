# Revision Log

## 19 September 2026 - First production-profile build: iOS/Android 1.0.0 (5)

On David's explicit authorisation ("generate the new android and apple builds and submit both to stores when they are done"), the first ever `production`-profile build was run, following the full pre-build validation checklist: `npm run typecheck` clean, Jest 104 suites/916 tests pass, `secrets:check` clean (423 files), resolved Expo config confirmed (real icon, `ios.supportsTablet: false`, correct bundle id, OTA update URL/runtime policy), `expo export --platform web` clean, `git diff --check` clean, from commit `313d064`.

Result: iOS `1.0.0 (5)` (EAS `22860bae-49bb-46de-9ccb-0e4cb5a51ca8`) and Android `1.0.0 (5)` (EAS `a7ed9204-270b-482e-a329-2f72f863ceef`), both correctly pulling the `production` EAS environment (confirmed in build logs: `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, both RevenueCat keys, all loaded from "production"). This is the first build genuinely wired to `lilica-production` rather than `lilica-development`.

Both submitted immediately after: iOS to App Store Connect/TestFlight (using the durable Apple API key, zero manual credential entry), Android to Google Play's **internal** track specifically -- David explicitly chose to keep the Android submission on `internal` rather than switch `eas.json`'s `submit.production.android.track` to `production`, so this release is not yet publicly visible on Google Play. iOS submissions always upload to App Store Connect/TestFlight regardless of profile; going public on the App Store still requires a separate manual "submit for review" and release action in App Store Connect, not performed here.

This supersedes `1.0.0 (4)` (the `store-test` build, still dev-backed) as the current binary baseline. It is the first build carrying the app's real, live-configured backend end to end. What remains before this is a genuine public release is unchanged from the store-readiness list already tracked in `docs/REVENUECAT_BILLING_SETUP_REPORT.md` and `docs/BUILD_RELEASE_CONTROL.md` -- none of it something a build resolves.

## 19 September 2026 - Production Supabase/EAS, durable submission credentials, RevenueCat production webhook, and production Auth email

On David's explicit instruction, in a single session:

**Store-test build approved, built, and submitted.** Full build statement given and approved (both platforms, `store-test` profile, commit `f3d0dcb`, full delta = returning-account fix + approved icon + OTA foundation + notification bell/Care Summary PDF/webhook hardening + the 17 September batch). Result: iOS `1.0.0 (4)` (EAS `6d89e118-ab7b-4065-a279-03ef1b581b66`) submitted to TestFlight; Android `1.0.0 (4)` (EAS `56cb08fe-f5a7-4b16-b101-ce6fe5a63ae1`) submitted to Google Play internal, release status `COMPLETED`. This is now the current native baseline and the first OTA-capable Lilica binary (no OTA has been published from it).

**Durable submission credentials configured, superseding the old one-time-use pattern.** David supplied a permanent Apple App Store Connect API key (`AuthKey_5LPXC77JFN.p8`, Key ID `5LPXC77JFN`, Issuer ID `67953a69-92f8-4cce-931b-8543e12844dd`) and a permanent Google Play service-account JSON key for `revenuecat-service-account@lilica-6gy1l5.iam.gserviceaccount.com`. Both are stored outside the repository at `C:\Users\DavidPC\.lilica-credentials\` and referenced by absolute path in `eas.json`'s `submit.store-test` and `submit.production` blocks, so `eas submit --non-interactive` now works for both platforms without any interactive credential setup. **This is a standing, permanent policy point: no future agent should ever ask David to re-supply these.** See `docs/BUILD_RELEASE_CONTROL.md` and `AGENTS.md` for the matching CRITICAL sections.

**Production Supabase project and EAS production environment created.** New project `lilica-production` (`luyoyupghbxjftqwzcfn`, eu-west-2). All 22 migrations through `20260917130000_medical_log.sql` applied via `supabase db push --linked --include-all`; the full pgTAP suite then run and passed against it (23 files, 496 assertions, identical to `lilica-development`). `entitlement-webhook` and `send-invitation-email` edge functions deployed. Supabase secrets `REVENUECAT_WEBHOOK_SECRET` (freshly generated) and `RESEND_API_KEY` (David-supplied) set on the production project. EAS `production` environment populated with `EXPO_PUBLIC_SUPABASE_URL`/`EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY` pointing at `lilica-production`, plus the same RevenueCat public SDK keys already used in `preview` (RevenueCat is one shared project regardless of Supabase environment). The CLI was explicitly re-linked back to `lilica-development` afterward so day-to-day work is unaffected. The production database password is saved at `C:\Users\DavidPC\.lilica_production_db_password.txt`, outside the repo.

**RevenueCat production webhook integration created (David, manually, in the RevenueCat dashboard).** A second, separate webhook integration ("Lilica production entitlement sync") now points at `lilica-production`'s `entitlement-webhook` function, using the `REVENUECAT_WEBHOOK_SECRET` value already set as that project's Supabase secret. The original `lilica-development` webhook integration ("Lilica entitlement sync") is untouched and continues to serve development. Production RevenueCat events now reach the production database.

**Production Supabase Auth email (SMTP) configured (David, manually, in the Supabase dashboard).** Custom SMTP enabled on `lilica-production` using the same verified `luxfordinteractive.com` Resend domain and sender (`auth@luxfordinteractive.com`) as `lilica-development`, authenticated with the Resend API key David supplied (stored only as the `RESEND_API_KEY` Supabase secret, never in Git). Real users' signup verification codes and password-reset emails can now send reliably from the production project.

**Net effect:** the two remaining backend gaps that would have made a `production`-profile build non-functional for real paying customers (no entitlement sync, no reliable auth email) are now closed. A technically correct, fully backed `production`-profile build is now possible. This does **not** by itself authorise a production build, and it does not close the separate store-readiness gates (Apple subscription review screenshot/metadata, any real purchase/restore/renewal QA, complete store listings, Google Play Data Safety form, Google Play production-track eligibility, Apple reviewer demo access, and the manual "submit for review"/publish action in both consoles) -- all of which remain outstanding and are unrelated to any build. See `docs/BUILD_RELEASE_CONTROL.md`, `docs/NEXT_NATIVE_BUILD_MANIFEST.md`, and `docs/REVENUECAT_BILLING_SETUP_REPORT.md` for the current authoritative state of each.

## 19 September 2026 - Independent verification, RevenueCat cross-environment webhook bug fixed, and 3 production Auth config gaps closed

David then supplied a RevenueCat Secret API key (`sk_...`, saved outside the repo at `C:\Users\DavidPC\.lilica-credentials\revenuecat_secret_api_key.txt`), enabling independent verification rather than trusting manual-step completion at face value.

**Verified via the RevenueCat v2 API (`GET /v2/projects/projf734d172/integrations/webhooks`):** both webhook integrations genuinely exist -- `whintgr4d880b6211` "Lilica entitlement sync" -> `lilica-development`, and the new `whintgr9b96a2d848` "Lilica production entitlement sync" -> `lilica-production`.

**Bug found and fixed:** neither webhook had an `environment` filter set (both `null`), meaning RevenueCat was delivering every event -- sandbox test purchases and real production purchases alike -- to *both* Supabase projects. Fixed via `POST /v2/projects/projf734d172/integrations/webhooks/{id}` with `{"environment":"sandbox"}` on the development webhook and `{"environment":"production"}` on the production webhook. Each project's webhook is now correctly scoped to only its own environment's events.

**Verified via a scoped `supabase config diff`/`config push` against `lilica-production`:** custom SMTP was confirmed genuinely enabled (`host: smtp.resend.com`, `port: 465`, `user: resend`, `admin_email: auth@luxfordinteractive.com`) -- David's manual dashboard step was done correctly. The diff also surfaced two further real gaps David had not yet touched: `site_url` was still Supabase's default `http://localhost:3000` (would have sent real users' email confirmation/password-reset links to a dead localhost address instead of back into the app), and `otp_length` was `8` against the app's 6-digit-code UI/copy (would have broken signup/login verification outright for real users).

**Fixed precisely, not via a blind bulk push** (this repo's standing rule forbids that): `supabase/config.toml`'s ~8 other already-declared-but-unapproved differences (email template subject lines, `minimum_password_length`, MFA/Twilio toggles, storage/pooler settings) were temporarily edited to match production's *current* live values so the diff surfaced only the 3 approved fields, confirmed via `supabase config diff`, pushed via `supabase config push`, then `supabase/config.toml` was restored exactly via `git checkout --` (confirmed clean against HEAD afterward). Result: `site_url` -> `lilica://auth/callback`, `additional_redirect_urls` -> `["lilica://auth/callback","lilica://auth/recovery"]`, `otp_length` -> `6`, all confirmed live via a follow-up diff. As an inseparable side effect of the `otp_length` push, the confirmation/recovery email template *content* (the HTML body) was also synced to match `lilica-development`'s -- Supabase bundles a template's content with its own config service. `auth.sms.twilio.enabled` was left untouched -- Supabase's own config push refused to encode it ("cannot turn the active provider off"), so no unintended change landed there at all, not even the small one that had been anticipated. The CLI was re-linked back to `lilica-development` immediately after.

**Current state: the production backend is now genuinely complete and correctly configured** -- schema, edge functions, secrets, RevenueCat webhook (correctly scoped), and Auth (SMTP + URLs + OTP length) all verified, not merely assumed. Nothing here authorises a production build or public release; the store-readiness gates listed in the entry above remain fully outstanding.

## 19 September 2026 - Medical Log migration applied and proven

`supabase/migrations/20260917130000_medical_log.sql` (widens `records_type`, `record_domain_for_type()`, and `apply_record_mutation()`'s own type whitelist to accept `condition`/`medicine`) was, at David's explicit instruction, run for real: `npm run validate:backend` locally (Docker Desktop started fresh) recreated the local database from every migration and passed all pgTAP assertions -- 23 files, 496 tests, including `medical_log.test.sql`'s 9 assertions -- with only two pre-existing, unrelated lint warnings (`delete_care_space`, `accept_invitation_group`), neither introduced by this migration. `npx supabase db push --linked --dry-run` against `lilica-development` listed only this one migration, no seeds or roles; `npx supabase db push --linked` applied it; `npx supabase test db --linked` then passed the identical 23 files/496 tests against the hosted database. Local and linked migration histories now match through `20260917130000`. No Auth configuration, Storage resource, seed, role or production project was touched.

This closes the one outstanding technical gap named in the 17 September entry below and in `docs/NEXT_NATIVE_BUILD_MANIFEST.md`'s "Current limitations" -- the Medical Log's database side is now proven, not just hand-reviewed. This does not authorise a native build; the app-side `condition`/`medicine` record types were already committed source, and still await the next expressly approved iOS/Android build alongside the rest of the queued delta.

## 17 September 2026 - Post-build implementation batch: intro copy, biometric app lock, structured Medical Log

Three approved, scope-bounded product changes (`\downloads\lilbatch.txt`), implemented and validated on top of checkpoint `240708c`, committed and pushed as `55d4fc1` on David's explicit instruction.

**Intro copy.** The third Welcome intro screen's heading now reads exactly "Share and organise care, with clarity" (`src/screens/WelcomeScreen.tsx`). No other intro copy, layout, imagery, animation, navigation or timing changed.

**Biometric app lock.** A new, opt-in, device-level unlock layered on top of the existing Supabase session -- never a second Lilica identity, never a replacement for it. New native dependency `expo-local-authentication@~57.0.3`; `app.json` gained `NSFaceIDUsageDescription`. New `src/biometricLock.ts` (the native boundary and a `useBiometricLock()` hook), `src/components/BiometricLockScreen.tsx` (a full-screen cover rendered INSTEAD OF the app's real content while locked, never layered on top of it), a new "Security" row in `AccountScreen.tsx`, and `App.tsx` wiring. Account-isolated per device via a per-user AsyncStorage key (the same convention `src/storage.ts` already uses); locks on cold launch and on returning from a genuine background trip (a 5-second grace window absorbs brief interruptions like a share sheet); the OS device passcode is deliberately never accepted as a silent fallback (`disableDeviceFallback: true`); the only fallback when biometrics fail is signing out to the existing Lilica login. Real device biometrics are entirely untested until a new native build exists -- Expo Go cannot load this module at all. See `docs/NEXT_NATIVE_BUILD_MANIFEST.md` for the full native-build delta.

**Structured Medical Log.** Three deliberately separate areas -- Care needs, Diagnosed conditions, Prescribed medicines -- never a single generic text box. Care needs reuse the existing `careNote` type and its established editor/permission/sync path exactly as-is. Conditions and medicines are two genuinely new record types (`condition`/`medicine`, both mapped to the existing `health` permission domain), because they need their own active/closed lifecycle (`closedAt` -- closing is never deletion, and can be reopened) that no existing type has; medicines additionally carry a repeat-vs-duration schedule with an optional end date. New migration `supabase/migrations/20260917130000_medical_log.sql` widens the `records_type` CHECK constraint, `record_domain_for_type()`, and `apply_record_mutation()`'s own separate type whitelist (a second structural defence documented by `supabase/tests/database/record_domain_fail_closed.test.sql`) -- applied and proven on `lilica-development` 19 September 2026, see that entry above. New `src/screens/MedicalLogScreen.tsx`, reachable only from the Settings drawer's person-scoped group -- deliberately not added to `FirstThingScreen.tsx`'s protected everyday category stack, so Home's Add flow and the onboarding interest carousel are completely unchanged. Care-space/domain-permission isolation (Maggie's Medical Log never shows Ben's, a member without the health domain granted is refused server-side) reuses the exact existing Phase 15 enforcement -- no second permission system.

Validation: `npm run typecheck` clean; `npm test`: 104 suites/916 tests, ALL GREEN (100 suites/880 tests pre-batch, plus 4 new suites/36 new tests); `npm run secrets:check` clean (422 files); `npx expo config --type public` clean (confirms `NSFaceIDUsageDescription`, `ios.supportsTablet: false` unchanged, the approved icon unchanged); `npx expo export --platform web` clean; `git diff --check` clean (only the existing CRLF warnings). Backend/pgTAP validation could not run at the time (Docker Desktop off) -- see the 19 September 2026 entry above for the subsequent database-proven deployment. See `LILBATCH_COMPLETION_REPORT.txt` (repository root) for the full report, including everything discovered but deliberately not fixed, and the complete file list.

**No EAS build, submission, or OTA update was started or requested by this batch.** Both new features require a new native build before they exist anywhere outside this source tree -- biometric because it is a new native module, Medical Log because it ships in the same tree. Neither is deliverable by OTA to the existing iOS build 3/Android build 2. The next native build still requires David's separate, explicit approval.

## 16 September 2026 - Approved Lilica app icon

The product owner iteratively reviewed and approved a replacement native app icon: a rounded deep-terracotta `L`, a clearly separated muted-olive leaf, and a near-ink deep blue-teal field. The approved artwork is stored as `assets/lilica-app-icon.png` and is now referenced by Expo's shared icon, Android adaptive icon and web favicon configuration. The previous Expo placeholder assets remain in the repository but are no longer active icon references. No EAS build, store submission or OTA was started; the icon will reach installed native apps only through a future build explicitly approved by the product owner.

## 16 September 2026 - RevenueCat production-readiness preflight

The first external-subscription setup step is complete without creating any store or RevenueCat resources. RevenueCat webhook verification now follows the current timestamped `X-RevenueCat-Webhook-Signature` contract (`t=...,v1=...`), signs `timestamp.raw_body`, rejects malformed or replayed deliveries beyond five minutes, uses constant-time comparison, and retains provider-event idempotency. The purchase client now fails closed unless the current offering contains an Annual package, instead of falling back to an arbitrary first package.

The Subscription surface loads and displays the native store's formatted local annual price throughout the purchase CTA and renewal disclosure. If product metadata is unavailable, purchase is disabled rather than showing a potentially incorrect currency. Lilica's backend-owned 60-day free period remains separate from store billing: while it is active, no purchase CTA is offered and the app-level handler independently rejects an early purchase attempt. Restore Purchases remains available. Expired/read-only prompts now route users to the annual subscription without hard-coding a UK price before the localized store product is loaded. Public per-platform RevenueCat SDK key placeholders were added to `.env.example`; no real key or secret was added.

Focused evidence: `npm run typecheck` passes; 9 suites/103 assertions covering billing, entitlement copy, Subscription UI, read-only routing, webhook mapping/signatures and the Settings entry path pass. Secret scan, public Expo config, web export and `git diff --check` pass. The repository-wide combined Jest command retained its known timeout behaviour; no individual failure was reported before timeout. Expo's compatibility check continues to report the pre-existing Expo SDK 57 patch-level dependency drift; dependencies were deliberately not upgraded in this preflight. Real purchase/webhook delivery remains pending the later Apple, Google Play, RevenueCat, Supabase deployment and native-build steps.

## 16 September 2026 - Notification centre, exportable Care Summary and People refinements

Follow-up navigation correction: the persistent bottom tab bar now explicitly dismisses every secondary destination rendered ahead of the primary-tab branches before changing tabs. This fixes Exportable Care Summary appearing to ignore Home/Calendar/To Do/People presses and applies the same predictable "primary navigation wins" rule to Search, Recent Activity, Wellbeing updates, Care Circle, Join Care Circle and the full Contacts list.

The Settings drawer's Recent Activity shortcut has been replaced by a notification bell on all four primary tabs. Its account-and-care-space-scoped unread badge combines the existing server-authoritative, permission-filtered Care Circle activity feed with local reminder occasions and current overdue records. Opening it uses a soft bell-origin animation, marks the current items viewed on that device, opens related records through the existing shared editor, and retains the existing full Recent Activity screen behind "View all recent activity". This is an in-app centre, not remote push infrastructure.

Care Summary can now generate and share a detailed local PDF through `expo-print` and the existing native share sheet. The report uses the complete authorised record/member inputs supplied to the active care space rather than the on-screen five-item summary limits, includes the loaded recent-activity snapshot, escapes user content before HTML rendering, and carries a sensitive-information warning. No PDF or care data is uploaded by this feature. People's link now reads **Exportable Care Summary**.

The user-facing `task` category is renamed **Errand or activity** throughout onboarding/add/edit presentation while its canonical stored type remains `task`. People now places Key contacts on its own stable pale-blue nested background without changing its bounded preview, Add or View-all behavior. The Help group adds **Suggest a feature**, opening a pre-addressed email to `admin@luxfordinteractive.com`, and FAQ now explains PDF export and feature requests. A stable empty Care Circle member-array constant also fixes an existing repeated avatar-effect render loop exposed by the People regression suite.

Automated evidence at this checkpoint: TypeScript clean; 168 focused assertions pass across PDF generation/sharing, Care Summary, People, FAQ/help, category wording/editors, notification centre, Settings and protected Phase 22 visuals. The repository-wide Jest command retains its known combined-run timeout tendency, so these suites were run in bounded groups. Physical-device review remains required for PDF share/save presentation and the anchored notification animation.

## 16 September 2026 - Phase 22 implementation checkpoint and final To Do tile separation

The product-owner-approved Phase 22 visual direction has now been migrated in controlled batches across the typography/icon foundation, shared production foundation, Home, Calendar, To Do, People and secondary pages. Secondary pages use the agreed warm-neutral visual system without changing their product behavior; a physical-review correction places Back, centred title and Close on one coherent header row while preserving the Settings root header. The People Ask Lilica placeholder remains present.

The final To Do review found that record tiles lacked the breathing room and subtle lift already used by Home. List rows now use an 8px gap instead of 4px, and both List and Grid tiles use the existing shared `shadow.soft` elevation. Approved section panels, tile colours, icon colours, matte steel-blue gradient, filters, joined List/Grid control and tile-origin editor animation are unchanged. `npm run typecheck` and 22 focused To Do tests pass. No database, Supabase, authentication, billing, care-space or record-domain behavior changed.

## 15 September 2026 - Phase 22 visual direction approved, implementation not started

Following the source/screenshot audit in `docs/PHASE_22_VISUAL_AUDIT.md`, the product owner and Codex worked through the visual direction iteratively rather than accepting a one-shot redesign. The review covered the BBC-inspired premium hierarchy, To Do's matte mineral-blue gradient and surfaced status groups, the pale smoked-rose Overdue treatment, Home and To Do list/grid controls, the relationship between all four tabs, a warm-neutral secondary-page system, a compact single-header Settings drawer, compact humanist-sans typography with Fraunces reserved for the Lilica wordmark, and a coherent icon-led bottom navigation. Rejected iterations and the exact accepted constraints are recorded in `docs/PHASE_22_APPROVED_VISUAL_DIRECTION.md` alongside the visual references.

This checkpoint is documentation and mock imagery only. No application source, theme, component, dependency, database, Supabase configuration, runtime configuration or product behaviour was changed. The product owner has approved the visual direction, but Phase 22 implementation has not started. GPT review and a separate explicit implementation prompt remain required. The approved report defines protected behaviour, small implementation batches, per-batch stop points, automated validation, complete diff review and Android/iPhone physical QA as release conditions.

## 15 September 2026 - Care Circle invitation and multi-person access flows: physical QA PASSED, work complete

Physical QA has now passed on the complete Care Circle invitation system, including the multi-supported-person invitation behaviour added this week. The product owner confirmed, on a real device with two genuine accounts (davidmaruta2@gmail.com organising Maggie and Ben; dtm35@cam.ac.uk as invitee):

- the organiser sees both Maggie and Ben under "Who can they help with?" when inviting someone, and can explicitly select either or both;
- a Maggie+Ben invitation correctly represents both supported people throughout (creation, confirmation, email/Share wording, invitation code);
- "Join a Care Circle" is discoverable and the code-entry/preview/accept flow works;
- invitation acceptance succeeds, and the invitee's normal supported-person switcher subsequently shows exactly the authorised supported people.

This closes out the full sequence of Care Circle invitation work landed over 14-15 September 2026: real email/Share delivery, the invitation-code locator and its landing page, server-persisted delivery state, invitation-code rate limiting (and its later security correction, closing a real client-bypassable throttle), multi-person invitation groups (`care_space_invitation_groups`, `invite_member_group()`/`accept_invitation_group()`/`decline_invitation_group()`/`revoke_invitation_group()`), the organiser's supported-person selection UI, existing-member/additional-person handling, "Join a Care Circle" discoverability and a real navigation-state bug fix, and the real root-cause fix for the organiser-eligible-people list (previously populated only by a one-shot, un-retried reconnect call, now refreshed every time Care Circle opens). Full detail across every addendum in `docs/PHASE_15_ARCHITECTURE.md`.

Final validation on the exact committed tree: `npm run typecheck` clean; `npm run secrets:check` clean; `npx expo config --type public` clean; `git diff --check` clean; targeted Care Circle/invitation Jest suites green; local pgTAP for the invitation-code and multi-person-group migrations green (487/487, unchanged from the prior addendum). Committed and pushed to `prephase22-remove-supported-person-faq-help`. Phase 22 not started.

## 14 September 2026 - Care Circle final architectural closure: server-authoritative delivery state, invitation-code rate limiting, shared avatar visibility

Three corrections following GPT/product-owner review of the invitation & joining flow completion (same day, entry below). Full detail in `docs/PHASE_15_ARCHITECTURE.md`'s matching addendum - summarised here.

**Delivery state is now server-authoritative.** The prior entry's `invitationDeliveryStatus` (device-local, AsyncStorage) was correctly flagged as unacceptable for something Lilica itself already knows server-side. Three additive columns on `care_space_invitations` (`last_email_sent_at`, `email_send_count`, `last_share_opened_at`) plus two new organiser-only RPCs (`record_invitation_email_sent()` - called by the Edge Function only after Resend accepts; `record_invitation_share_opened()` - called by the client only after a non-cancelled native Share) now carry this truth. Survives app restart, reinstall, and a different device/session. The prior local-only mechanism (`App.tsx`'s `recordInvitationDelivery()`, `LocalCareSpaceState.invitationDeliveryStatus`) was removed as genuinely superseded.

**A real Postgres transactional-semantics bug was found and fixed while adding rate limiting.** Incrementing a counter and then later raising "not found" in the SAME function call rolls back the increment too - Postgres undoes an entire statement's effects on an uncaught exception. This would have silently no-opped the rate limiter for exactly the case it exists to catch. Fixed by splitting the throttle check into its own RPC (`record_invitation_code_attempt()`), called by the client before `resolve_invitation_by_code()`, so it commits independently. A second bug, found by the pgTAP test written to prove this: the throttle exception used SQLSTATE `P0004`, which collides with Postgres's own reserved `assert_failure` code - one of the two codes `WHEN OTHERS` explicitly does not catch. Fixed to use the plain default (P0001). Threshold: 10 attempts per rolling 5-minute window per account, in a table with no client-readable/writable RLS policies at all.

**Shared avatar visibility.** A direct product-owner report: a Care Circle member's real photo never showed to fellow members. `profile_avatars.sql`'s own header had explicitly deferred this decision at launch. One new Storage RLS policy now permits reading another member's avatar only while sharing an active care-space membership with them - proven to track membership directly (removal revokes visibility too).

`npm run typecheck` clean; `npm test`: 90 suites/808 tests, ALL GREEN; local pgTAP 458/458 (32 new assertions across three new test files); linked migration parity confirmed; linked `db lint` clean (same single pre-existing, unrelated finding). Two new migrations applied to `lilica-development`. Everything else remains uncommitted, per standing instruction.

## 14 September 2026 - Care Circle invitation & joining flow completion (`\downloads\carecircle.txt`)
## 14 September 2026 - Care Circle invitation & joining flow completion (`\downloads\carecircle.txt`)

A holistic, additive completion of the Care Circle invitation system, prompted by physical testing plus an agreed enhancement. Full detail in `docs/PHASE_15_ARCHITECTURE.md`'s matching addendum - summarised here.

**New: a human-friendly invitation code.** `care_space_invitations.invite_code` (additive column, server-generated, unique, 8 unambiguous characters, `supabase/migrations/20260916140000_invitation_code.sql`) is a pure locator for the SAME invitation an email/deep link already resolves - never a second security model. `resolve_invitation_by_code()` (new RPC) returns a minimal, safe preview only (never `invitee_email`, never `care_space_id`); acceptance still goes exclusively through the completely unmodified `accept_invitation()`, proven directly by a new pgTAP test (15 assertions) showing an unrelated authenticated account can preview a code but is refused when it tries to accept, while the genuinely invited account, resolving the same code, succeeds with exactly the invitation's own role/domains.

**New: "Join a Care Circle."** One new screen (`src/screens/JoinCareCircleScreen.tsx`, code entry -> calm review -> explicit Join) reachable from a genuinely new user's onboarding (one narrow routing fork, `src/screens/JoinOrSetupScreen.tsx`, inserted before - never replacing - the existing careFork screen) and from an existing user's own Care Circle screen ("Join another Care Circle"). Both converge on the same component and the same acceptance handler every other route already used.

**Fixed: a real, physically-tested UX bug.** The Care Circle screen used to show a just-created invitation twice at once (an "Invitation created" panel AND a duplicate "Pending invitations" row), and the panel never closed after a real send succeeded. CREATED and DELIVERED are now genuinely distinct, non-overlapping states, with truthful delivery-state text ("Invitation emailed."/"Sharing opened." - never "Invitation shared," since Android's Share API cannot confirm actual completion) persisted per device via a new optional `LocalCareSpaceState.invitationDeliveryStatus` field - no schema change, reusing the exact same AsyncStorage persistence every other local-only UI truth already goes through, since the server itself genuinely has no delivery-state column.

**Email and Share now also carry the code**, matching the brief's own example wording exactly; the web link gained an optional `&code=` query parameter so the hosted landing page can show it as a pre-launch fallback.

**A real, pre-existing gap was found and flagged, not fixed** (out of this brief's own scope - "unrelated" changes were explicitly forbidden): `friendlyAuthError('profile')`, used by every OTHER function in `src/careCircle.ts`, silently replaces every RPC's own specific error text with a generic "Your profile could not be saved just now" fallback. The new `resolveInvitationByCode()` deliberately does NOT use this shared helper, for exactly this reason - but the existing gap in `inviteMember`/`acceptInvitation`/`declineInvitation`/`revokeInvitation` remains, named honestly here for a future, properly-scoped fix.

**Validated end-to-end against the real `lilica-development` database**, not only pgTAP: a genuine invitation, its real server-generated code, a real send through the deployed `send-invitation-email` Edge Function, a genuinely unrelated account's preview, and that same account's correctly-refused accept attempt.

`npm run typecheck` clean; `npm test`: 88 suites/787 tests, ALL GREEN; `npm run secrets:check` clean; `npx expo config --type public` clean; `git diff --check` clean; local pgTAP 430/430 (+15 new); linked migration parity confirmed; linked `db lint` clean (only the same pre-existing, unrelated `delete_care_space` finding). Migration applied to `lilica-development`. Everything else remains uncommitted on the main working branch, per this task's own explicit stop condition ("DO NOT COMMIT. DO NOT PUSH.").

## 14 September 2026 - Invitation deep-link physical test failure investigated: root cause is Expo Go, not a code defect; landing-page copy corrected; Care Circle invitation delivery UX corrected

Two separate corrections from the same day's physical testing, both detailed in full in `docs/PHASE_15_ARCHITECTURE.md`'s own matching addenda -- summarised here:

**1. Invitation delivery UX correction.** Physical testing found the Care Circle screen showing the same just-created invitation TWICE at once -- an "Invitation created" panel AND a "Pending invitations" row, each with its own Send/Share actions, and the panel never closing after a real send succeeded. Rewired so CREATED and DELIVERED are two genuinely distinct, non-overlapping UI states for the same invitation row: the creation panel is excluded from the Pending list while showing; a real email/share success closes it and the SAME invitation reappears exactly once, truthfully labelled ("Invitation emailed."/"Sharing opened." -- never "Invitation shared," since Android's Share API cannot confirm actual completion, only that the sheet opened); a failure or cancellation changes nothing; Dismiss closes the panel without destroying the invitation, which then shows as "Not sent yet." This truthful delivery state is now genuinely PERSISTED per device (a new optional `LocalCareSpaceState.invitationDeliveryStatus` field, saved through the exact same `JSON.stringify(state)` AsyncStorage path every other local-only UI truth already uses -- no schema/migration, since the server itself has no delivery-state column and none was added). Honest, named limitation: this is per-device memory only, not synced across an organiser's other devices.

**2. Invitation deep-link physical test failure, investigated and NOT found to be a code defect.** A real iPhone test: email delivery and the hosted landing page both passed; tapping "Open in Lilica" produced iOS's "address is invalid" error. Full audit of the link chain (email -> `invitationLinks.ts` -> deployed landing page script -> `app.json`'s scheme declaration -> `App.tsx`'s Linking hook) found ONE consistent canonical contract throughout, syntactically well-formed. Root cause: no EAS build of Lilica has ever existed, so the test device was running Expo Go, which cannot register a third-party app's own custom URL scheme -- a fundamental Expo Go limitation, confirmed by direct configuration inspection (`npx expo config --type introspect`), not a Lilica bug. No code fix exists for this; a real installed build is required before this specific step can be physically retested. Separately, genuinely fixed regardless: the landing page's copy previously implied a failed tap proves the app isn't installed, which the page cannot know -- split into two calmer, separate, accurate statements.

`npm run typecheck` clean; `npm test`: 87 suites/773 tests, ALL GREEN; `npm run secrets:check` clean; `npx expo config --type public` clean; `git diff --check` clean. No database/schema change. Not committed or pushed on the main working branch. The landing-page copy fix was made only to the local reference copy (`public/invite/index.html`) -- NOT pushed to the live `github-pages-hosting` branch this pass, since this task's stop condition did not carve out that exception this time.

## 14 September 2026 - Invitation journey physical closure: custom domain live, 404-vs-200 routing decision, real application-path email test, onboarding fix now unit-tested

Second closure pass on the same day, following GPT/product-owner review of the prior entry below. Three substantive changes:

**1. Custom domain completed.** The four required A records were added at GoDaddy for `lilica.co.uk` (with explicit product-owner authorisation, after first checking - and disclosing - that the apex was already serving a real, non-empty GoDaddy Website Builder page that this would replace). DNS genuinely propagated (confirmed against both 8.8.8.8 and 1.1.1.1) and the site serves correct content at the apex. TLS for the custom domain is still mid-provisioning as of writing - GitHub is currently serving its generic `*.github.io` certificate rather than one issued for `lilica.co.uk` (confirmed by direct certificate inspection), which is GitHub's normal automatic delay after DNS verification, not a configuration error; nothing further can be done to force it. Resend's own SPF/DKIM/DMARC/MX records were re-confirmed fully intact and the domain still reports `verified` afterward.

**2. The /invite/<id> 404-vs-200 routing question was resolved deliberately, not left as-is by default.** The prior GitHub Pages 404.html SPA-fallback trick served the correct landing-page body but left the real HTTP status at 404 - a genuine risk for an email link, since corporate link-scanners and some webmail preview crawlers judge a link by its status code rather than its rendered body. Moved the invitation id from a path segment to a query parameter (`https://lilica.co.uk/invite/?id=<id>`), a real static file returning a genuine 200 on every static host, needing no path-based server routing at all. Changed consistently across the one canonical format's every use: `src/invitationLinks.ts` (`invitationWebUrl`/`parseInvitationIdFromUrl`, the latter keeping backward-compatible parsing of the old path form as a harmless fallback since nothing generates it anymore), the deployed `send-invitation-email` Edge Function's `joinUrl` (redeployed), and `public/invite/index.html`'s own client-side parsing script (updated identically on the isolated `github-pages-hosting` branch's `public/invite/index.html` and `public/404.html`, committed `5f1466f`, pushed, and deployed - GitHub's own environment protection rules had separately, silently locked Pages deployment to `master` only after the first deploy; fixed by adding `github-pages-hosting` as an allowed deployment branch). The custom-scheme app link (`lilica://invite/<id>`) is unaffected - it never touches an HTTP server, so it kept its simpler path form. Tests updated to match (`tests/invitation-links.test.ts`, `tests/care-circle-invitation-delivery.test.tsx`).

**3. The brand-new-invitee onboarding fix now has real, isolated unit-test coverage**, closing the one honest gap in the prior entry. The fix's state-transition logic was extracted from `App.tsx`'s `handleAcceptInvitation()` into a small, pure, exported function (`resolveOnboardingStateAfterAcceptingInvitation()` in `src/careSpaceState.ts`, alongside its existing tested siblings like `integrateReconnectedCareSpaces()`) - a deliberately minimal extraction, not the giant App.tsx render-harness refactor the brief explicitly warned against. New tests (`tests/resolve-onboarding-after-accept-invitation.test.ts`) directly prove: a brand-new invitee resolves to `onboardingComplete: true, stage: 'home'`, never `initialPersonStage()`'s branches; an existing organiser accepting a second invitation is a true no-op (same object reference, untouched); and decline's fall-through path stays intact since it never calls this function at all.

**Real application-path email test.** Rather than a direct Resend API call, a genuine invitation was created and sent through the actual production RPC/Edge-Function pipeline: a disposable, admin-created (pre-confirmed, since completing a real signup's email-OTP step needs a human inbox this agent cannot read) test organiser account signed in with a real password grant, called the real `bootstrap_supported_people` and `invite_member` RPCs, then called the deployed `send-invitation-email` Edge Function with that organiser's own real `Authorization` header - exactly the path `CareCircleScreen.tsx`'s `sendInvitationEmail()` takes. Resend confirmed `last_event: delivered`; fetching the email back from Resend's own API confirmed its exact text contains the new canonical URL: `https://lilica.co.uk/invite/?id=<the real invitation id>`. The same invitation was then revoked via the real `revoke_invitation` RPC, and `accept_invitation` against the now-revoked id was confirmed rejected server-side (`Invitation not found`) - proving revoke is enforced by the server, not merely hidden client-side. The disposable test organiser account and its "Testina" care space remain in `lilica-development` only (never production) as harmless disposable test content, per the brief's own explicit allowance - deleting them isn't possible without going through the separate `delete_my_account()` operation, since `auth.users` is `ON DELETE RESTRICT`-referenced from `care_space_memberships` by design (see this file's Phase 18B entry).

`npm run typecheck` clean; `npm test`: 85 suites/752 tests, ALL GREEN; `npm run secrets:check` clean; `npx expo config --type public` clean; `npx expo export --platform web` clean; `git diff --check` clean; no new database migration this pass. Every genuinely physical-device item (tapping a real link, a real mobile browser render, `Open in Lilica` on an installed build, restricted-permission projection walkthroughs) remains explicitly NOT RUN - none of it can be honestly claimed from an automated check, and none was fabricated. See the completion report for the full physical QA matrix and exact outstanding release-configuration dependencies. Main working branch remains uncommitted, per standing instruction; the only pushed work is the isolated `github-pages-hosting` branch's routing fix.

## 14 September 2026 - Invitation journey closure: GitHub Pages hosting, brand-new-invitee onboarding fix, real journey tracing

**GitHub Pages hosting**, per the product owner's own explicit decision (free, git-based static hosting). Isolated entirely to a new `github-pages-hosting` branch, created from a clean `master` via a separate git worktree so none of the wider uncommitted Care Circle/Phase 20D work was ever touched - that branch contains ONLY `public/` (the invitation landing page, root page, and `.well-known` files) and a GitHub Actions workflow (`actions/upload-pages-artifact` + `actions/deploy-pages`) that deploys just that folder. GitHub Pages was enabled via the API (`build_type: workflow`) and the deployment was run and verified for real: `https://davidmaruta2.github.io/lilica/` returns 200, `/invite/<id>` resolves via the classic GitHub Pages 404.html-fallback technique (a real 404 status, serving the same page content, while the browser's own URL bar - and this page's own client-side path parsing - is unaffected), and both `.well-known` files return 200. The GitHub Pages custom-domain SETTING was pointed at `lilica.co.uk` (a safe, reversible, GitHub-side-only change) - but no DNS record was touched. Adding the required DNS (four A records at GoDaddy, GitHub's own long-published apex-domain values) is left for the product owner to do themselves, deliberately, since it's a live, hard-to-reverse change to a domain whose existing Resend mail records must not be disturbed - see the completion report for the exact records.

**A real, previously-unfound bug in the brand-new-invitee journey** was found by directly tracing `App.tsx`'s own top-level render-priority chain (not assumed): `onboardingComplete` is set to `true` in exactly one place, `completeOnboarding()` (finishing the ordinary "create your first supported person" flow) - `handleAcceptInvitation()` never set it. A genuinely new user who signs up and accepts a Care Circle invitation WITHOUT ever going through that ordinary flow therefore had `onboardingComplete` stuck at its default `false` forever; `renderAuthenticatedOnboarding()`'s own stage-resolution logic (`state.onboardingComplete ? 'home' : initialPersonStage(state)`) would then route them straight into the normal "who are you caring for" onboarding on the very next render, despite already having real, accepted access to a shared care space - exactly the failure mode the brief's own brand-new-invitee test worried about. Fixed with the smallest safe conditional change: `handleAcceptInvitation()` now applies the exact same state transition `completeOnboarding()` already uses (`onboardingComplete: true, stage: 'home'`) whenever it wasn't already true - a no-op for an existing organiser accepting a second invitation. This fix could not be covered by an automated test (this project has no App.tsx-level render harness, by established convention - see `tests/settings-navigation.test.tsx`'s own header comment); it is verified by direct logical tracing of the exact state values at each step, stated honestly as such rather than claimed as an automated PASS.

Also confirmed by direct tracing, unchanged, already correct: the post-auth routing priority (`showInvitations` is checked before the onboarding-stage switch, so a pending invitation is always shown before any onboarding screen); decline-with-no-other-invitation correctly falls through to normal onboarding (`onboardingComplete` legitimately stays `false`); multiple pending invitations are handled independently (`showInvitations` only auto-closes once the list is genuinely empty); the email Join button and native Share already used the canonical `https://lilica.co.uk/invite/<id>` HTTPS URL, never the custom scheme directly - no change was needed for either.

`npm run typecheck` clean; `npm test`: 84 suites/748 tests, ALL GREEN (unchanged from the prior entry - this pass added no new automated tests of its own, per the honest limitation above). No new database migration. Not committed or pushed on the main working branch - the ONE deliberate exception is the isolated `github-pages-hosting` branch, which was pushed because GitHub Pages cannot serve content that doesn't exist on the remote; see the completion report for the exact, narrow scope of what that branch contains.

## 14 September 2026 - Care Circle invitation delivery: real email, native Share, deep linking

Implemented Part B of the Care Circle security/invitation-delivery closure brief. **Provider: Resend**, reusing the exact same account already sending Lilica's Supabase Auth verification/password-reset emails (the product owner's own explicit choice, not a second provider) - domain `lilica.co.uk` was added and verified in Resend during this session, and a scoped API key was set as the `RESEND_API_KEY` Supabase Edge Function secret (`lilica-development` project, via `npx supabase secrets set`, never committed to the repository).

**Secure token**: no new column was added. `care_space_invitations.id` (a server-owned `gen_random_uuid()`, already cryptographically unguessable, already tied to exactly one row, already naturally single-use via the row's own `status`) already satisfies every requirement in the brief - using it directly, rather than adding a redundant second token, is the smallest safe change. **Claim rule**: already correctly implemented in `accept_invitation()` (`lower(invitation.invitee_email) <> caller_email` check, using the authenticated user's own verified email from `auth.users`) - audited and confirmed, not rebuilt. **Expiry**: already existed (14 days, lazily enforced) - preserved unchanged.

**Email**: new Edge Function `supabase/functions/send-invitation-email` (deployed), a user-scoped Supabase client (never service-role) so the existing `care_space_invitations_select_organiser` RLS policy is the sole authority check. Content generation is a pure, unit-tested module (`email-content.ts`, mirroring `entitlement-webhook`'s own `mapping.ts` pattern) - restrained and privacy-safe (inviter's name, supported person's first name, a Join link; never a record title, task, bill, health note, or financial detail). **Verified end-to-end, for real**: a genuine test email was sent to `dtm35@cam.ac.uk` and confirmed `"last_event":"delivered"` via Resend's own API before this was considered done - not merely "the API accepted the request."

**Client**: `sendInvitationEmail()` (`src/careCircle.ts`), `inviteMember()` now returns the created invitation's own id (needed to immediately offer delivery for that specific invitation). `CareCircleScreen.tsx` gained a `DeliveryActions` component (Send by email / Share invitation, shared between "just created" and "resend an existing pending invitation" - both always target the same existing row, never creating a second one) with honest state distinguishing "Invitation created" from "Invitation emailed" from a genuine failure ("The invitation was created, but the email couldn't be sent" + Retry, never a fabricated success). `InvitationsScreen.tsx` now shows each granted domain's own full `DOMAIN_DESCRIPTIONS` text (the same source of truth the organiser sees), not just its bare label.

**Native Share**: `Share.share()` with inviter name, supported person's first name, and the invitation link - no protected care content, no separate WhatsApp/SMS integration (the native share sheet handles that).

**Deep linking**: `src/invitationLinks.ts` (the one canonical link format/parser, shared by email/share/deep-link handling) and `src/invitationDeepLink.ts` (a second, independent `Linking` listener alongside `AuthProvider`'s own existing one - RN's Linking emitter calls every subscriber, so this never interferes with the existing auth-callback handling). A signed-in user tapping an invitation link jumps straight to the existing Invitations review screen; a signed-out tap relies entirely on the pre-existing, already-correct pending-invitation discovery (`list_my_invitations()`, checked on every signed-in bootstrap) once they authenticate - no new onboarding branch was added, per the brief's own explicit instruction. `app.json` gained `ios.associatedDomains` (`applinks:lilica.co.uk`) and Android `intentFilters` for the same domain, plus placeholder `.well-known/apple-app-site-association` and `.well-known/assetlinks.json` files (`public/.well-known/`) and an honest static landing page (`public/invite/index.html`, no App Store/Google Play buttons - **Lilica has never been built via EAS or submitted to either store, so those links would have nowhere real to point; a fake button would be actively misleading**).

**Explicitly NOT working yet, and why**: universal/app links (tapping `https://lilica.co.uk/invite/...` and having it open Lilica directly, bypassing the browser) require Lilica's real Apple Team ID and Android app-signing certificate fingerprint published in the two `.well-known` files above - both are placeholders today. The `lilica://` custom-scheme link (already registered) works today for anyone who already has Lilica installed. The landing page itself is written but not yet deployed/hosted anywhere - `lilica.co.uk`'s DNS points at Resend's mail records only so far; serving the static page requires a separate hosting choice (documented in the page's own header comment: Netlify/Vercel/GitHub Pages, each with its own required rewrite rule for `/invite/*`).

`npm run typecheck` clean; `npm test`: 84 suites/748 tests, ALL GREEN (fixed two pre-existing tests whose assertions assumed the OLD `inviteMember()` return shape and the old bare-label-only invitation display, both superseded by this work). `npm run secrets:check`, `npx expo config`, `npx expo export --platform web`, `git diff --check` all clean. No database migration was needed for this Part B work (the existing schema already had everything required). Not committed or pushed.

## 14 September 2026 - Care Circle security closure: Everyday copy, fail-closed record-domain mapping, assignments domain leak fixed, clock-dependent test fixed

Closure follow-up to the Care Circle permission-domain audit, reviewed by GPT and the product owner. Four items closed:

1. **Everyday copy corrected**: removed the "plus anything else not covered by the categories below" clause (`DOMAIN_DESCRIPTIONS.general`, `src/careCircle.ts`) - the schema permits exactly eight record types today, all explicitly mapped, so the UI should not suggest an open-ended fallback exists.

2. **`record_domain_for_type()` hardened to fail closed** (`supabase/migrations/20260916090000_fix_assignments_domain_scoped_read.sql`): every one of the eight real record types now has its own explicit branch; any other value now raises an exception rather than silently defaulting to `general`. The `records_type` CHECK constraint already made an unmapped type impossible to store - this is now a second, independent structural defence, not reliance on the constraint alone. New pgTAP (`record_domain_fail_closed.test.sql`, 10 assertions) found and proved a THIRD, even earlier layer not previously documented: `apply_record_mutation()` has its own explicit whitelist check on `record_type` that rejects an unmapped value before the insert (and therefore before the CHECK constraint or this function) is ever reached.

3. **Fixed the genuine `assignments` read-policy inconsistency** flagged by the earlier audit: its SELECT policy checked blanket `'general'` domain access rather than the specific record/occurrence's own domain, letting a general-only member technically read assignment metadata (never record content) for a financial/health/home/documents record. Fixed by resolving the actual owning record's domain (via `record_id` directly, or `occurrence_id` -> `occurrences.record_id`) - matching every other per-row policy in this schema. New pgTAP (`assignments_domain_scoped_read.test.sql`, 9 assertions) proves a general-only member is denied for every other domain, gains access once granted, and that organiser access and assignment-mutation authority are both unaffected.

4. **Fixed the known clock-dependent test** (`tests/home-strip-navigation.test.tsx`): one `describe` block used the real system clock (`new Date().toISOString()`) to build a "due today" record while `ToDoScreen`'s own grouping compares local date components - a UTC/local mismatch near a timezone boundary could silently misclassify the record. Fixed the TEST (added the same `jest.useFakeTimers()`/`setSystemTime()` pattern this file's own sibling blocks already use) - no production code changed.

New migration applied to `lilica-development` after a clean dry-run; 415/415 pgTAP assertions pass (local and linked, up from 396). `npm run typecheck` clean; **`npm test`: 80 suites/727 tests, ALL GREEN** (the previously-known failing test is now fixed, not merely excluded). `npm run secrets:check`, `npx expo config`, `npx expo export --platform web`, `git diff --check`, and `npx supabase db lint` (local and linked) all clean - the lint output shows only the same pre-existing, unrelated `delete_care_space` advisory note already present before this task. Not committed or pushed.

**Part B of this same brief (a complete Care Circle invitation delivery system - secure tokens, transactional email, deep linking, a not-installed landing page, native Share) was intentionally NOT started in this pass.** It requires product-owner decisions and external account provisioning (an email-provider account and verified sending domain, at minimum) that cannot be made or invented unilaterally - see the conversation for the scoping question raised before any Part B implementation begins.

## 15 September 2026 - Care Circle permission-domain explanations, and a full domain/permission audit

Direct product-owner question while physically reviewing the Care Circle invite flow: "the What can they see options seem too few to cover all the different record types... how will the things not represented by the options be permissioned?" A full code-level audit (not from memory) confirmed the five domains ARE exhaustive: every one of Lilica's eight canonical record types (`records_type` check constraint: `appointment`, `task`, `bill`, `homeMatter`, `document`, `contact`, `careNote`, `update`) resolves to exactly one of the five permission domains via `record_domain_for_type()`, a `generated always as (...) stored` column on `records` - not client-suppliable, cannot diverge from `record_type`. `appointment`/`task`/`contact`/`update` fall through an explicit `else -> general` catch-all; the CHECK constraint on `record_type` means no unmapped/unknown type can be inserted at all, so that catch-all is defence-in-depth, not an active gap today. `membership_has_domain_access()` denies by default (no explicit grant row = no access) - fails closed, not open. Attachments (`record_attachments`) inherit their PARENT record's own domain via the RLS policy's own join, never a separate "documents" classification regardless of file type. One pre-existing, narrow nuance was found and is recorded (not fixed, out of this task's scope): the `assignments` table's own read policy gates on blanket `'general'` access rather than the underlying record's own domain, so an assignment row's existence/assignee-name (never the record's content) is technically visible to a member without that record's own domain grant - flagged for a future explicit product-owner decision.

Fixed the actual product ask: `CareCircleScreen.tsx`'s "What can they see?" selector, previously a wrapping pill row with no explanation at all, is now a vertical list where each domain stays compact until selected, then reveals its own real access description directly beneath it (independently - multiple selected domains each show their own line, never a collapsed vague summary), returning to compact when deselected. New `DOMAIN_DESCRIPTIONS` (`src/careCircle.ts`) written to match the verified mapping exactly, in plain user-facing language with no internal record-type names. Each row's `accessibilityLabel` states the domain name, selection state, and (when selected) exactly what it grants, for screen-reader users. The invitation payload (`grantedDomains`) is completely unchanged - proven directly by a new test. 11 new tests (`tests/care-circle-domain-explanations.test.tsx`) cover all five domains rendering, each one's explanation appearing/disappearing correctly, multiple simultaneous selections, no domain's description leaking into another's, the unchanged payload, and accessibility labelling.

Also directly investigated and answered: "I sent an invite... nothing was received" - confirmed by code inspection that Lilica has never sent an invitation email to anyone; `invite_member()` only ever creates a database row, discovered by the invitee only once they sign in to Lilica with that exact email address. Not a bug - a known, already-documented gap (CLAUDE.md's own standing note not to represent email/push invitation delivery as complete).

`npm run typecheck` clean; `npm test` 80 suites/727 tests (one pre-existing, unrelated, system-clock-dependent test in `tests/home-strip-navigation.test.tsx` fails independently of this work - confirmed via `git status` that neither `HomeScreen.tsx` nor `ToDoScreen.tsx` were touched by anything in this session; every other suite passes). Not committed or pushed.

## 15 September 2026 - Phase 20D closure correction: subscription disclosure in the multi-organiser deletion journey; a second, deeper reconnect bug fixed

**Correction, same day as the structural closure below.** The structural-closure completion report deferred adding the App Store/Google Play subscription disclosure to the new multi-organiser deletion request/approval journey. GPT + product owner review required this not be deferred. Fixed: `RemoveCareSpaceConfirm.tsx` gained a `requiresAllOrganisers` prop distinguishing sole-organiser wording ("This cannot be undone." / "Removing does not itself cancel...") from multi-organiser wording ("Once every organiser agrees, this cannot be undone." / "all active organisers must agree..." / "Requesting removal does not itself cancel..."); `ManageCareScreen.tsx`'s pending-approval card shows its own calm permanence-plus-disclosure line to the organiser being asked to approve. Direct inspection while making this fix found the original sole-organiser Remove flow never actually carried this disclosure either - the earlier report's claim that it was "already correct and already shipped" was mistaken; both paths carry it now, for the first time. New/extended tests prove all five required points (permanence, agreement requirement, subscription disclosure on both the request and approval steps, and that sole-organiser behaviour is unchanged) - `tests/remove-care-space.test.tsx`, `tests/manage-care-screen.test.tsx`.

**A second, deeper cause of the "I deleted Beauty and Janet but still see Janet" report was also found and fixed**, after the product owner confirmed the bug survived a genuinely fresh Expo Go session (fully closed and rescanned): `integrateReconnectedCareSpaces()` - which runs on every app startup - had always been designed to only ADD or UPDATE local care-space entries from the server's response, deliberately NEVER removing one the server no longer lists (correct for a transient reconnect glitch, wrong once a care space is genuinely, permanently gone). A previously-synced care space that is later deleted or has its membership revoked therefore survived in local storage forever, since every future app launch just kept merging into the stale copy rather than ever re-examining whether it should still exist. Fixed narrowly: a care space is now pruned on reconnect only when it was previously known to be server-synced (has its own `membershipId`, is not `local-`-prefixed) AND the server's own authoritative response no longer lists it - a local-only, never-synced space is never touched. New test file `tests/reconnect-prunes-deleted-care-space.test.ts` (4 tests) proves the exact reported scenario, that only the removed space is pruned, that a local-only space is never pruned, and that the original "never wipe out a space just because one request omitted it" guarantee for spaces the server DOES still list is unaffected.

Also investigated and reconfirmed (not a code issue): a floating gear-shaped overlay reported as present on iOS, absent on Android - a full search of every commit in this repository's entire git history found no matching element anywhere, ever; this remains Expo Go's own development-mode chrome (documented 11 September 2026), not a Lilica defect.

`npm run typecheck` clean; `npm test` 79 suites/716 tests (up from 78/709), zero regressions. No database/schema change was required or made, confirmed. Not committed or pushed.

## 15 September 2026 - Phase 20D structural closure: Archive/Restore, multi-organiser delete consent, organiser handoff, Documents, Manage [Name]'s care

Implemented from the approved `\downloads\20-22.txt` brief, building on the reviewed `docs/PHASE_20D_CARE_SPACE_LIFECYCLE_PROPOSAL.md`/`docs/PHASE_20E_DOCUMENTS_PROPOSAL.md`. This is the final structural/product-architecture pass before Phase 22 visual polish resumes (still not started).

**ARCHIVE** (new, reversible, non-destructive): `care_spaces.status` (`active`/`archived`), `archive_care_space()`/`restore_care_space()` (any active organiser, idempotent). History, records, documents, membership all survive unchanged. The single existing `can_access_care_space_records()` write-gate (already the one choke point every record/occurrence/attachment/link mutation calls) now also denies every `'write'` action against an archived care space - one surgical change automatically covers every existing mutation RPC; reads are deliberately unaffected. `invite_member()` gained its own explicit archive check (Care Circle invitations aren't routed through that shared gate). Client: `src/components/ArchivedGate.tsx` (its own calm wording, never billing copy - brief section 7), wired into the existing Phase 21C `guardMutation`/`isReadOnly` mechanism generalised to also check `isArchived`; `src/screens/ArchivedCareScreen.tsx` (an "Archived care" row in Settings' Account group, shown only when the account has at least one).

**DELETE, tightened**: the existing, already-shipped `delete_care_space()` is retained (checkbox confirmation, cascade, document cleanup all unchanged) but its authority model is tightened - a sole active organiser deletes immediately as before; with multiple active organisers, one alone can no longer delete unilaterally. New `care_space_deletion_requests`/`care_space_deletion_approvals` tables (deliberately `ON DELETE CASCADE`, ephemeral workflow state, not permanent audit) back `request_care_space_deletion()` (the requester's own approval counts automatically; a sole organiser is deleted immediately, no request row created), `approve_care_space_deletion()` (performs the actual deletion once every active organiser has approved), `decline_care_space_deletion()` (any active organiser), `cancel_care_space_deletion()` (the original requester only), and `get_care_space_deletion_status()`. A real bug found only by running the extended pgTAP suite: `care_space_deletion_requests.requested_by_membership_id` is a second, separate non-cascading FK to `care_space_memberships` (its own `care_space_id` FK cascades, but only once `care_spaces` itself is deleted, which happens after memberships) - fixed by explicitly deleting the request row inside `delete_care_space()`'s existing cascade sequence, the same class of bug already found once before for `care_space_domain_grants`.

**HANDOFF**: `promote_to_organiser()` - the one thing genuinely missing, since `leave_care_space()` already correctly blocked a sole organiser from leaving. `leave_care_space()` gained one more check (organiser-count invariant checked first, since it's the more fundamental blocker): an organiser who is also the care space's `commercial_owner_id` must transfer commercial ownership (Phase 21B's existing `transfer_care_space_commercial_ownership()`, reused unchanged) before they can leave.

**DOCUMENTS**: `src/documents.ts` + `src/screens/DocumentsScreen.tsx`, a pure client-side projection over the existing `record_attachments` table (Phase 16) - no new schema, no new permission model, modelled directly on `ContactsListScreen.tsx`'s own bounded-list architecture per the brief's explicit instruction. A real bug found by testing: on a fetch failure, `documents` state was left `undefined` forever, so the "Loading…" branch (checked first) never released to show the real error - fixed by setting it to `[]` alongside the error.

**MANAGE [NAME]'S CARE**: `src/screens/ManageCareScreen.tsx` consolidates the above into one destination with an explicit dangerous-action hierarchy (status/handoff first, Archive/Restore next, permanent removal last, its own visually-separated danger area) - links to, rather than duplicates, Care Circle.

**Final drawer structure**: `"[Name]'s care"` now also carries Documents and Manage [Name]'s care (added only once each destination was genuinely working, per the brief's own sequencing instruction); Account and Help groups unchanged.

New migration `20260915090000_phase20d_lifecycle_and_documents.sql`, applied to `lilica-development` after a clean dry-run. `npx supabase test db` (local and linked): 396/396 pgTAP assertions pass (up from 358), including two new files (`care_space_lifecycle.test.sql`, `care_space_deletion_consent.test.sql`) plus an extended `delete_care_space.test.sql`. `npm run typecheck` clean; `npm test` 78 suites/709 tests (up from 72/671), zero regressions. `npx supabase db lint`: one pre-existing, unrelated advisory (`delete_care_space`'s own `caller_membership` variable, present before this task) - no new lint findings. Not committed or pushed, per the brief's own explicit instruction.

**Two genuine, unrelated live bugs were also found and fixed during this same session, from direct product-owner reports while testing:**
- `projectActiveCareSpace()` left every projected field (most visibly `supportedPersonName`, which Home reads for its avatar) stuck at the last value when the LAST remaining care space was removed, instead of clearing it - "I deleted Beauty and Janet but the app home page dashboard still shows Janet avatar." Fixed with a regression test quoting the exact report.
- The Settings drawer's row list was a plain, non-scrolling `View` - as more rows were added this session (Documents, Manage [Name]'s care, Archived care, Contact) it began overflowing the screen with no way to reach the rows below the fold. Fixed by wrapping it in a genuine `ScrollView`.

A separately reported symptom (a floating gear-shaped overlay, blue on iOS, absent on Android) was investigated again by direct code search and no matching element exists anywhere in Lilica's own source - it continues to match the dated, already-documented Expo Go development-chrome finding (11 September 2026) rather than a Lilica defect; not otherwise actioned in this entry.

## 14 September 2026 - FAQ and "How to use Lilica" added to Settings

Two new, always-available Settings drawer entries, direct product-owner request: a new "Help" group (below Account, above the footer) with **How to use Lilica** (`src/screens/HowToUseScreen.tsx`) - a short, numbered walkthrough of Home/Calendar/To Do/People/Care Circle/Settings and how to record something, stay up to date, and work offline - and **FAQ** (`src/screens/FaqScreen.tsx`) - fourteen real questions covering care spaces, Care Circle roles, adding/removing a supported person (the feature shipped earlier the same day), leaving a care space, reminders, documents, Recent Activity/Care Summary, data export, account deletion and its subscription caveat, and offline behaviour - rendered as the same collapsed-by-default accordion `PrivacyDataScreen.tsx` already established. Both are static reference content: no new data source, no new RPC, no permission gating - offered unconditionally, like Account. Wired into `App.tsx`'s existing `settingsSection` in-drawer pattern (two more branches, no new state mechanism). New tests: `tests/faq-howto-screens.test.tsx`; one pre-existing test in `tests/settings-cog.test.tsx` asserting "no Help/About row exists" was updated to match reality (Help now exists; there is still genuinely no About row). `npm run typecheck` clean; `npm test` 72 suites/669 tests, zero regressions. Not committed or pushed.

**Same day, follow-up:** a third Help entry, **Contact** (`src/screens/ContactScreen.tsx`), was added directly below FAQ per further direct product-owner request - shows the real support address (`admin@luxfordinteractive.com`, exported as `SUPPORT_EMAIL`) and a genuine `Email us` action (`Linking.openURL('mailto:...')`, the same pattern `SubscriptionScreen.tsx`'s "Manage subscription" link already uses) - never a fake button. `npm run typecheck` clean; `npm test` 72 suites/671 tests, zero regressions.

## 14 September 2026 - Remove-supported-person capability; Phase 20C Option 2 drawer change implemented; multi-space Remove list and collapsible Privacy & data sections (`prephase22.txt`)

**A genuine, previously-missing organiser capability was built end-to-end**, prompted by a direct product-owner question ("as organiser, how do I remove supported persons if they no longer need support?") that investigation confirmed had no answer anywhere in the codebase - `delete_my_account()` only ever detaches memberships, and `leave_care_space()`/`remove_member()` only ever end one membership. New migration `20260914120000_delete_care_space.sql`: `delete_care_space()` (security definer, any active organiser of the target care space, idempotent), performing a full cascade delete in strict dependency order across ~15 tables (derived by direct inspection of `information_schema.referential_constraints`, since almost every FK here is `ON DELETE RESTRICT`, not cascade). Running the pgTAP suite while building it found two real, previously-latent schema bugs: `care_space_activity_is_immutable()` blocked the cascade's own DELETE (fixed with a narrow, transaction-scoped exception, mirroring Phase 21B's own established pattern for `entitlement_events`), and `care_space_domain_grants.granted_by_membership_id` is `ON DELETE NO ACTION`, not cascade (fixed by deleting via both FK columns explicitly). Client: `deleteCareSpace()` in `src/careSpaces.ts` (removes cloud Storage attachments best-effort, queuing failures through a new sibling of Phase 18's existing durable cleanup queue); a checkbox-gated confirmation modal, `src/components/RemoveCareSpaceConfirm.tsx`, added to Privacy & data. Applied to `lilica-development`; 358 pgTAP assertions pass on both local and linked.

Two direct follow-up bug reports from live testing were then fixed. First: "I have two supported people but it only offers to remove one." The "Remove a supported person" section only ever showed the currently active care space - fixed by adding a `role` column to `list_my_supported_people()` (new migration `20260914130000_list_my_supported_people_role.sql`, applied to `lilica-development`) threaded through `reconnectCareSpaces()`/`careSpaceState.ts`/`LocalCareSpaceState`, so `PrivacyDataScreen` now lists every care space this account organises as its own "Remove [Name]" row. Second: "shouldn't all these menu items under Privacy and data be collapsed so it's neater" - `PrivacyDataScreen`'s six sections are now a collapsed-by-default accordion (tap a header to expand), purely presentational.

**Phase 20C's own approved Option 2 drawer change (see `docs/PHASE_20C_NAVIGATION_RECONCILIATION.md`, section 26 addendum) was also implemented**: a new `"[Name]'s care"` group (Care Summary, Recent Activity) above the existing Account group in the Settings drawer. A third live bug report during testing - "opening Maggie's Recent Activity/Care Summary, clicking back goes straight to People instead of the menu" - was fixed once, incorrectly (a `careOverlayOpenedFromSettings` flag making the existing full-screen overlay's Back conditionally reopen the drawer), then fully reverted after a follow-up report ("care summary and recent activity menu items appear to be opening whole new pages instead of remaining in the menu") showed the real defect was architectural: these two screens now render **in-drawer** via `settingsSection`, exactly like Account/Care Circle/Privacy & data already do - no new state at all. People's own separate links to these two screens are unaffected and still use the original full-screen overlay path.

Also created, per the `prephase22.txt` brief's explicit "audit/proposal only, no implementation" instruction: `docs/PHASE_20D_CARE_SPACE_LIFECYCLE_PROPOSAL.md` (archive/leave/transfer/delete lifecycle analysis - explicitly notes the tension with the remove-supported-person feature above, which the brief's Part B asked not to be implemented but was already shipped from an earlier, separate, explicit instruction in this same session), `docs/PHASE_20E_DOCUMENTS_PROPOSAL.md` (a Documents collection screen proposal, modelled on the existing `ContactsListScreen.tsx`), and `docs/PRE_PHASE_22_PRODUCT_GAP_REVIEW.md` (revisits Phase 20A's ten deferred Jointly-inspired gaps).

`npm run typecheck` clean; `npm test` 71 suites/662 tests, zero regressions. Not committed or pushed, per the brief's own explicit instruction. Phase 22 was not resumed.

## 14 September 2026 - Phase 20C: Jointly navigation/discoverability reconciliation - audit/proposal only

Phase 22A's own visual audit was paused mid-task to run this reconciliation first, per explicit product-owner direction. `docs/PHASE_20C_NAVIGATION_RECONCILIATION.md` (new) reconciles the current Lilica navigation map against Phase 20's own Jointly evidence, distinct from Phase 20's capability-parity work (preserved unchanged). Finding: most of Jointly's apparent menu breadth already exists in Lilica, reached through tabs/People rather than a drawer row; the genuine problem is that Care Summary and Recent Activity (both already built and approved in Phase 20B) have exactly one, easily-missed route each, and no Documents collection destination exists at all. Recommends (not yet approved) adding a small, clearly person-scoped `"[Name]'s care"` section (Care Summary, Recent Activity) above the existing Settings drawer's account rows - a hybrid Settings/navigation-hub model - while explicitly not duplicating the permanent bottom tabs, Add, or Key Contacts, and not building a new Documents screen without a separate product decision. **Audit/proposal only - no navigation, screen, or code change was made.** Not committed or pushed, per the brief's own explicit instruction. Phase 22A's own drawer-visual findings should wait for this decision before being finalised.

## 14 September 2026 - EAS build standing requirements recorded; app.json corrected for iOS phone-only

Ahead of any real EAS build (none has ever been run for Lilica - no `eas.json`/EAS project exists), the product owner set three standing requirements, recorded in `docs/PHASE_19_ARCHITECTURE.md`'s store/release-readiness section and in `AGENTS.md`/`CLAUDE.md`: no `eas build` is ever run without explicit, express consent for that specific build; iOS must stay phone-only (`app.json`'s `ios.supportsTablet` corrected from `true` to `false`, so App Store Connect/TestFlight never requires iPad screenshots); OTA (`expo-updates`) must be configured from the very first build, not added later. Only the `app.json` correction was actionable today - `eas init`/`runtimeVersion`/`updates.url` configuration all require a real EAS project and are deferred to whenever the first build is actually authorised.

## 14 September 2026 - Repository-wide em-dash/en-dash removal

Standing product-owner rule: no em-dashes (-) or en-dashes (-) anywhere in this repository - UI copy, code comments, docs, commit messages. A full sweep replaced every instance with a plain hyphen (a single space either side where it separated a clause, no added space where it joined two values directly, e.g. `9pm-8am`) across every doc, `AGENTS.md`/`CLAUDE.md`/`README.md`, `App.tsx`, all of `src/`, `tests/`, and `supabase/config.toml`. Three real user-visible/test-asserted strings needed a source fix, not just a text edit: `RecordEditor`'s "Yes/No" prompt labels and `App.tsx`'s quiet-hours label, plus their matching tests. Verified zero em/en-dash characters remain anywhere; full test suite unaffected.

## 14 September 2026 - Home/Search interaction refinement (`search4.txt`) implemented

A bounded UI/interaction refinement matching an approved mock (`\downloads\search4.png`), not a redesign - no Home/Search domain logic changed. `src/screens/HomeScreen.tsx`: the avatar grew from 34px to 56px with a larger initial, the supported person's first name now shows directly beneath it, and the search affordance moved out of the header (previously a small icon button) into its own real search-bar-styled element next to the avatar - a magnifying-glass glyph immediately followed by "Everything for [Name], in one place." (no gap between them, corrected mid-task from an initial small gap per product-owner review), still opening the same existing dedicated Search screen (Phase 20B) rather than expanding inline. An earlier hand-drawn ring+handle icon was replaced with a real magnifying-glass Unicode glyph after it read as a stray mark at this size. The horizontal dashboard strip (Overdue/Due today/Coming up/etc.) gained left/right scroll chevrons with real measured enable/disable state tied to actual scroll position, discoverability only - swipe/drag scrolling is unchanged and still works. `src/screens/SearchScreen.tsx` needed no code change at all - inspection confirmed it already met every requirement (dynamic "Search [FirstName]" title, `autoFocus`, dynamic helper copy, plain-callback back navigation with no stage/onboarding routing); only tests were added (`tests/search4-home-refinement.test.tsx`, new; `tests/phase20b-search-screen.test.tsx`, two new assertions; `tests/home-person-switcher.test.tsx`, one assertion corrected for the person's name now legitimately appearing twice once the switcher is open). `npm run typecheck` clean; `npm test` 67 suites/635 tests (up from 66/621, zero regressions).

## 14 September 2026 - Phase 21C: Expired Trial / Read-Only UX + Phase 21 Closure implemented

Implemented from the approved `phase21c.txt` brief. Phase 21B's own completion report identified a genuine UX gap: the server correctly rejects a gated mutation once entitlement expires, but the client still let a user begin a mutation flow and discover only at Save time that it couldn't be saved. This phase closes that gap with a proactive, centralised client-side layer only - **no schema change** (`supabase db diff --linked` confirmed no changes before and after), the approved Phase 21B server architecture, mutation inventory, and commercial model (60 days free, then £8.99/year) are all unchanged. Full detail: `docs/PHASE_21_ARCHITECTURE.md` section 23; validation/QA: `docs/PHASE_21_QA.md`'s matching section.

A single `isReadOnly`/`guardMutation` mechanism in `App.tsx`, driven by Phase 21B's own existing `getCareSpaceCommercialStatus()` RPC and its existing 72-hour offline cache, gates exactly five entry points: Home/To Do `+Add`, Person's per-section Add links (and Key Contacts' own Add link), the shared existing-record Edit trigger (`RecordQuickEditor.tsx`/`FirstThingScreen.tsx` - the Edit button stays visible, never silently hidden), and Care Circle's "Invite someone". A new `src/components/ReadOnlyGate.tsx` shows the approved calm copy with a genuine owner/collaborator distinction: a collaborator never sees the owner's raw entitlement status, is never told their own subscription would help, and sees no Subscribe action at all - only "this care space is currently read-only." `remove_member()`/`leave_care_space()`/`accept_invitation()`/`decline_invitation()`/Account/Privacy/Export remain deliberately ungated, matching their unchanged server-side status. A new `hasEntitlementHeldMutations()` read in `src/recordSync.ts` (reusing Phase 21B's own `isEntitlementHeldMutation()` predicate, no new state) drives one small, dismissible, non-modal notice for the rare remaining race where the server still rejects a mutation after the proactive gate has already passed.

`npm run typecheck` clean; `npm test` 66 suites/621 tests (up from 61/602, zero regressions); 16 new tests across 5 new files. Not committed or pushed, per the brief's own explicit instruction. **Phase 21 is now engineering-complete; real store purchase QA remains explicitly outstanding** - no RevenueCat project, store products, development build, or sandbox testing exist yet.

## 14 September 2026 - Phase 21B: Billing, Subscription & Entitlement implemented

Implemented from the approved `Downloads\phase21b.txt` brief, building on the reviewed `docs/PHASE_21_ARCHITECTURE_PROPOSAL.md`. Final approved commercial model: **60 days completely free, then £8.99/year**, one subscription per Lilica account covering every care space that account commercially owns. Full detail: `docs/PHASE_21_ARCHITECTURE.md`; validation/QA: `docs/PHASE_21_QA.md`.

New migration `20260914090000_phase21b_billing_entitlement.sql`: `care_spaces.commercial_owner_id` (deliberately distinct from `bootstrap_owner_id` and from Care Circle `role`); `entitlements`/`entitlement_events` (RLS-restricted, no client write access); `care_space_has_active_entitlement()`/`user_has_active_entitlement()`/`get_care_space_commercial_status()`; `transfer_care_space_commercial_ownership()`; `apply_entitlement_update()` (webhook-only write path). Ten existing RPCs redefined (same signatures) to add entitlement enforcement at exactly the point each already had its permission check - `remove_member()`/`leave_care_space()` deliberately left ungated as safety/exit actions, resolving a genuine tension in the brief's own instructions (documented explicitly in the architecture doc). A real bug was caught by re-running the existing Phase 18B pgTAP suite against the new schema before writing any new tests: the first version of `entitlement_events`'s immutability trigger blocked the `on delete set null` FK cascade needed for account deletion itself - fixed and reverified.

Client: `src/entitlement.ts` (server-state display/offline-grace cache), `src/billing.ts` (RevenueCat `react-native-purchases@10.9.1`, version-compatibility verified against its own published peer dependencies before installing), a new Subscription section inside the existing Settings drawer (`SubscriptionScreen.tsx`), and a held-mutation recovery mechanism reusing `src/recordSync.ts`'s own existing outbox `'rejected'` status rather than a new parallel store - an entitlement-caused rejection is retried automatically once entitlement is restored, while a genuine permission rejection stays permanently blocked as before. The account-deletion confirmation now discloses that deleting a Lilica account does not cancel an App Store/Google Play subscription (brief section 16). A new Edge Function, `supabase/functions/entitlement-webhook`, receives RevenueCat's own webhook (field names/event types/signature mechanism sourced from RevenueCat's current documentation) - its pure mapping/signature logic is unit-tested; the function itself is **not yet deployed** (no RevenueCat project exists yet).

New migration applied to `lilica-development` after a clean dry-run (341 pgTAP assertions/12 files, up from 305/11, both local and linked; migration histories verified matching). `npm run typecheck` clean; `npm test` 61 suites/602 tests (up from 56/541, zero regressions). Not committed or pushed, per the brief's own explicit instruction.

**Genuinely outstanding, honestly marked rather than fabricated**: proactive client-side read-only UX (disabling mutation affordances ahead of an attempt, rather than only reacting to a server rejection); the webhook's real end-to-end delivery; any physical device/store-sandbox QA at all (requires a development build, a real RevenueCat project, and sandbox store accounts, none of which exist yet). The server-side entitlement gate is fully authoritative regardless of any of these.

## 13 September 2026 - Phase 21A pricing correction: free period extended to two months

Product-owner direction: the free trial period is now two months, not one - a deliberate strategic choice to build more dependency on the product before the purchase decision. Applied throughout `docs/PHASE_21_ARCHITECTURE_PROPOSAL.md` (trial-start semantics now a fixed 60-day duration rather than 30, with the same calendar-month-ambiguity reasoning preserved) and every other living document recording the pricing direction (`docs/CORE_SYSTEM_CONTRACT.md`, `docs/PROJECT_BRIEF.md`, `docs/PHASE_18_ARCHITECTURE.md`, this file's own Phase 21 roadmap entry). The £8.99/year price itself (corrected the same day from £9.99) is unchanged by this update. Documentation only - no code, schema, or dependency touched.

## 13 September 2026 - Phase 21A: billing/subscription architecture review underway (analysis only, not implemented)

Implemented from `Downloads\phase21a.txt`'s absolute no-code gate. Produced `docs/PHASE_21_ARCHITECTURE_PROPOSAL.md` - a review-only architecture/product-rules/implementation-plan document for Phase 21 (Billing, Subscription & Membership/Entitlement), covering: a recommended commercial-ownership model (a new `care_spaces.commercial_owner_id`, deliberately distinct from `bootstrap_owner_id` and from `care_space_memberships.role`); resolution of all nine named complex-collaboration scenarios; a trial-start recommendation (first `bootstrap_supported_people()` call) and precise one-month semantics (30x24 hours, not calendar-month); an app-managed-trial-vs-store-managed-trial comparison (recommends app-managed); a direct-integration-vs-subscription-management-service comparison sourced from RevenueCat's own current 2026 pricing (recommends RevenueCat); a full entitlement state machine, decision table, mutation inventory, and 25-scenario matrix; server-enforcement, offline, security-threat-model, and privacy designs; Apple/Google compliance findings sourced from their own current documentation (including two newly-surfaced launch dependencies: an April 2026 Apple pre-purchase disclosure requirement, and a 2026 Google web-based account-deletion-resource requirement); and 8 explicit open decisions flagged for GPT/product-owner approval before any Phase 21B implementation prompt is issued.

**Zero billing code, dependency, migration, or schema was created** - this is a proposal only, per the brief's own explicit prohibition. `docs/PHASE_21_ARCHITECTURE.md` (the final, approved version) is deliberately not created yet. Not committed or pushed, per this brief's own explicit instruction.

## 13 September 2026 - Phase 20 closure: physically tested and approved

Phase 20B (Recent Activity, Search, Care Summary) was physically tested by the product owner and approved ("tested and it works"). Phase 20 - both the 20A audit and the 20B implementation - is now complete. `docs/PHASE_20_QA.md`'s checklist is marked complete on the basis of that general confirmation, not a separately reported item-by-item walkthrough. Already committed and pushed (`1bcbf2a`); this entry and the QA-doc update itself are a documentation-only closure note, not a reopening of the implementation.

## 13 September 2026 - Phase 20B: approved carer-focused enhancements (Recent Activity, Search, Care Summary)

Implemented from the approved brief `phase20.txt`. Phase 20A's audit (`docs/PHASE_20_GAP_AUDIT.md`, preserved unchanged) identified many candidates; only three were approved for this phase - every other candidate remains explicitly not implemented (see that document's own backlog). Full detail: `docs/PHASE_20_ARCHITECTURE.md`; physical-device checklist: `docs/PHASE_20_QA.md`.

**Recent Activity / Care History**: no true immutable activity/event store existed before this migration (confirmed by inspection, not assumed) - new append-only `care_space_activity` table plus `list_recent_activity()`, both reusing the exact same domain-grant RLS decision every record read already uses. `apply_record_mutation()` (redefined a second time, carefully based on the CURRENT Phase-15 body rather than the original Phase 7 body, to avoid silently regressing Phase 15's domain-write-access fix), `mark_attachment_upload_status()`, `accept_invitation()`, `remove_member()` and `leave_care_space()` each now log a truthful, non-fabricated activity event at the exact point they already know a genuine state transition occurred - never backfilled, never inferred beyond what the stored data itself proves changed. A "Recent activity" link on People's Care Circle card opens a new bounded, paginated, day-grouped screen; never a Home preview.

**Unified Current-Person Search**: purely client-side (`src/search.ts`), no schema, no new RPC, no AI/embeddings/external search service - searches the exact already-authorised record array already loaded for the active care space, scoped to the current supported person only. A new header search icon on Home opens a new `SearchScreen`; results open the same shared record-detail path every other list already uses.

**Care / Handover Summary**: named "Care summary" (not "Emergency Medical Record"/"Emergency Mode", both explicitly forbidden - the naming choice is documented in the architecture doc). A pure client-side projection (`src/careSummary.ts`) over records/Care Circle members/recent activity already loaded elsewhere - no new schema, no new profile fields, no duplicate data store. Distinct from Home (context/continuity/people, not an actionable worklist) and from the existing data export (unchanged; the summary's own logic is deliberately reusable by a future, separately-approved human-readable export). A "Care summary" link sits beneath People's own "Person being supported" card.

Multi-person isolation (server-side, via the same RLS every other table uses, and client-side, via an effect that force-closes all three screens the moment the active care space id genuinely changes) and former/revoked-member behaviour (historical attribution survives; access does not) are both proven directly by new pgTAP and Jest coverage, not merely asserted.

New migration `supabase/migrations/20260913120000_phase20b_activity_search_summary.sql` (26 new pgTAP assertions, 305 total across 11 files) - applied to `lilica-development` after a clean dry-run; migration histories confirmed matching; linked pgTAP (305/11) and lint both clean afterward. `npm run typecheck` clean; `npm test` 56 suites/541 tests (up from 48/482, zero regressions in the full pre-existing suite). New client modules `src/activity.ts`/`src/search.ts`/`src/careSummary.ts`, new screens `src/screens/RecentActivityScreen.tsx`/`src/screens/SearchScreen.tsx`/`src/screens/CareSummaryScreen.tsx`, new `src/components/SearchButton.tsx`; `App.tsx`/`src/screens/HomeScreen.tsx`/`src/screens/PersonScreen.tsx` wiring only (no protected Home/To Do/Calendar projection logic touched). Not committed or pushed, per the brief's own explicit instruction to wait for review and physical QA first.

## 13 September 2026 - Phase 20A: Competitive Gap Review audit (analysis only, not implemented)

Phase 19 approved. Phase 20 audit underway. Implemented from `Downloads\phase20.txt`'s absolute no-code gate - audit and report only, zero application code/database/dependency/UI change. Full analysis in the new `docs/PHASE_20_GAP_AUDIT.md`: a current-Lilica capability inventory built from direct inspection of the real implementation; a Jointly/Carers UK capability inventory sourced from the official Apple App Store listing and Carers UK's own page (SEO comparison articles deliberately not used), with evidence quality distinguished (VERIFIED CURRENT / OBSERVED IN PRODUCT-OWNER SCREENSHOT / UNCERTAIN); a full comparison matrix; carer-needs analysis for every significant Jointly capability; a gap classification (A-F) for every candidate; one-year-user, family-handover, emergency, and six-person-collaboration scenario tests; a data/security and legal/privacy impact table; P0-P3 prioritisation (no P0 found); and MINIMUM/RECOMMENDED/EXPANDED implementation sets with relative effort estimates. No candidate enhancement is approved by this document - implementation requires a separate, explicit GPT-and-product-owner-approved prompt. Not committed or pushed, per this brief's own explicit instruction.

## 13 September 2026 - Phase 19: Production Hardening And Release Readiness (audit/hardening pass)

Implemented from the approved brief `Downloads\phase19.txt`. This is an audit/hardening phase, not feature development - its own scope boundary excludes Ask Lilica, OCR, messaging, new record types/Care-Circle concepts/People/document features, a new reminders architecture, subscription/billing, StoreKit, trial logic, AI, search, whole-care-space deletion and unrelated visual redesign; none of that was touched. Full detail: `docs/PHASE_19_ARCHITECTURE.md`; the complete release-readiness matrix: `docs/PHASE_19_QA.md`.

Baseline reconfirmed clean before any work: TypeScript, Jest (48 suites/482 tests, zero warnings), secrets scan (226 files), local and linked (`lilica-development`) pgTAP/lint (279 assertions/10 files both), migration history parity, `expo config`/`expo export --platform web`.

**A real, committed (non-rolled-back) two-account collaboration and account-deletion rehearsal** was run directly against `lilica-development` via `npx supabase db query --linked`, with explicit product-owner approval (flagged beforehand as genuinely irreversible for the throwaway data, unlike pgTAP's own transactional proof). Proved for real: a contributor with only `general`-domain access is rejected by the server (`42501`) when assigning a financial record - the record was invisible to her before the domain check even ran; `delete_my_account()` succeeds for real, detaches/snapshots the membership correctly, survives a same-user retry as a safe no-op, and is correctly blocked with a real server error (`P0001`) for a sole organiser; the deleted user's records/links/attachments/assignments all survive, still correctly attributed and resolvable via `resolve_membership_identities()` by the remaining collaborator. All throwaway data was fully removed afterward and `lilica-development`'s pgTAP/lint were re-confirmed clean. This closes out Phase 18B's own outstanding two-account QA item at the server/database layer specifically; on-device UI QA remains separately outstanding.

Also completed this phase: an RLS/permission adversarial-coverage audit across all 10 pgTAP files (every RLS-bearing table confirmed `authenticated`-only by grant); a dependency/security review (`npm audit --production` - 11 moderate findings, all traced to build-time-only Expo tooling via a transitive `uuid` dependency, zero runtime exposure; zero service-role-key or secret leakage found in client code via grep); a static accessibility-label audit (131 occurrences across 54 files); a store/release-config review (`app.json` - real bundle IDs and permission strings present, no `versionCode`/`buildNumber`/`eas.json` yet, correctly out of scope since store submission is explicitly forbidden this phase).

**No release-blocking (A) defect was found.** Two B-candidates were identified and deliberately left unfixed as not justified (missing explicit anon-role pgTAP tests where the guarantee is already structural; missing EAS build-profile config, irrelevant until submission is authorised) - see `docs/PHASE_19_QA.md`'s release blocker register. Items requiring physical hardware (offline/restart/recovery, screen-reader pass, physical device matrix, reminder engine on a dev build, large-data performance feel) are honestly marked NOT RUN there, not fabricated. Legal/privacy decision items (E) - no Privacy Policy/Terms URL, and whether former-member display-name retention satisfies erasure expectations - are recorded for product-owner/legal review, not implementation.

`npm run typecheck` clean; `npm test` unchanged at 48 suites/482 tests (no new application code was written - this phase reviewed and cited existing coverage); `git diff --check` clean. Diff audit: documentation only - `docs/PHASE_19_ARCHITECTURE.md` (new), `docs/PHASE_19_QA.md` (new), this file, `AGENTS.md`, `CLAUDE.md`, `docs/LUMEN_HANDOFF.md`. No application code, schema, or migration changed; the one state-changing action was the explicitly-approved, fully-cleaned-up database rehearsal described above. **Production release is not implied by this phase's completion - it remains a separate, explicit product-owner go/no-go**, per the brief's own requirement. Not committed or pushed, per the brief's own explicit instruction to wait for approval.

## 13 September 2026 - Roadmap extended through Phase 22 (documentation only)

Product-owner decision, made while Phase 19 (Production Hardening & Release Readiness) was already being executed separately: the canonical roadmap is extended with a three-phase approved extension beyond Phase 19, per `Downloads\addphases.txt`.

- **Phase 20 - Competitive Gap Review & Carer-Focused Enhancements.** Systematically compare finished Lilica against established carer-focused products, beginning with Jointly from Carers UK, to identify genuine gaps. Must begin with an audit/report classifying every candidate feature (A-F) before any implementation; only the precise enhancement set explicitly approved by GPT and the product owner afterward may be implemented.
- **Phase 21 - Billing, Subscription & Membership/Entitlement.** Implements Lilica's complete commercial entitlement lifecycle. Approved pricing direction: two months fully free, then £8.99/year, as one subscription per user account. Several product decisions (access after lapse, collaborator/membership commercial questions) remain to be finalised before implementation. Commercial "membership" is explicitly distinct from `care_space_memberships` security membership and must never itself grant care-space access.
- **Phase 22 - Premium Visual Optimisation & Experience Polish.** The final planned phase. Must begin with a complete visual/experience audit/report only - no code, theme or component changes - with an explicit CRITICAL/HIGH/MEDIUM/OPTIONAL priority format and a "DO NOT TOUCH" list, reviewed and approved by GPT and the product owner before any implementation prompt is issued.

Canonical wording for all three phases (Purpose/Dependencies/Deliverables/Acceptance/Regression protection/Explicit exclusions/Stop point, plus the Phase 19-22 sequence) was added to `docs/CORE_SYSTEM_CONTRACT.md`, preserving the original Phase 19 wording/history unchanged. `docs/PROJECT_BRIEF.md`, `docs/LUMEN_HANDOFF.md`, `AGENTS.md`, `CLAUDE.md` and `README.md` were each given a concise, dated pointer to the extension so no incoming agent could mistakenly conclude Phase 19 is the final remaining work. No `PHASE_20/21/22_ARCHITECTURE.md`/`_QA.md` placeholder files were created - those are created only once each phase actually begins and its scope is approved.

**Confirmation: no code, schema, backend, migration, dependency or app-config change occurred in making this roadmap update.** Documentation only - see the matching completion report for the full file list, stale-reference audit and `git diff --check` result. Phase 19's own in-progress work (implementation and validation, separately executed) was not touched, altered, or interrupted by this pass.

## 13 September 2026 - Settings drawer visual/safe-area fix, profile pictures, Care Circle avatar details

Three changes, committed together, none of them Phase 18B or database/backend work:

**Settings drawer bug fix.** Physical QA found the drawer's header colliding with the status bar/notch on open. Root cause: `SettingsMenu`'s drawer renders inside a `Modal`, which presents its content on a separate native surface from the app's main window -- `react-native-safe-area-context`'s insets, correct everywhere else in the app, were not reliably reaching a `SafeAreaView` measuring inside that separate surface. Fixed by reading the inset with a plain `useContext(SafeAreaInsetsContext)` in `SettingsMenu`'s own place in the React tree (a normal descendant of `App.tsx`'s `SafeAreaProvider`, outside the `Modal`) and applying it as ordinary `padding` inside the Modal -- no measurement ever happens inside the portal itself. Deliberately not the throwing `useSafeAreaInsets()` hook, since that crashes when a test renders the component without a wrapping provider; `useContext` degrades to a safe zero-inset default instead.

**Settings drawer visual-quality pass.** The drawer's previous plain grey-bordered rows and undersized muted title read as a generic system sheet, not part of Lilica's own visual language. Now: a bold, primary-accented "Settings" title; each row is a soft-shadow "elevated card" (the same language Home/To Do's own record cards already use) with a small coloured accent mark and a primary-toned chevron; the header's Close is a pill button; a quiet Lilica wordmark fills the space beneath a short menu list instead of leaving it looking unfinished. No navigation/architecture change -- still the same physically-approved right-hand drawer with internal `section` state.

**Profile pictures.** An organiser can now add a real photo to their own account (Account screen: tap the avatar, pick from the photo library, square-cropped). New private `profile-avatars` Storage bucket, one fixed path per user (`{userId}/avatar.jpg`, upsert overwrites so a changed photo never orphans the old file), RLS scoped to the owner only. `src/profileAvatar.ts` wraps pick/upload/signed-URL-resolve; `AccountScreen` shows the resolved photo with a real "Change/Add photo" action and a real error message on failure, never a fake success. Also fixed a real, latent pre-existing bug found while touching this code: `AuthProvider.saveProfile()` used to drop `avatarPath` from local state on every display-name save (it never touched the database, only the in-memory profile object, but a saved photo would appear to vanish locally until the next full reload). The same photo now also appears for the signed-in user's own "You" tile in the People tab's Care Circle preview and its detail popup, closing a gap reported directly after the feature shipped -- `CareCircleMember` (from `list_care_space_members()`) has no `avatarPath` field of its own, so nothing had ever resolved it there. Showing OTHER members' photos to each other remains a separate, real product decision, not made here.

**Care Circle avatar details.** Tapping any Care Circle avatar in the People tab now softly pops out (new `src/components/MemberDetailPopup.tsx`, a small fade/scale-in card, backdrop-dismiss) that member's real name, role, relationship and what they can see -- reusing exactly the same `CareCircleMember` data the People preview and Settings' own Care Circle management screen already read. No fabricated detail, no new data source.

New migration `20260912180000_profile_avatars.sql` (5 pgTAP assertions, 279 total across 10 files) -- applied to `lilica-development` (see `docs/SUPABASE_OPERATIONS.md`'s "Hosted profile-avatars deployment" entry). New tests: `profile_avatars` pgTAP suite, `tests/profile-avatar.test.ts` (9), `tests/account-screen-avatar.test.tsx` (5), plus one new test in `tests/phase13-person.test.tsx` for the You-avatar fix.

`npm run typecheck` clean; `npm test` 48 suites/482 tests, zero regressions; secrets scan clean (226 files); local `db:reset`/`db:test`/`db:lint` clean (279 pgTAP assertions). Committed as `d79b62d`, pushed.

## 13 September 2026 - To Do background: post-Phase-18B visual QA correction (physically approved)

Physical visual QA of the Phase 18B build found a real layout defect on the To Do screen, unrelated to Phase 18B's own account-deletion/export work: the olive gradient background ended well above the bottom tab bar on a short/empty To Do list ("Nothing needs doing right now"), exposing a large plain cream rectangle beneath it. A first attempt at a fix (widening `ScreenBackdrop`'s gradient distribution and adding `flexGrow`) was reported as still cutting off, traced to an absolutely-positioned gradient child whose resolved height inside a separately-flexed parent did not reliably track that parent's flex-resolved height in practice.

**Corrected properly**: in a new opt-in `stretch` mode, `src/components/ScreenBackdrop.tsx`'s `LinearGradient` is now the flex container itself (`flex: 1`), with no absolute positioning involved -- there is exactly one node whose layout height decides where the colour interpolation ends, and it is the same node the ScrollView's own `flexGrow: 1` content container (added to `ToDoScreen`'s own `content` style) stretches to fill. This guarantees the gradient spans the full, actually-rendered screen height on any device or content length, continuing smoothly all the way down rather than reaching a flat final colour partway down. `ToDoScreen.tsx` also gained a wider, later-starting `stops` distribution (deep olive holding through ~38% of the height, easing through the existing `colors.olive`/`tabAccent.todo.tint`/`colors.oliveSoft` tones already approved for this screen -- no new colour introduced) via the same opt-in mechanism. Home/Calendar/People pass neither `stretch` nor `stops` and are structurally untouched -- proven directly in `tests/todo-background-fix.test.tsx` (4 tests), which also asserts a stretched instance's gradient style carries no fixed `height` at all.

This was a visual/layout correction only. No To Do functional or domain behaviour changed -- assignment/completion/filter/date logic, Add, and the Phase 18B "Assignee no longer available" fallback (`ToDoScreen.tsx`'s own `assignmentLabel()`) are all untouched and confirmed still present.

**Physically inspected and approved by the product owner** on a real device: the existing olive identity is retained, the background reads as one continuous surface through the entire available content viewport (empty, populated and scrolling states all checked), no accidental cream rectangle remains, and the bottom tab bar is unaffected.

Diff: A (Phase 18B, unrelated to this entry) -- `src/accountLifecycle.ts`, `src/components/RecordDetail.tsx`, `src/screens/PrivacyDataScreen.tsx`, plus this same `ToDoScreen.tsx`'s own separate "Assignee no longer available" hunk. C (this correction) -- `src/components/ScreenBackdrop.tsx`, `src/screens/ToDoScreen.tsx`'s gradient/layout hunks, `tests/todo-background-fix.test.tsx` (new). No database migration; no schema, RLS or backend change.

## 12 September 2026 - Phase 18B: Account Deletion & Complete Data Export

Implemented from the approved Phase 18B brief (`Downloads\18b.txt`), whose own Hard Start Gate required the physically-QA-passed Settings drawer correction committed and pushed first (`74b1a61`, confirmed `HEAD == origin/master`, clean working tree). PRE_PHASE_18B baseline: TypeScript clean, 44 suites/436 tests, secrets clean (211 files), 245 pgTAP assertions/8 files, clean lint.

**A second architectural blocker Phase 18A's own investigation had missed** was found the way it should be -- a failing pgTAP assertion, not a later surprise: `care_spaces.bootstrap_owner_id` is a SECOND, separate `on delete restrict` reference to `auth.users(id)`, distinct from `care_space_memberships.user_id`. Its own existing comment ("Idempotency metadata only; access is granted exclusively through membership") confirmed it was safe to detach the same way.

New migration `supabase/migrations/20260912170000_phase18b_account_deletion.sql`:
- `care_space_memberships.user_id` and `care_spaces.bootstrap_owner_id` both become nullable; new column `care_space_memberships.former_display_name` (a minimal snapshot, never an email); `membership_status` gains `'former'` alongside `'active'`/`'revoked'` -- and because every existing access decision already requires `'active'`, a former membership is denied access everywhere with zero new RLS logic. `protect_membership_identity()` extended to allow exactly one new transition (a live `user_id` detaching to null), never a reassignment, never a re-attachment.
- `delete_my_account()`: `security definer`, derives its target exclusively from `auth.uid()`, idempotent (a retry against an already-deleted identity is a safe no-op), re-runs the sole-organiser check server-side (never trusts the client precheck alone), snapshots+detaches every membership the caller holds, detaches `bootstrap_owner_id` the same way, revokes the caller's own still-pending sent invitations only, then deletes the `auth.users` row (whose cascade already removes the live `profiles` row -- Phase 5, unchanged).
- `resolve_membership_identities()`: a new, small, read-only function resolving specific membership ids (an assignee, a document's creator) to a truthful display name -- live or a former-member snapshot -- scoped to care spaces the caller is themselves an active member of.
- `account_deletion_precheck()` and `export_my_data()` are completely unchanged and reused exactly as Phase 18A shipped them.

Client (`src/accountLifecycle.ts`): new `deleteMyAccount()` (calls the RPC, surfaces its real blocker message verbatim rather than a generic one); `exportMyData()` rewritten to include actual authorised document files -- for each attachment the export already includes, downloads its current cloud copy (never a stale local one) via a short-lived signed URL, organised into `documents/<care space>/<safe filename>`, augmenting `data.json` with `exportPath`/`fileAvailable` per attachment (never a storage path/signed URL/token). Per direct product-owner decision (no new zip dependency approved), the result is `data.json` plus each document offered individually through the native share sheet, not one combined `.zip` -- a deliberate, named scope reduction from the brief's own preferred shape. `PrivacyDataScreen.tsx`'s "Delete account" replaces Phase 18A's honest "not available yet" terminal state with the real flow (precheck -> blocker or a genuine destructive confirmation -> `deleteMyAccount()` -> `onAccountDeleted()`, which mirrors "Clear data from this device"'s own local-cleanup-then-sign-out sequence -- server deletion always first). Its Export section now lists every produced file with its own Share action and an honest skipped-document count.

Also fixed a real, pre-existing gap named in its own code comment: `RecordDetail.tsx`/`ToDoScreen.tsx`'s `assignmentLabel()` used to silently return `undefined` (rendering nothing) for an assignee no longer in the active member list -- it now shows "Assignee no longer available" instead. The fuller "David M -- Former member" treatment (via the new `resolve_membership_identities()`) is implemented and tested server-side but not yet wired into this or any other display surface -- a named scope reduction given this phase's size.

New tests: `supabase/tests/database/phase18b_account_deletion.test.sql` (29 assertions, 274 total/9 files) -- unauthenticated rejection, sole-organiser block, an account with no care spaces deleting freely, a second organiser unblocking deletion, the auth/profile rows actually gone, the membership surviving detached/former/snapshotted, selective invitation revocation, shared records/links/attachments/assignments all surviving still attributed to the former membership, `resolve_membership_identities()` correctness, idempotent retry, and the trigger refusing both reattachment and reassignment. `tests/phase18-account-lifecycle.test.ts` extended (+11), `tests/phase18-privacy-data-screen.test.tsx` extended (+7), `tests/record-detail.test.tsx`/`tests/todo-card-hierarchy.test.tsx` each gained one test for the new assignee fallback.

Validation: `npm run typecheck` clean; `npm test` 44 suites/450 tests (up from 44/436, zero regressions); `npm run secrets:check` clean (213 files); `npx expo config --type public`/`npx expo export --platform web` clean; local AND linked `db:test`/`db:lint` both clean (274 pgTAP assertions, up from 245) after applying the new migration to `lilica-development` (dry-run confirmed exactly one pending migration beforehand; migration histories verified matching after).

Explicit, named limitations (not silently absent): no `.zip` archive (per product-owner decision); "David M -- Former member" attribution is a tested server capability not yet wired into every display surface; email change, in-app password change, an About screen, and care-space deletion remain out of scope, unchanged from Phase 18A. Diff audit: A (implementation) -- `App.tsx`, `src/accountLifecycle.ts`, `src/screens/PrivacyDataScreen.tsx`, `src/components/RecordDetail.tsx`, `src/screens/ToDoScreen.tsx`; B (database) -- one new migration; C (tests) -- one new pgTAP file, four updated client test files; D (docs) -- this file, `docs/PHASE_18_ARCHITECTURE.md`, `docs/PHASE_18_QA.md`, `docs/SUPABASE_OPERATIONS.md`, `AGENTS.md`, `CLAUDE.md`, `docs/LUMEN_HANDOFF.md`. E (unexpected) -- empty. The Settings drawer itself (`src/components/SettingsMenu.tsx`) was not touched. Billing/paywall, whole-care-space deletion and Phase 19+ were not started. Not committed or pushed, per the brief's own explicit instruction to stop and wait for GPT/product-owner review, physical QA and two-account QA first.

## 12 September 2026 - Settings navigation correction (post-Phase-18 physical QA)

Physical testing of Phase 18's Settings surface (`Downloads\settings.txt`) rejected the navigation model, twice in the same day: first a full-width bottom sheet (opened "far down the page, disconnected from the cog"), then an anchored dropdown card fixed in response to that -- both handed Account/Care Circle/Privacy & data off to separate top-level screens that replaced whichever tab was showing, so each screen's own Back button dropped the user on the dashboard instead of back into Settings. Neither ever felt like one self-contained menu.

**Settings is now a right-hand application drawer with its own internal navigation hierarchy. Settings subsections render within the drawer and do not become ordinary application screens.** Full mechanism: `docs/PHASE_18_ARCHITECTURE.md`'s "Settings navigation correction" addendum.

- `src/components/SettingsMenu.tsx`: rewritten as a genuine sliding drawer -- `Animated.timing`-driven slide-in from the right at ~88% of screen width (capped for tablets), dimmed backdrop, safe-area-respecting header with a persistent Close. Gained a required `section: 'menu' | 'account' | 'careCircle' | 'privacyData'` prop and a `children` slot: the menu list renders when `section === 'menu'`; otherwise the drawer's body is whatever screen `children` holds, and a row press no longer calls `onClose` -- it only tells the parent which section to show.
- `App.tsx`: replaced the `showAccount`/`showPrivacyData` booleans (which drove separate top-level `content` branches) with a single `settingsSection` state, owned by the same component that already owns all three screens' real data/callbacks. `AccountScreen`/`CareCircleScreen`/`PrivacyDataScreen` are now rendered as `SettingsMenu`'s `children`, unchanged except each one's `onBack` now points at `setSettingsSection('menu')` instead of exiting. Closing the drawer resets `settingsSection` to `'menu'` so the next open starts at the root. People's own direct "Manage care circle" link (`showCareCircle`, bypassing the drawer entirely) is unchanged -- it was never part of this complaint.
- New `tests/settings-navigation.test.tsx` (9 tests): cog opens the drawer at its root; Account/Care Circle/Privacy & data each open inside the drawer and their own internal back returns to the root; the underlying route/selected-person text never changes while navigating any of them; closing the drawer after visiting every section (from each of Home/Calendar/To Do/People) restores exactly the same route/person; Phase 18's real handlers are still what gets called, never a stand-in.
- `tests/settings-cog.test.tsx` updated: a menu row press now asserts it does NOT call `onClose` (previously asserted the opposite); added a test that `section !== 'menu'` renders the passed-in `children`, not the row list; split the old ambiguous "Close settings" label into the header's `Close` and the backdrop's `Dismiss settings`, each independently addressable and tested.
- Validation: `npm run typecheck` clean; `npm test` 44 suites/436 tests (up from 43/425 -- the whole existing suite re-run, zero regressions: Home/Calendar/To Do/People, person switching, add, record editing, Care Circle, invitations, permissions, assignment, documents, export, account deletion, auth, onboarding, keyboard handling and bottom-tab navigation all still pass unchanged).
- No database migration -- this was a presentation/navigation-ownership correction only, confirmed against the brief's own explicit instruction to stop and explain rather than create one if any schema change seemed necessary (none did).
- No Phase 18 business logic was rewritten: `PrivacyDataScreen`'s What-Lilica-stores/export/local-data/leave/delete-account-precheck behaviour, `CareCircleScreen`'s membership/invitation/role/permission logic, and `AccountScreen`'s display-name/reminders/sign-out behaviour are byte-for-byte the same components, just re-parented under the drawer with a redirected `onBack`.
- Not committed or pushed, per this brief's own explicit instruction (and the product owner's own separate "hold off on push").

## 12 September 2026 - Phase 18: Privacy, Settings, Export & Account Lifecycle

Implemented from the approved Phase 18 brief (its Hard Start Gate - Phase 17 complete, physical QA confirmed, committed and pushed - was satisfied by product-owner confirmation before this work began; `PRE_PHASE_18_BASELINE`: HEAD `f1baa0c`, in sync with `origin/master`, TypeScript clean, 39 Jest suites/395 tests, 228 pgTAP assertions/7 files).

**The load-bearing finding**: before writing any UI, the schema was inspected for what happens if `auth.users` is deleted. `care_space_memberships.user_id references auth.users(id) on delete restrict`, and membership rows are themselves permanently `on delete restrict`-referenced from `records`, `occurrences`, `assignments`, `record_links`, `record_attachments` and every mutation-receipt table - by deliberate Phase 7/8 design, to preserve historical attribution. Real `auth.users` deletion is therefore impossible today without either an FK violation or destroying shared care-space history, and needs its own, separately-approved schema change. Per the brief's own explicit instruction, this was reported rather than worked around: only a real, read-only precheck was built (`account_deletion_precheck()`), and the UI is honest that deletion itself isn't available yet.

- New migration `supabase/migrations/20260912150000_phase18_privacy_export.sql`: `account_deletion_precheck()` (returns which of the caller's care spaces they sole-organise, i.e. would block deletion) and `export_my_data()` (a single read-only `jsonb` document of the caller's own profile plus every care space they belong to - records/attachments-metadata/record-links, each filtered through the exact same `membership_has_domain_access()` every ordinary read already uses; a link is included only when BOTH its endpoints individually pass that check, mirroring `record_links`' own RLS). Both `security definer`, both `revoke ... from public/anon`, both `grant execute ... to authenticated` only. 17 new pgTAP assertions (245 total across 8 files).
- New `src/documentCleanupQueue.ts`: closes a real Phase 17-acknowledged gap. An AsyncStorage-backed durable retry queue (mirrors `src/recordSync.ts`'s own cache pattern) - `enqueueDocumentCleanup()` is awaited as the literal first step of `App.tsx`'s `removeRecord()`, before any network attempt, so an app kill or offline delete can never forget a record still owes cloud cleanup; `retryPendingDocumentCleanup()` (retried at app startup and every care-space reconciliation pass, same lifecycle points as the existing upload-retry effect) removes the Storage object, tombstones the attachment row and all the record's links, clearing a queue entry only once genuinely successful. Replaces Phase 17's best-effort fire-and-forget calls in `App.tsx`.
- New `src/localData.ts`: `pendingLocalWork()` (checks the record cache's outbox, unuploaded attachments, and the new cleanup queue) and `clearLocalDataForOwner()` (removes only this owner's AsyncStorage keys and local attachment directories, never touches Supabase). "Clear data from this device" always confirms, names any genuinely pending work first, and signs out afterward rather than attempting a risky live rebuild of in-memory state from a cache just wiped out from under it.
- New `src/accountLifecycle.ts`: `checkAccountDeletionEligibility()` (calls the new precheck RPC) and `exportMyData()` (calls the new export RPC, writes the JSON to a local file via `expo-file-system`, shares it via `expo-sharing` - no cloud storage bucket, no signed URL, no server-side artifact to ever clean up). An earlier draft also had a `updateDisplayName()` duplicating `profiles` writes - removed after discovering `AuthProvider.tsx` already exposes a working `saveProfile()`; `AccountScreen`'s new inline name-edit UI reuses that instead, so only one place ever writes `profiles.display_name`.
- New `src/screens/PrivacyDataScreen.tsx`: What Lilica stores, Export your data, Device & local data, Care spaces (current space/role, "Leave [space]" offered only to non-organiser roles - reuses Phase 15's previously-unwired `leaveCareSpace()`), Delete account (precheck, honest current-limitation message, never a fake success).
- `src/components/SettingsMenu.tsx`: new required `onOpenPrivacyData` prop, new always-shown "Privacy & data" entry. `src/screens/AccountScreen.tsx`: new required `onSaveDisplayName` prop, inline name-edit UI. `App.tsx`: new `PrivacyDataScreen` render branch, `showPrivacyData` state, `handlePrivacyCareSpaceLeft()`/`handlePrivacyClearLocalData()`, `removeRecord()`'s tail now goes through the new durable queue, a new retry effect alongside the existing upload-retry one, `AccountScreen`/`SettingsMenu`/`TabBar` usages updated for the new props/reset.
- New tests: `tests/phase18-document-cleanup-queue.test.ts` (8), `tests/phase18-account-lifecycle.test.ts` (5), `tests/phase18-local-data.test.ts` (9), `tests/phase18-privacy-data-screen.test.tsx` (8) = 30 new tests. `tests/settings-cog.test.tsx` (6 call sites) and `tests/auth-flow.test.tsx` (1 call site) updated for the two new required props.
- Validation: `npm run typecheck` clean; `npm test` 43 suites/425 tests (up from 39/395, zero regressions); `npm run secrets:check` clean (206 files); `npx supabase db push --linked --dry-run` then applied to `lilica-development` - migration histories verified matching, 245 pgTAP assertions/8 files clean lint on both local and linked databases; `npx expo config --type public` and `npx expo export --platform web` clean.
- Diff audit: production code - `App.tsx`, `src/components/SettingsMenu.tsx`, `src/screens/AccountScreen.tsx`, `src/accountLifecycle.ts` (new), `src/documentCleanupQueue.ts` (new), `src/localData.ts` (new), `src/screens/PrivacyDataScreen.tsx` (new); database - one new migration; tests - `tests/settings-cog.test.tsx`, `tests/auth-flow.test.tsx`, four new test files; docs - this file, `docs/PHASE_18_ARCHITECTURE.md` (new), `docs/PHASE_18_QA.md` (new), `docs/SUPABASE_OPERATIONS.md`, `AGENTS.md`, `CLAUDE.md`, `docs/LUMEN_HANDOFF.md`. Nothing unexpected/unrelated.
- Explicit, named limitations (not silently absent): real account/auth-identity deletion (the load-bearing finding above); email change and in-app password change (both judged genuinely new auth-flow surface, not small additions); export excludes actual document file bytes and activity/history (no activity-log table exists yet); no About screen added; care-space deletion has no implementation or UI (no lifecycle policy exists in the schema for it).
- Not committed or pushed, per the brief's own explicit instruction to stop and wait for product-owner review/physical QA first.

## 12 September 2026 - Phase 17: Document Experience & Integration

Implemented from the approved Phase 17 brief (`Downloads\phase17.txt`), which explicitly named its starting point as the committed Phase 16 foundation (`deb79e2`). `PRE_PHASE_17_BASELINE`: HEAD `deb79e2` (in sync with `origin/master`), one known uncommitted change (`PHASE_16_COMPLETION_REPORT.txt`, a scratch Notepad file, not app code), TypeScript clean, 36 Jest suites/366 tests, 228 pgTAP assertions/7 files, clean local database rebuild.

**Step 1 (explicitly instructed by this brief, unlike Phase 16): applied the Phase 16 migration to `lilica-development`.** Dry-run confirmed exactly `20260912100000_phase16_document_maturity.sql` pending with no unrelated diff; applied via `supabase db push --linked`; `supabase migration list --linked` confirmed local/remote histories now match; `supabase test db --linked` passed all 228 assertions; `supabase db lint --linked` clean; the private `document-attachments` Storage bucket independently confirmed to exist there (`public: false`, 25 MB limit).

Then the actual UI/integration work - full detail in `docs/PHASE_17_ARCHITECTURE.md`:

- `src/components/RecordEditor.tsx` (document type only): new "Related to" section (`+ Link something` opens the new `src/components/RelatedRecordPicker.tsx` modal, showing only care-space-scoped eligible candidates built by the host from `isLinkableRecordType()`/`linkPickerSummary()`, both new in `src/records.ts`) and "Does anything need doing?" section (calm Yes/No wording, never a generic checkbox; Yes reveals the existing task fields). Picking/creating on a brand-new draft stages locally and flushes via `onLinkRecord`/`onCreateLinkedTask` from inside `save()` once the new record's id is known (the exact "second-record orchestration" the earlier Documents investigation flagged as the one genuinely new piece needed); on an existing document, both act immediately via the same two callback props.
- `src/components/RecordDetail.tsx`: new `relatedRecords`/`onOpenLinkedRecord`/`onViewDocument` props. One stored `record_links` row renders under a context-aware label depending on which side is open and the link type (`relatedSectionLabel()`) - "Related to"/"Documents"/"Action"/"Related document" - never a duplicate reverse row, since it's the same `list_record_links()` call with a different anchor. "View document" renders one row per attachment (never assumes a single attachment), each wired to Phase 16's `openAttachment()`.
- `src/components/RecordQuickEditor.tsx` and `src/screens/FirstThingScreen.tsx`: both hosts now do the real persistence -- `createRecordLink`/`removeRecordLink` (Phase 16's RPC wrapper) for links, building and saving a REAL canonical task record through the existing, unmodified `onSaveRecord` path for "Does anything need doing?", then linking it (`action_for`, task as source). Both gained in-sheet link navigation (`activeRecordId`/`openLinkedRecord()` respectively) so tapping a related record retargets the SAME sheet to that record's own read-only detail rather than closing/reopening a different one -- a Document → Appointment → Document traversal just keeps replacing what's on screen, never stacking hidden state.
- `App.tsx`: (1) a new effect scans the active care space's records for document attachments with a local file but no confirmed cloud upload, and queues them via `queuePendingAttachmentUploads()` -- this is what resumes a pending upload after a restart AND uploads a pre-Phase-16 document's existing local attachment the first time its owning device is online again, using the exact same function Phase 16 already wrote and this phase is the first to actually call; (2) `removeRecord()` now also fires (fire-and-forget, only after the canonical record's own tombstone/sync is already enqueued) two new best-effort cleanup calls -- `removeAllLinksForRecord()` (new, `src/recordLinks.ts`) and `cleanupDocumentAttachments()` (new, `src/attachments.ts`) -- completing the file/link lifecycle Phase 16 deliberately left unwired, without ever touching the record on the other end of a link.

Explicitly named, accepted limitations (documented rather than silently absent): the offline-delete cleanup above is best-effort only, not yet retried if it fails while offline; the Important Documents gateway's own list is unchanged (already showed expiry, already fully clickable) -- no new "Expiring soon"/"Recently added" grouping was added, since a threshold for "soon" is a product decision, not derivable from existing data; a brand-new document's "Related to" supports staging only one pick before its first Save (an existing document can accumulate any number over time) -- a deliberate scope reduction for the create flow, not an architectural limit.

New tests: `tests/phase17-record-editor.test.tsx` (8), `tests/phase17-record-detail.test.tsx` (8), `tests/phase17-record-quick-editor.test.tsx` (7, mocking `src/recordLinks.ts`/`src/attachments.ts` at the module boundary to prove the host wiring itself, including a genuine bidirectional-navigation test using an argument-aware mock so the appointment's own side correctly shows "Documents" rather than a self-referencing loop). Extended `tests/phase16-attachments.test.ts` (+4, `cleanupDocumentAttachments`) and `tests/phase16-record-links.test.ts` (+2, `removeAllLinksForRecord`). `careSpaceId` became a new required prop on `RecordQuickEditor`/`FirstThingScreen` (both RPC boundaries are always care-space-scoped) -- updated in `App.tsx` and every existing test file that constructs either component directly (`tests/record-quick-editor.test.tsx`, `tests/phase1-ui.characterization.test.tsx`, `tests/phase9-record-management.test.tsx`, `tests/responsive-layout.test.tsx`), with no existing assertion weakened or removed.

Validation: `npm run typecheck` clean; `npm test` 39 suites/395 tests (up from 36/366, zero regressions -- every pre-existing test that could plausibly touch this area, including the full auth/onboarding/records/sync/occurrences/assignment/projections/collaboration/UI suites named in the brief's own regression harness, still passes unchanged); `npm run secrets:check` clean; `npx expo config --type public` and `npx expo export --platform web` clean; local AND linked `db:test`/`db:lint` both clean (228 pgTAP assertions, no new migration this phase). One pre-existing, unrelated condition noted but deliberately not touched per this brief's own "no dependency upgrades" instruction: `npx expo install --check` reports several Expo packages a patch version behind their expected range -- present before this phase, not introduced by it.

`docs/PHASE_16_QA.md` updated in place (not replaced) to be the live Phase 17 physical-device checklist, per that phase's own explicit instruction. Not committed or pushed, per the brief's own instruction to stop and wait for product-owner review/physical QA first.

## 12 September 2026 - Phase 16 Document Maturity: foundation implemented (UI deferred to Phase 17)

Implemented from the approved Phase 16 brief (`Downloads\card.txt`). Baseline before this work: HEAD `a00c099` (in sync with `origin/master`), one known uncommitted change (`src/screens/PersonScreen.tsx`'s 2-column tile grid, from immediately prior in this session), TypeScript clean, 34 Jest suites/348 tests (347 passing; one pre-existing, unrelated UTC/local-date flake already documented below), 183 pgTAP assertions/6 files, clean lint, clean local rebuild.

Per direct product-owner instruction partway through this work, the brief's full scope (schema+RLS+storage, AND the RecordEditor/RecordDetail UI integration, AND physical QA) was split: this entry covers the durable foundation only - schema, permissions, cloud file transfer, and the client library/orchestration functions, all tested. The UI that actually lets a user create a link, answer "does anything need doing?", or tap "View document" is deliberately not built yet and is now **Phase 17**. Full detail: `docs/PHASE_16_ARCHITECTURE.md`; QA carried forward: `docs/PHASE_16_QA.md`.

- New migration `supabase/migrations/20260912100000_phase16_document_maturity.sql`: `record_links` (one stored relationship per pair, queryable from either side via `list_record_links()`, RLS requiring read access to BOTH sides to prevent revealing a link to a record the caller can't see, idempotent `create_record_link()`/tombstoning `remove_record_link()`); `record_attachments` (durable cloud-side attachment metadata distinct from the existing device-local `records.attachment_manifest`, idempotent `upsert_record_attachment()`, `mark_attachment_upload_status()` - never optimistically marks a file uploaded before the byte transfer succeeds, `remove_record_attachment()`); a new private `document-attachments` Storage bucket with `storage.objects` RLS mirroring the exact same `can_access_care_space_records()` domain-grant decision every record read/write already uses (no second authorisation system, no service-role key ever reaches the client).
- **Fixed a real, pre-existing bug found during the earlier Documents investigation**: `src/recordSync.ts`'s `localRecordFromRow()` used to reconstruct a pulled record's `attachments` from ONLY whatever the pulling device already had locally, never from the server's own `attachment_manifest` - so a document's attachments were invisible on any second device (or after a reinstall) even though the metadata was sitting in Supabase the whole time. Now builds `attachments` from the authoritative server manifest, overlaying a local `uri` only where this same device already has a copy under that attachment id.
- `src/types.ts`: `RecordAttachment.uri` is now optional (was required) - a record pulled fresh on a second device genuinely has no local file yet; new optional `storageObjectPath`/`uploadStatus` fields ride inside the existing `attachments` array and, since `recordPayload()` already spreads every attachment field except `uri`, flow through the existing sync mechanism with zero changes needed there.
- New `src/recordLinks.ts` (RPC wrapper, mirrors `src/careCircle.ts`'s exact pattern) and `src/attachments.ts` (`attachmentStoragePath()`, `queuePendingAttachmentUploads()` - upload/retry orchestration, never falsely marks a file uploaded on any failure, `openAttachment()` - "View document" via a local-copy-or-signed-URL-download then the native share sheet, using the newly added `expo-sharing` dependency). Neither `queuePendingAttachmentUploads` nor `openAttachment` is wired to any caller yet - that wiring is explicit Phase 17 scope, listed in full in `docs/PHASE_16_ARCHITECTURE.md`.
- Explicit `documents`-domain pgTAP coverage per the brief's section 13 (45 new assertions, 228 total across 7 files, zero regressions): organiser/contributor-with-grant/contributor-without-grant/a synthetic read-only grant/viewer/pending invitee/cross-care-space member/revoked member/assignment-does-not-grant-access, plus link idempotency/tombstone/cross-care-space-rejection/non-disclosure-to-an-unauthorised-viewer, plus attachment metadata idempotency and `storage.objects` RLS proven at the row level. One documented limitation: the storage DELETE policy could not be exercised via raw SQL locally (Supabase's own `storage.protect_delete()` blocks any raw delete regardless of role) - the policy itself is still correct and in place for the real Storage API.
- New client tests: `tests/phase16-record-links.test.ts` (5 tests), `tests/phase16-attachments.test.ts` (12 tests, with explicit `expo-file-system`/`expo-sharing` mocks). Two `tests/record-sync.test.ts` cases updated/added to prove the second-device fix (one previously asserted the OLD buggy behaviour and now asserts the fix; one new test proves this device's own local `uri` still survives its own create-then-echo round trip).
- Also folded into this pass, from direct product-owner instruction, several rounds of the same visual thread: `src/screens/PersonScreen.tsx`'s "Care circle" heading went through multiple iterations in one sitting, since its position on the shared gradient backdrop is content-dependent (it follows the variable-length Key contacts list above it, so it can land in either the deep teal zone or the pale mint tint zone depending on how much is above it). `colors.primary` text, the Settings cog's bright blue, and `colors.clay` were each tried as a single text colour meant to read on both zones and rejected on sight; a shared pill wrapping both the heading and a separate "Manage" link was tried next and rejected as one long fused pill; finally a heading-only pill and a separately-chipped "Manage" link were tried and rejected as two disconnected chips. It landed on: the heading and the "Manage" action are the SAME element -- a single `colors.primarySoft`/`radius.pill` chip (the same soft-primary pairing already used for this screen's person/contact avatar circles) reading "Manage care circle", replacing both the static "Care circle" heading and the separate "Manage" link (kept as a plain, non-pressable chip only in the rare case `onOpenCareCircle` is unavailable, i.e. a local-only care space with nothing to manage yet). The `accessibilityLabel` stayed exactly `"Manage Care Circle"` throughout, so `tests/phase13-person.test.tsx`/`tests/settings-cog.test.tsx` needed no changes. `src/screens/CalendarScreen.tsx`'s "Nothing planned for this day." empty-state text is now the normal dark colour (the now-unused `agendaSubtext` light style was removed) - the "Today" heading itself was already dark from an earlier fix this session.
- `src/screens/ToDoScreen.tsx`'s cards now lay out as a 2-up grid of tiles too, in line with People's Key contacts/Care circle tiles (`groupList` wraps into a row, each `row` at `width: '48%'`). Per direct follow-up feedback that the tile's text looked "squished" at half width, the tile's own internal layout changed from icon-beside-text (horizontal) to icon-above-text (vertical, `flexDirection: 'column'`) so the title/meta text spreads across the tile's own full width rather than being crushed into half a row; the trailing chevron (redundant once the whole tile is obviously a pressable card) was removed.
- Migration dry-run only, per standing instruction: `npx supabase db push --linked --dry-run` against `lilica-development` confirmed exactly this one pending migration, no unrelated diff. **Not applied to the hosted database** - awaiting explicit instruction.
- Validation: `npm run typecheck` clean; `npm test` 36 suites/366 tests (up from 34/348); `npm run secrets:check` clean; `npm run db:reset && npm run db:test && npm run db:lint` clean (228 pgTAP assertions, up from 183).
- Diff audit: production code - `src/types.ts`, `src/recordSync.ts`, `src/recordLinks.ts` (new), `src/attachments.ts` (new), `src/screens/PersonScreen.tsx`, `src/screens/CalendarScreen.tsx`, `src/components/SettingsCogButton.tsx`, `package.json`/`package-lock.json` (expo-sharing), `app.json` (expo-sharing config plugin); database - one new migration; tests - `tests/record-sync.test.ts`, two new test files; docs - this file, `docs/PHASE_16_ARCHITECTURE.md` (new), `docs/PHASE_16_QA.md` (new), `docs/PHASE_16_DOCUMENTS_INVESTIGATION.md` (pointer added, not rewritten), `docs/PHASE_15_ARCHITECTURE.md` (unchanged this pass - its addendum already covered the RLS verification), `AGENTS.md`, `CLAUDE.md`, `docs/LUMEN_HANDOFF.md`. Nothing unexpected/unrelated. Phase 17 was not started beyond being named and scoped in the docs above.

## 12 September 2026 - Phase 16 candidate: Documents architecture investigation, and Phase 15 RLS gap verified not real

Two investigation-only tasks, no code changed in either:

1. **Documents architecture investigation** (`docs/PHASE_16_DOCUMENTS_INVESTIGATION.md`), tracing the current repository line by line to establish what already supports the emerging Phase 16 product direction (a document may be stored for reference, LINKED to another record, and/or lead to a real TASK - never becoming a pseudo-task or pseudo-appointment itself). Key findings, condensed in that doc: the task-creation pipeline is fully reusable as-is; a typed `RecordLink` concept (`src/domain/types.ts`) was designed once and unit-tested in isolation but never persisted, never wired to the real record model, and has no table/RLS/UI; a real, previously-unknown gap was found (not caused by this investigation) - synced attachment metadata (`attachment_manifest`) is never read back into a pulled record's `attachments` field (`localRecordFromRow()` in `src/recordSync.ts` keeps only what the same device already has locally), so a document's attachments are invisible on any second device or reinstall even though the metadata is in Supabase; no document viewer exists anywhere (`RecordDetail.tsx` shows only the attachment's filename as text); `expiryDate` is architecturally isolated from a task's `dueDate`/an appointment's `eventDate` (good) but is otherwise inert (never drives Home's Today/Upcoming, never reminder-eligible).
2. **Phase 15 server-side permission verification**, triggered by a gap the investigation above flagged from reading `supabase/migrations/20260910150000_phase7_records.sql` in isolation (its `can_access_care_space_records()` hard-codes `role = 'organiser'`). Established this is **not a real gap**: `supabase/migrations/20260911120000_phase15_care_circle.sql` redefines the identical function name/signature (`create or replace function`) to properly honour Contributor/Viewer domain grants via the new `membership_has_domain_access()`, every existing caller (the `records` RLS policy, `apply_record_mutation()`, Phase 8 occurrence policies) automatically runs the new body, this is confirmed deployed to `lilica-development` (not just written locally, per `docs/SUPABASE_OPERATIONS.md`), and is directly exercised by 31 passing pgTAP assertions covering contributor-granted/ungranted domains, viewer read-without-write, and immediate revocation. Documented as an addendum to `docs/PHASE_15_ARCHITECTURE.md`.

Neither investigation authorises Phase 16 to begin - per the standing rule, it needs its own separate, explicit, bounded implementation prompt.

## 12 September 2026 - People tab: Care circle heading/Manage link colour

Product-owner observation: the "Care circle" section's position on the People tab's shared gradient backdrop (`ScreenBackdrop`) is content-dependent - it sits after the Key contacts list, so with enough contacts added it scrolls down out of the deep teal zone and into the lighter tint zone, where the white text the other (fixed-position) headings use becomes unreadable. `src/screens/PersonScreen.tsx`: "Care circle" and "Manage" now use `tone="primary"` (`colors.primary`, `#63364D`) - the same colour as the Ask Lilica question-mark circle's fill - instead of white. This colour reads clearly on the light tint (dark-on-light) and stays visible, if less crisp, on the deep teal, unlike the alternative of white (invisible on light) or leaving it white-only (invisible once scrolled into the tint zone). `npm run typecheck` and `npm test` pass (one pre-existing, unrelated UTC/local-date flake in `tests/home-strip-navigation.test.tsx`'s "Today / Needs doing" test, confirmed via `git stash` to already fail on the prior commit before this change - not caused by it, not yet fixed).

## 11 September 2026 - Record editor: Done/backdrop/swipe now save pending valid content

Product-owner report, tested and confirmed on-device: "tapping done before tapping add doesn't save the content... tapping done whether add was tapped or not should actually save and add the item and save." Previously, dismissing the record sheet via Done, a backdrop tap, or a swipe-down discarded any typed content unless the visible Save/Add button was pressed first.

- `src/components/RecordEditor.tsx`: converted to `forwardRef`, exposing `RecordEditorHandle = { save: () => boolean }` via `useImperativeHandle` - wraps the existing internal `save()`/`canSave` validation unchanged, never a second save path.
- `src/components/RecordSheet.tsx`: gained `onBeforeDismiss?: () => void`, called once as the first line of its single internal `dismiss()` - the one function shared by Done, backdrop tap and swipe-release alike.
- `src/components/RecordQuickEditor.tsx` and `src/screens/FirstThingScreen.tsx` (the two `RecordSheet` hosts) wire `onBeforeDismiss` to the mounted editor's `save()`, only while an editor is actually open (never from the read-only detail view). `RecordQuickEditor` needed a `closingViaSheet` ref flag to stop a brand-new record's `onSave` handler from calling `onDismiss()` a second time mid-close-animation (its pre-existing behaviour on an explicit button press) when triggered via the sheet's own closing animation instead.
- Tests: replaced the now-obsolete `tests/phase1-ui.characterization.test.tsx` test asserting "Done discards" with two reflecting the new behaviour; added direct `tests/record-quick-editor.test.tsx` coverage for edit-then-Done, new-draft-then-Done, and invalid-draft-then-Done (an invalid draft, e.g. a required date left blank, still correctly does not get force-saved). `npm run typecheck` clean; full suite 34/34 suites, 348/348 tests. Verified on-device after a full Expo Go restart (the `forwardRef` conversion did not survive an ordinary Fast Refresh reload, which briefly looked like the fix hadn't landed - a full app close/reopen resolved it).
- Also folded into this same commit (`ac53407`): two explicit heading-colour overrides from direct product-owner instruction - To Do's "Today / Needs doing" heading stays white regardless of render order (`src/screens/ToDoScreen.tsx`); Calendar's "Today" agenda heading reverted to default/black (`src/screens/CalendarScreen.tsx`).

## 11 September 2026 - Onboarding category alignment, Calendar layout, view/edit separation, and a full visual pass (Home/Calendar/To Do/People)

Committed as `258ee41`, bundling several product-owner-directed passes from the same working session:

- **View/edit separation** (new architecture, later reused throughout): an EXISTING record now opens a read-only `RecordDetail` view first (new `src/components/RecordDetail.tsx`, one shared category-aware presentation used by every host), never the editor directly. Edit - gated by the real Phase 15 `canEditRecord()` capability check (a viewer never sees it; a contributor only for a domain they were actually granted) - reaches the exact same, unchanged `RecordEditor`. Save returns to the (now updated) detail, staying open. A brand-new draft (Add) is unaffected - it still opens straight into the editor and still closes on save, exactly as before.
- **Visual pass** across Home, Calendar, To Do and People (Home was later reverted to its original plain background - see below): new `src/theme.ts` export `tabAccent` (per-tab `{deep, tint}` colour pairs reusing Welcome's own slide colours) and new `src/components/ScreenBackdrop.tsx` (a shared gradient backdrop, `expo-linear-gradient`). Iterated through several rounds of direct visual feedback on an HTML mock artifact before implementation. One genuine architectural bug found and fixed during this work, prompted by the product owner asking "can the headings colour adapt based on the background?": `ScreenBackdrop` originally sat OUTSIDE each screen's `ScrollView` (fixed to the viewport, so a heading's colour-vs-background relationship changed continuously with scroll position) - fixed by moving it INSIDE the `ScrollView` (content-relative, scrolls away with everything else), after which static per-heading colour decisions became valid. Per explicit instruction, Home's background was subsequently reverted in full (`git checkout -- src/screens/HomeScreen.tsx`) back to its plain original state; Calendar/To Do/People keep the visual pass.
- **Onboarding category alignment / Calendar layout**: presentation-only adjustments bundled into the same commit (see commit `258ee41` diff for the exact scope; no domain/schema change).
- Validated at 32+/32+ suites passing before commit; see the visual-pass follow-up entries below for the Wordmark and Settings-cog fixes that landed immediately after.

## 11 September 2026 - Wordmark-on-dark and Settings-cog follow-ups (same visual pass)

Two direct product-owner reports against the freshly-landed visual pass, fixed the same day:

- **Wordmark invisible on dark backgrounds**: `src/components/Wordmark.tsx`'s tone resolution checked `isCompact` before `isLight` (`tone={isCompact ? 'muted' : isLight ? 'white' : 'default'}`), so `tone="light"` was silently ignored whenever `size="compact"` was also set - which every one of the 4 tabs' headers does. Reordered to `tone={isLight ? 'white' : isCompact ? 'muted' : 'default'}`, plus a new `wrapCompactLight` style (opacity 0.85, up from the light-watermark default of 0.6, which was still too faint on a deep background).
- **Settings cog restyled bright blue with a white gear**: `src/components/SettingsCogButton.tsx` gained a local `BRIGHT_BLUE = '#2E7DF5'` constant (deliberately not added to the shared `colors` theme, as a one-off accent) plus a glow (`shadowOpacity: 0.65`, `shadowRadius: 8`); gear teeth/ring changed from `colors.blue` to `colors.white`.
- Separately investigated and resolved as a non-issue: a reported "duplicate/oversized Settings cog" turned out to be Expo Go's own development-mode chrome (tapping it opened Expo Go's own settings, not Lilica's), not Lilica code - documented as a standing "known false alarm" in `CLAUDE.md` so it is not re-investigated.

## 11 September 2026 - Fix record-open "flash" architecturally; Corrective Task 10: People tab IA

Committed as `3272a64`:

- **Flash bug**, root-caused via a product-owner-led walkthrough rather than assumption: tapping a record briefly showed the wrong screen behind the sheet on every tab. The actual cause was `openRecordFromProjection` navigating the whole app to `FirstThingScreen` just to host the editor - swapping that whole screen in/out on open/close is what read as a flash, not an animation-timing issue (an earlier `instant` prop on the sheet, since removed, could never have fixed it). Fixed architecturally with a new `src/components/RecordQuickEditor.tsx`: the exact same `RecordSheet`+`RecordEditor`/`RecordDetail` pairing `FirstThingScreen` already used, but mounted directly as an overlay over whichever screen is already showing - Home/Calendar/To Do/People never unmount, so there is nothing behind the sheet to flash to.
- **Corrective Task 10** (`docs/CORRECTIVE_TASK_10_PEOPLE_IA.md`): renamed the fourth tab from Person/Care Circle to **People**, centred on Supported people / Key contacts (external, no Lilica account) / the real Care circle (authenticated members, from Phase 15's own membership list) / an Ask Lilica placeholder (moved from Home, still not implemented) - no longer a second Home dashboard duplicating bills/home/documents/care-note sections, which had given the tab no distinct purpose from Home's own "Recently added". Those record types are untouched in storage and still appear correctly in Home/Calendar/To Do; see `docs/CORRECTIVE_TASK_10_QA.md`.
- Copy tweaks in the same pass: removed a redundant subtitle line above "Person being supported" (which repeated the heading immediately below it) and amended "People you support" to "Person being supported"; removed the two remaining user-facing em-dashes in `PersonScreen.tsx` per the standing no-AI-dashes rule (code-comment em-dashes elsewhere were flagged, not swept, as out of scope).
- Validated at 32/32 suites before commit.

## 11 September 2026 - Phase 15 follow-up: invitee-facing Invitations screen

Requested directly ("build the invitee-facing invitation screen next") as the first of Phase 15's documented follow-up items. New `src/screens/InvitationsScreen.tsx`: lists the signed-in account's own pending invitations (by authenticated email, via the existing `list_my_invitations()`), with Accept/Decline per invitation and a "Not now" that leaves every invitation untouched.

`App.tsx` fetches invitations in the same effect that already runs `reconnectCareSpaces()` once per signed-in owner. The screen auto-opens the first time that fetch finds any pending invitation - ahead of the normal onboarding/Home flow, so a brand-new invitee who signed up specifically to accept sees it immediately - and does not reopen itself again this session once dismissed. Person's header gained an "Invitations (N)" link (shown only when N > 0) as the way back in after "Not now". Accepting needed no new client-side plumbing to surface the resulting membership: the existing, unmodified `reconnectCareSpaces()`/`integrateReconnectedCareSpaces()` flow already picks it up, since `list_my_supported_people()` was never role-filtered. A failed accept/decline shows the server's own error inline against that invitation and leaves it in the list, never guessing success. `src/careCircle.ts` gained an exported `DOMAIN_LABELS` map (factored out of `CareCircleScreen`'s local one) so both screens describe domains identically.

New `tests/phase15-invitations-screen.test.tsx` (5 tests: content shown, accept/decline call the right id, a failure surfaces inline without removing the invitation, "Not now" touches nothing). `npm run typecheck`, `npm test` (23 suites/263 tests, up from 258) and secret scan/web export all pass; no database change.

Diff audit: `App.tsx`, `src/careCircle.ts`, `src/screens/PersonScreen.tsx`, `src/screens/CareCircleScreen.tsx` (only its `DOMAIN_OPTIONS` now derives from the shared `DOMAIN_LABELS`, no behaviour change), one new screen, one new test file, plus `docs/PHASE_15_ARCHITECTURE.md`/`docs/PHASE_15_QA.md`/`AGENTS.md`/`CLAUDE.md`/`docs/LUMEN_HANDOFF.md` updated to remove the now-resolved "no invitee screen yet" caveat.

## 11 September 2026 - Phase 15 care-circle invitations and collaboration

Implemented from a written, bounded product-owner brief whose own Hard Start Gate (product-owner confirmation of Phase 14 physical QA) was explicitly waived by the product owner before this phase began - Phase 14 is implemented/validated but not yet physically tested on a development build. `PRE_PHASE_15_BASELINE`: HEAD `52d28c0` (in sync with `origin/master`), clean working tree, TypeScript clean, 20 Jest suites/247 tests, secret scan clean, 183 pgTAP assertions across 5 files (152 Phase 6/7/8/9-era plus 31 already-counted-elsewhere - see note below), clean DB lint, clean web export. (Baseline pgTAP count before this phase's own new test file was 152 across 5 files; this phase adds a 6th file.)

Core invariant: sharing must be real, explicit, permissioned and revocable - never inferred from a name, email, relationship label, contact, or the legacy `responsiblePerson` free-text field. Assignment ("who's expected to deal with this") and permission ("who's allowed to see this") stay strictly separate; assigning a record to someone without visibility into its domain is rejected server-side, not merely hidden client-side.

Traced the existing Phase 6/7 skeleton before writing anything new, per the brief's own instruction: `care_space_memberships.membership_status` (active/revoked) already existed and needed no new column for removal/leaving; `can_access_care_space_records()` was already code-commented since Phase 7 as the intended Phase 15 extension point; the domain taxonomy (general/health/financial/home/documents) already existed via `record_domain_for_type()`.

- New migration `supabase/migrations/20260911120000_phase15_care_circle.sql`: extends `care_space_memberships.role` to `organiser`/`contributor`/`viewer` (was hardcoded organiser-only); new `care_space_domain_grants` (explicit per-membership per-domain read/write, default-deny); new `care_space_invitations` (pending/accepted/declined/expired/revoked lifecycle); new `membership_has_domain_access()` and a redefined `can_access_care_space_records()` deciding access by role-or-grant; a redefined `apply_record_mutation()` (same signature) checking write access against a record's real domain (was hardcoded to `'general'`) and rejecting any `assignedMembershipId` lacking active status or domain read access - closing the gap where an unvalidated assignment could grant de facto visibility, and where a replayed offline mutation could recreate access for a removed member; new RPCs `invite_member`, `list_my_invitations`, `accept_invitation`, `decline_invitation`, `revoke_invitation`, `change_member_role`, `remove_member`, `leave_care_space`, `list_care_space_members`, `list_care_space_invitations`, all authenticated/authorised/transactional/idempotent with sole-organiser safety on removal/role-change/leaving.
- `protect_membership_identity()` redefined to drop `role` from its immutability check (role is now a legitimate, authorised transition gated by `change_member_role()`'s own checks, not by trigger immutability) while keeping `care_space_id`/`user_id`/`bootstrap_id` immutable.
- New `src/careCircle.ts`: a thin, typed RPC wrapper following `src/careSpaces.ts`'s exact pattern (`{ ok, data | message }`, never throws, all authority server-side).
- New `src/screens/CareCircleScreen.tsx`, reachable from Person's header via a "Care Circle" link next to "Account" (shown only for a real, non-local, synced care space).
- `src/records.ts` gained `recordDomainForType()`, an exact client-side mirror of the server's `record_domain_for_type()`.
- `src/components/RecordEditor.tsx`'s Assigned-to control gained an optional `careCircleMembers` prop: when present, offers every active member with visibility into the record's domain instead of only Unassigned/You; falls back to exactly the pre-Phase-15 behaviour when absent, so all existing Phase 9/12/14 tests pass unchanged.
- `App.tsx` fetches the active care space's members/invitations via `listCareSpaceMembers`/`listCareSpaceInvitations` whenever the active space changes or Care Circle is opened, and threads `careCircleMembers` down through `FirstThingScreen` to `RecordEditor`.

New test files: `supabase/tests/database/phase15_care_circle.test.sql` (31 pgTAP assertions - invite→pending→zero-access, accept→exactly-granted-domains, default-deny, forbidden/permitted assignment, viewer read-without-write, immediate zero-access on removal with attribution retained, stale/replayed mutation cannot restore removed-member access, sole-organiser removal/leave protection, invitation terminal states, cross-space/non-organiser denial - 183 total across 6 files, zero regressions), `tests/phase15-care-circle.test.ts` (8 tests - RPC wrapper mapping, `recordDomainForType()` parity), `tests/phase15-record-editor.test.tsx` (4 tests - domain-eligible filtering, membership-ID-not-name storage, pre-Phase-15 fallback). `npm run typecheck`, `npm test` (22 suites/258 tests, up from 247) and `npm run validate:all` all pass. Migration dry-run listed exactly `20260911120000_phase15_care_circle.sql`; applied to `lilica-development` only; linked pgTAP (183 assertions) and lint both clean afterward - see `docs/SUPABASE_OPERATIONS.md`'s "Hosted Phase 15 deployment" entry.

Deliberately not built this phase, documented rather than faked (see `docs/PHASE_15_ARCHITECTURE.md`'s "What Is Deliberately Not Built Yet"): an invitee-facing "you've been invited" screen (the RPCs exist; no screen calls them for someone not yet a member), push/email delivery of the invitation itself, an "assignee no longer has access" UI treatment, and an in-app role-change/organiser-handoff control (their server functions exist and are tested independently). None of these are represented as complete anywhere in the docs.

Diff audit: one new migration, `supabase/tests/database/phase15_care_circle.test.sql`, `src/careCircle.ts`, `src/screens/CareCircleScreen.tsx`, `src/records.ts`, `src/components/RecordEditor.tsx`, `src/screens/FirstThingScreen.tsx`, `App.tsx`, `src/screens/PersonScreen.tsx`, two new test files, plus this documentation pass (`AGENTS.md`, `CLAUDE.md`, `README.md`, `docs/PROJECT_BRIEF.md`, `docs/LUMEN_HANDOFF.md`, `docs/SUPABASE_OPERATIONS.md`, this file, `docs/PHASE_15_ARCHITECTURE.md`, `docs/PHASE_15_QA.md`) - every changed file has a clear Phase 15 reason. No Welcome/auth/onboarding/Home/Calendar/To-Do/Person-design/theme-token file was redesigned; Person gained only the same kind of small header link Phase 13 already established for Account. Phase 7 sync internals and Phase 14's reminder engine internals were not modified (only reused, unchanged, as data sources). No secrets, push credentials or generated exports included.

## 11 September 2026 - Phase 14 reminder and notification engine

Implemented from a written, bounded product-owner brief. `PRE_PHASE_14_BASELINE`: HEAD `ee4df9a` (in sync with `origin/master`), clean working tree, TypeScript clean, 17 Jest suites/208 tests, secret scan clean, 152 pgTAP assertions across 5 files, clean DB lint, clean web export.

Scoped to **local device notifications only**, per explicit product-owner decision after flagging a real constraint up front: Expo Go has not reliably supported local notifications since SDK 53, so testing this phase for real needs a development build rather than the usual Expo Go workflow. Server/push delivery, device-token registration and Phase 15 collaboration notifications are deliberately deferred and documented, not faked, per the brief's own instruction to implement only the safe foundation and stop.

The core invariant - record/occurrence state, in-app attention state, and notification-delivery state are three separate things, and dismissing/snoozing a notification never completes/pays/attends anything - was already anticipated in `src/domain/core.ts`'s unused `acknowledgeReminder()` helper from Phase 8; Phase 14's local implementation follows the same separation for the client-side model.

- New `src/reminders.ts`: pure domain logic (eligibility, default offsets - 1 day/2 hours before a timed appointment, 3 days before/on the day for a date-only item - quiet-hours policy, deterministic idempotent identifiers, conservative lock-screen-safe wording). No native dependency; fully unit-tested.
- New `src/notifications.ts`: the thin `expo-notifications` boundary, guarded for web throughout. One reconciliation entry point (`reconcileRecordReminders`) called from `App.tsx`'s `saveRecord`/`removeRecord` after every mutation - covers creation, date edits, completion and cancellation through the single existing save path.
- `src/types.ts` gained two optional fields - `remindersEnabled`, `reminderScheduleVersion` - riding inside the existing record shape exactly like Phase 9's `assignedMembershipId`. No migration.
- `RecordEditor.tsx` gained a "Remind me" toggle (appointment/task/bill/homeMatter only), gated behind an explicit permission request that only fires when the user turns it on - never at launch or during onboarding. The schedule version bumps only when the record's own relevant date/time actually changes.
- `AccountScreen.tsx` gained a minimal Reminders section (master switch, quiet hours on/off) - reached via Person's existing "Account" link.
- New `expo-notifications` dependency (`app.json` gained its config plugin, a `color` tint and an Android "Reminders" channel); `npx expo install --check` and the web export both remain clean with it installed.

New test files: `tests/phase14-reminders.test.ts` (21 tests - eligibility, the fixed appointment/bill scenarios, too-late-lead-time omission, quiet hours including an overnight window, BST/GMT/month-end/leap-day handling, idempotent identifiers, conservative content), `tests/phase14-notifications.test.ts` (12 tests, `expo-notifications` mocked - idempotent scheduling, version-change cancellation, completion/cancellation suppression, snooze namespacing and its natural ceiling, permission mapping), `tests/phase14-record-editor.test.tsx` (6 tests - toggle visibility per type, permission gating, schedule-version bump behaviour). `npm run typecheck`, `npm test` (20 suites/247 tests, up from 208) and `npm run validate:all` (152 pgTAP assertions unchanged, no migration, clean lint, clean web export) all pass.

Diff audit: `App.tsx`, `app.json`, `package.json`/`package-lock.json`, `src/components/RecordEditor.tsx`, `src/screens/AccountScreen.tsx`, `src/screens/FirstThingScreen.tsx`, `src/types.ts`, `tests/auth-flow.test.tsx` (updated for `AccountScreen`'s new required props) plus two new source files and three new test files - every changed file has a clear Phase 14 reason. No welcome/auth/onboarding/fork/Home/Calendar/To-Do/Person/theme-token file appears in the diff beyond the necessary `AccountScreen` addition. `git diff --check` clean; no secrets, push credentials or generated exports included.

## 11 September 2026 - Phase 13 Person projection

Implemented from a written, bounded product-owner brief. `PRE_PHASE_13_BASELINE`: HEAD `b76446f` (in sync with `origin/master`), clean working tree, TypeScript clean, 16 Jest suites/192 tests, secret scan clean, 152 pgTAP assertions across 5 files, clean DB lint, clean web export.

Tracing the current repository (per the brief's own instruction not to assume an old prototype structure survives) found a real structural mismatch worth reporting before building anything: the tab labelled with the supported person's name rendered `AccountScreen` - the organiser's own account settings - under copy that promised a person knowledge space it never delivered ("Keep their appointments, home details, documents and contacts together here."). Phase 13's whole purpose is to make that tab the real thing, so this was fixed as the necessary minimum: `AccountScreen` is now reached via a small "Account" link in the new Person screen's header (its own content unchanged, minus the stale copy), preserving sign-out access without adding a new tab.

- New `src/screens/PersonScreen.tsx`: groups `state.records` - the same projection Home/Calendar/To Do already read - into five durable-knowledge sections (Important contacts / Care & health information / Home / Documents & paperwork / Bills & renewals), each backed by one existing record type (`contact`/`careNote`/`homeMatter`/`document`/`bill`), omitted entirely when empty. Appointments, tasks and updates never appear - they belong to Calendar, To Do and Home's Latest respectively. A static "Care circle" section always shows the organiser as "You", the only member today, with no fake invite affordance. Self-care wording ("You") is driven by the explicit `relationshipType === 'Myself'` check already used elsewhere, never by matching a display name.
- `FirstThingScreen.tsx` gained a sibling `initialOpenType` prop (alongside Phase 10's `initialOpenRecordId`, same one-shot consume/clear contract) so Person's per-section "Add" links open a *new* draft of the right category directly, through the exact same category/record creation architecture.
- `AccountScreen.tsx`: removed the stale person-framing copy and its now-orphaned `supportedPersonName` prop; added an optional `onBack` (rendered as the existing shared `Header` component) so it can be reached and left from Person.
- `App.tsx`: the Person tab now renders `PersonScreen`, with a `showAccount` toggle (cleared when leaving the tab) swapping in `AccountScreen` when its "Account" link is tapped. Opening/adding records reuse the exact same `initialOpenRecordId`/`initialOpenType`/`onSaveRecord` paths Calendar and To Do already use.
- No database migration, no RLS change, no medical/legal inference anywhere in the new screen - verified by tests.

New `tests/phase13-person.test.tsx` (16 tests) covers the brief's fixed test scenario verbatim, no-inference guarantees (a medication note never implies a diagnosis, a power-of-attorney document is never shown as confirmed legal authority, no generated summaries), empty/sparse states, identity (opening the real record ID, no duplication), explicit (never name-matched) self-care wording, and care-space isolation. `npm run typecheck`, `npm test` (17 suites/208 tests, up from 192) and `npm run validate:all` (152 pgTAP assertions unchanged, clean lint, clean web export) all pass.

Diff audit: 4 files touched (`App.tsx`, `src/screens/AccountScreen.tsx`, `src/screens/FirstThingScreen.tsx`, `tests/auth-flow.test.tsx` - updated for `AccountScreen`'s prop change) plus one new screen and one new test file - all with a clear Phase 13 reason. No welcome/auth/onboarding/fork/Home/Calendar/To-Do/theme-token file appears in the diff beyond the necessary `AccountScreen` routing change. `git diff --check` clean; no secrets, env files or generated exports included.

Not committed or pushed pending product-owner review and physical-device QA, per the implementation brief. Phase 14+ was not started.

## 10 September 2026 - Phase 12 To Do projection

Roadmap numbering note first: the product owner clarified that the work committed as "Phase 10" (immediately below) implements the canonical roadmap's **Phase 11 - Calendar Projection** (`docs/CORE_SYSTEM_CONTRACT.md` numbers its own Phase 10 as Home Projection, effectively already delivered by the earlier Home redesign). Historical commits/entries saying "Phase 10" for Calendar are not renamed or rewritten; numbering resumes canonically from **Phase 12 - To Do Projection** onward, per this entry.

Implemented from a written, bounded product-owner brief. `PRE_PHASE_12_BASELINE`: HEAD `c85a58d` (in sync with `origin/master`), clean working tree, TypeScript clean, 15 Jest suites/176 tests, secret scan clean (135 files), 152 pgTAP assertions across 5 files, clean DB lint, clean web export.

To Do is a projection, not a second task store: it reads `state.records` (the same Home/Calendar already read) and a new `isActionableRecord()` in `src/records.ts` decides eligibility per `docs/CORE_SYSTEM_CONTRACT.md` section 9.3 - open task/bill/home-or-car-matter records, never appointments/documents/contacts/care-information/updates even though some are dated.

- New `src/screens/ToDoScreen.tsx`: Overdue / Today-Needs-doing / Upcoming groups (30-day horizon, matching Home's own Coming Up window), an All/Mine/Unassigned assignment-filter row, and per-row "Mark complete"/"Mark paid"/"Reopen" actions. A "Show completed" toggle (off by default) keeps completed items reachable for reopening without a second screen.
- New `completionUpdate()` in `src/components/RecordEditor.tsx`: the exact status/completed/completedAt/confirmationHistory transition `RecordEditor.save()` already applies inline for its "Already sorted" checkbox, factored out (save() itself untouched) so To Do's quick actions produce a byte-identical mutation to the established editor - never a second interpretation of "complete".
- Assignment filtering and the row's You/Unassigned badge use Phase 9's stable `assignedMembershipId` exclusively - never a display name or the legacy `responsiblePerson` text, and no third assignment representation was created.
- `App.tsx`: the To Do tab now renders `ToDoScreen` (previously a placeholder) once the active care space's setup is `ready`, mounted with `key={careSpaceId}` for the same clean-switch guarantee Calendar already has. Tapping a row or completing an item reuses the exact same `initialOpenRecordId`/`onSaveRecord` paths Calendar and Home already use.
- No database migration, no RLS change, no new assignment architecture.

New `tests/phase12-todo.test.tsx` (16 tests) covers the brief's fixed test scenario verbatim, eligibility, identity (no duplication), the shared completion transition, assignment filtering (including that `responsiblePerson` and a changed display name never affect it), care-space isolation, and that Add reuses the established creation flow. `npm run typecheck`, `npm test` (16 suites/192 tests, up from 176) and `npm run validate:all` (152 pgTAP assertions unchanged, clean lint, clean web export) all pass.

Diff audit: 3 files touched (`App.tsx`, `src/records.ts`, `src/components/RecordEditor.tsx`) plus one new screen and one new test file - all with a clear Phase 12 reason. No welcome/auth/onboarding/fork/PersonSwitcher/Home/Calendar/theme-token file appears in the diff. `git diff --check` clean; no secrets, env files or generated exports included.

Not committed or pushed pending product-owner review and physical-device QA, per the implementation brief. Phase 13+ was not started.

## 10 September 2026 - Phase 10 Calendar projection

Implemented from a written, bounded product-owner brief. `PRE_PHASE_10_BASELINE`: HEAD `62368db` (in sync with `origin/master`), clean working tree, TypeScript clean, 14 Jest suites/157 tests, clean web export -- the same numbers already recorded as passing immediately before this brief was issued, in this same session. No migration exists on either side of this phase, so the last recorded pgTAP baseline (152 assertions, Phase 8/9) is unchanged.

Calendar is a projection, not a second record system: it reads `state.records` -- the exact same active-care-space projection Home already reads via `projectActiveCareSpace()` -- and resolves each record's calendar date with a new `calendarDateForRecord()` in `src/records.ts`, mirroring `domain/recordOccurrence.ts`'s existing field-priority (appointment -> `eventDate`; task/bill/homeMatter -> `dueDate`; document -> `expiryDate`) without requiring a synced cloud record ID, for the same offline-first reason Home already bypasses the Phase 8 domain layer. Status (Overdue, etc.) reuses the existing `deriveRecordState()` exactly -- no second lifecycle interpretation.

- New `src/screens/CalendarScreen.tsx`: a month grid (Monday-first, today and selected-day marked, a small dot on dates with occurrences) and a selected-day agenda list below it, in the app's existing tokens/type/icon language. Categories reuse `CategoryIcon`/`categoryLabel`/`visualFor`, exported from `HomeScreen.tsx` (previously private, now shared -- zero behaviour change to Home).
- `App.tsx`: the Calendar tab now renders `CalendarScreen` (previously a placeholder `FoundationScreen`) once the active care space's setup is `ready`, mounted with `key={careSpaceId}` so switching person always starts fresh -- no stale state, no cross-space leak. Tapping an agenda item sets a new one-shot `calendarOpenRecordId` and navigates to the existing `firstThing` stage, the same screen Home's own Add action already uses.
- `src/screens/FirstThingScreen.tsx`: gained an optional `initialOpenRecordId`/`onInitialOpenHandled` prop pair. On mount, if set, it opens that exact existing record's editor via the screen's own existing `openEditor()` -- not a new editor, not a duplicate draft -- then reports back so the request is consumed exactly once; a later, unrelated visit (e.g. Home's plain Add button) never reopens a stale record.
- No database migration, no RLS change, no assignment-architecture change: Assigned-to display in Calendar (where used) is presentational only, reusing Phase 9's existing Unassigned/You scope. Legacy `responsiblePerson` is never surfaced as an assignment claim.

New `tests/phase10-calendar.test.tsx` (17 tests) covers month/date rendering, multi-occurrence days, category projection, undated-record exclusion, past-appointment/Overdue agreement with the existing engine, opening the correct underlying record ID, no cross-care-space leak on person switch, no mutation during month navigation, the empty-day state, and that `initialOpenRecordId` opens the same record rather than a new draft. `npm run typecheck`, `npm test` (15 suites/174 tests, up from 157 -- old tests unchanged, new tests additive) and `npm run validate` (web export included) all pass.

Diff audit: 4 files touched (`App.tsx`, `src/records.ts`, `src/screens/FirstThingScreen.tsx`, `src/screens/HomeScreen.tsx` -- the last for `export` keywords only) plus one new screen and one new test file. No welcome/auth/onboarding/fork/PersonSwitcher/privacy/interests/category-gateway/wheel-picker/keyboard/theme-token file appears in the diff. `git diff --check` clean; no secrets, env files or generated exports included.

Not committed or pushed pending product-owner review and physical-device QA, per the implementation brief. Phase 11+ was not started.

## 10 September 2026 - Calendar redesign: icon-chip day cells and an icon legend

Product owner asked for the Calendar's month grid to draw from Monzo's spending calendar (`222.jpeg`), sense-checked a first pass from ChatGPT (`111.png`), then asked for a mock in Lilica's own design system rather than adopting that pass wholesale. Mocked in a design canvas, revised once (the icon legend was present in markup but silently clipped by an `overflow:hidden` scroll container that had run out of vertical room -- fixed by trimming cell/spacing sizes and switching that container to scroll), approved, then implemented into `CalendarScreen.tsx`:

- Each day cell with occurrences now shows a small tinted icon-chip badge -- the same `CategoryIcon`/`visualFor` colours already used everywhere else -- for the highest-priority category that day (priority: appointment, task, bill, home/car matter, document), with a `+N` count beneath it when more than one item falls on that date.
- A day with any overdue item gets a small red corner dot on its badge (reusing `colors.danger`, no new interpretation of overdue -- still `deriveRecordState().overdue`).
- A new icon legend row beneath the grid explains every glyph that can appear, reusing the same icon/tint/label for each category plus a "Needs attention" key (`StatusIcon`'s existing alert icon, now exported from `HomeScreen.tsx` alongside `CategoryIcon`).

No architecture change: still the same `state.records` projection, the same `calendarDateForRecord()`/`deriveRecordState()` resolution, the same record editor on tap. New tests in `tests/phase10-calendar.test.tsx` cover the legend and the `+N` overflow badge (19 tests in that file now, up from 17). `npm run typecheck`, `npm test` (15 suites/176 tests) and `npm run validate` all pass. Not committed or pushed, per the standing Phase 10 instruction.

## 10 September 2026 - Home's horizontal strip repurposed into an "at a glance" status summary

Product owner (relaying a ChatGPT-drafted proposal, then a reference image) judged the horizontal category-snapshot row a duplicate of the record boxes - "wasting prime space, making Home feel like a catalogue" - and asked for it to summarise **state**, not **category**, with an explicit caution: only surface a chip backed by real current projection logic, never a fake count.

`src/screens/HomeScreen.tsx`: the row now shows up to five chips - Overdue, Due today, Coming up, Assigned to you, Updates this week - each counted straight from data the rest of Home already derives, nothing new:

- Overdue / Due today / Coming up: `deriveRecordState(record).overdue` / `.dueToday` / `.upcoming`, the same fields the section grouping above already uses.
- Updates this week: `deriveRecordState(record).recentlyUpdated` (already existed, unused until now).
- Assigned to you: `record.assignedMembershipId === currentSpace.membershipId` (Phase 9's stable-identity field). This chip is **omitted entirely**, not shown as a fake zero, whenever `activeCareSpace(state)?.membershipId` is unavailable - matching the caution exactly.

Two new soft-tint theme tokens (`dangerSoft`, `warningSoft`) added to `theme.ts`, extending the existing hue+soft-pair pattern every other color already has, for the Overdue/Due-today chips; Coming up reuses `olive`/`oliveSoft`, Assigned to you reuses `primary`/`primarySoft`, Updates this week reuses `blue`/`blueSoft` - no arbitrary new colors. New `StatusIcon` (alert, calendar, people, clock) built from plain Views, reusing the calendar/person shapes already drawn for `CategoryIcon` in this same file. The old `SNAPSHOT_TYPES`/8-category row (and its now-unused styles) is gone; `CategoryIcon`/`categoryLabel` remain, still used by the sections list below.

Two new tests in `tests/phase1-ui.characterization.test.tsx` cover the four always-present chips and the Assigned-to-you omission/inclusion behaviour. `npm run typecheck`, `npm test` (157 tests, up from 155) and `npm run validate` all pass.

## 10 September 2026 - Welcome carousel redesign, mocked and approved first

Product owner supplied a reference image (`Lilica Care Onboarding Screens.png`) and asked for a mockup before any code changed. Drafted a 3-artboard Claude Design canvas matching it against the app's real tokens (Fraunces headings, existing per-page colors, spacing/radius scale), iterated through two rounds of feedback (raise/lower text position; smaller hero circles with a bigger gap to the heading), then implemented the approved result into `src/screens/WelcomeScreen.tsx` and `src/components/BrandVisual.tsx`:

- Page 1 copy simplified to the reference's heading + single paragraph (dropped the old bullet pair and closing line); pages 2 and 3's body copy and bullet wording/order updated to match the reference exactly (e.g. "Household tasks" replacing "Home and car tasks").
- Plain bullet-dot rows on pages 2 and 3 replaced with icon-badge rows: a translucent circular badge holding a small drawn icon (calendar, document, house, check, people, chat, list, paper-plane), built from plain Views only, matching the app's existing icon technique (`Wordmark`'s leaf, `HomeScreen`'s `CategoryIcon`) - no icon library added.
- All three hero visuals (the real illustrated `BrandVisual` image, and the drawn `TogetherVisual`/`AheadVisual`) shrunk by about 17%, with more vertical space opened up between the hero and the heading below it.
- The final "Get started" button gained a trailing arrow (previously text-only), built as a small chevron View rather than the shared `Button` component's `icon` slot, since that slot renders before the label and the approved mock wants the arrow after it.
- One deliberate deviation from the reference, flagged and accepted: "Get started" stays on the last page only (page 3), not page 1 as the reference literally showed - matching the app's correct onboarding order.

Not changed: the paging/scroll mechanics, the compact-height fallback, `Screen`/keyboard architecture, auth, or any other screen. `npm run typecheck`, `npm test` (155 tests - one characterization test's copy assertion updated to match) and `npm run validate` (web export included) all pass.

## 10 September 2026 - Count-saved badge moved to the category-mark corner

Product-owner follow-up: the count badge (added earlier the same day) should sit like an unread-message badge - pinned to the corner of the category's icon mark - rather than in the row next to "Add". Moved `countBadge` into `categoryMark` as an absolutely-positioned corner badge (with a border matching the row's own background, so it reads as sitting on top of the icon), and "Add" now always renders plainly in its own slot regardless of whether the category has records. `npm run typecheck` and `npm test` (155 tests) both pass.

## 10 September 2026 - An appointment happening today shows under Today, not Needs attention

Product-owner question: why did an appointment scheduled for today appear under "Needs attention" on Home instead of "Today"?

`HomeScreen.tsx`'s `sectionFor()` checked the generic `derived.overdue || derived.dueToday` (true for any record type due exactly today) before its appointment-specific "Today" check, so a today's appointment always matched the first, generic branch and the appointment branch was never reached. Reordered so an overdue (missed) appointment still lands in "Needs attention", a today appointment now lands in "Today", and a bill/task/home-or-car-matter due today is unaffected - it still lands in "Needs attention", which is the existing, correct behaviour for those types.

Updated the characterization test that had documented the old behaviour (now split into two tests: today-vs-past appointment, and a bill still landing in Needs attention when due today). `npm run typecheck` and `npm test` (155 tests, up from 154) both pass.

## 10 September 2026 - Count badge coexists with Add; Home snapshot row matches the record boxes

Two corrections from the same product-owner check:

1. The saved-count badge just added to `FirstThingScreen`'s category boxes had replaced the "Add" label entirely once a category held records. The box is still there to add more, so "Add" now sits alongside the count badge rather than being replaced by it (`addAffordance` changed from a single fixed-width slot to a row holding both).
2. Home's horizontal category snapshot row (`HomeScreen.tsx`) only ever showed four categories (To do, Bill, Home or car matter, Appointment) - the original approved mockup's subset - which no longer matched the eight category boxes on the person's own records screen. `SNAPSHOT_TYPES` now lists all eight, in the same order as `firstItemOptions` (Appointment, Task, Bill, Home or car matter, Document, Contact, Care information, Update). The visual treatment (icon, tint, accent) for all eight already existed in `CATEGORY_VISUALS`/`CategoryIcon` from the original build - only four were ever wired into the row.

New test in `tests/phase9-record-management.test.tsx` covers Add and the count badge both being present together. `npm run typecheck` and `npm test` (154 tests, up from 153) both pass.

## 10 September 2026 - Category gateway: saved-count badge, not "Added"

Product-owner observation: on a supported person's records screen (`FirstThingScreen`, both first-time and everyday modes), each category box (Appointment, Bill, Home or car matter, etc.) showed "Added" once it held any records. Since the box is revisited repeatedly to add more, "Added" wrongly implied the category was now complete/done rather than just non-empty.

`src/screens/FirstThingScreen.tsx`: replaced the "Added"/"Add" text with a small numeric badge showing `savedRecords.length` once a category has any records; an empty category still shows "Add" as before. New `countBadge`/`countBadgeLabel` styles alongside the existing `addAffordance` styling - no other layout change.

Two new tests in `tests/phase9-record-management.test.tsx` cover the badge showing the correct count with no "Added" text present, and the empty-category "Add" affordance still rendering. `npm run typecheck` and `npm test` (153 tests, up from 151) both pass.

## 10 September 2026 - Repeats: horizontal Weekly/Bi-weekly/Monthly/6-monthly/Annually pills

Product-owner request, revised twice in the same sitting: first to a Weekly/Monthly/Yearly toggle with no explicit "Never" (deselecting clears recurrence), then to a horizontal scrolling row of five pills - Weekly, Bi-weekly, Monthly, 6-monthly, Annually - confirmed via `AskUserQuestion` as a scrollable row of pill buttons rather than a draggable-thumb slider, matching the existing horizontal interest-selection carousel's interaction style.

`src/components/RecordEditor.tsx`: the "Repeats" field (bill/home-or-car-matter records only, unchanged) is now a horizontally scrolling `ScrollView` of five pill buttons. Bi-weekly is `{ interval: 2, unit: 'week' }` and 6-monthly is `{ interval: 6, unit: 'month' }` - both already expressible by the existing `RecordRecurrence` type, no type or migration change needed. Selection compares both `interval` and `unit` (previously `unit` alone, which would have conflated Weekly/Bi-weekly and Monthly/6-monthly). Tapping the already-selected pill clears recurrence to `undefined`, so there's no separate "Never" option. `docs/PHASE_9_ARCHITECTURE.md`'s reference to the old toggle wording updated to match.

New `tests/record-recurrence.test.tsx` (4 tests) covers all five options rendering, Bi-weekly/6-monthly storing the correct distinct interval, and deselecting clearing recurrence. `npm run typecheck` and `npm test` (151 tests, up from 147) both pass.

## 10 September 2026 - Remove a duplicated person from the relationship summary

Reported directly by the product owner: after accidentally adding "Dad" twice on the "Who are you helping?" flow (duplicates are allowed by design, e.g. two grandparents), there was no way to remove the extra entry from the collapsed relationship summary screen reached via "+ Add another person" - the only existing remove control lived on the later `PeopleReviewScreen`, which isn't reached until every person already has a name.

Added a `Remove` control per row in `RelationshipScreen`'s collapsed summary, gated behind a new optional `onRemovePerson` prop (no control renders when it isn't supplied, so the `PeopleReviewScreen`'s own fallback usage of this component is unaffected). `App.tsx` wires it to a new `removeDraftPerson(draftId)` handler, mirroring the existing `PeopleReviewScreen` remove logic exactly: filters the person out of the draft, reindexes remaining order, clears `currentDraftId` if it pointed at the removed person, and drops back to the `relationship` wheel stage if the list becomes empty.

Two new tests in `tests/multi-person.test.tsx` cover the control appearing and firing, and staying absent when no handler is supplied. `npm run typecheck` and `npm test` (147 tests, up from 145) both pass.

## 10 September 2026 - Corrective fix: self/someone-else fork not reachable on resume

Physical-device testing reported that onboarding still went straight to the old "Who are you helping?" relationship screen, bypassing the approved "Whose wellbeing are you looking to support with Lilica?" fork, despite the fork's code being present and correct.

Traced the live routing rather than assuming stale build/cache. `App.tsx`'s own render-time normalization (`renderAuthenticatedOnboarding`) and `completeProfile` both correctly route a genuinely fresh pass through onboarding to `careFork` via `initialPersonStage()`. The actual bypass was in `src/storage.ts`'s `prepareOnboardingStateForStartup`, called on every authenticated app startup/resume: it trusted a previously persisted `stage: 'relationship'` verbatim and returned it unchanged, because `'relationship'` was never in the small list of stages that function re-normalizes. Any account whose local storage had already reached the relationship screen - including from testing before the fork stage existed - would resume directly there on the next app open, permanently skipping the fork, even with zero care spaces or draft people.

Fix (smallest possible integration point, one file): `prepareOnboardingStateForStartup` now re-routes a resumed `stage: 'relationship'` back to `'careFork'` whenever no care space or in-progress draft actually exists yet - the same "has onboarding actually started" test `initialPersonStage()` already uses in `App.tsx`, duplicated inline with a comment to keep both in sync. A resume mid-relationship-selection (care space or draft already present) is left untouched, so an in-progress "Someone else" pass or an existing user is never forced back through the fork.

No other file changed. Nothing in Phase 7/8/9 persistence, occurrence, assignment or sync logic, Home, auth, or the database schema was touched. Two new regression tests added to `tests/storage.characterization.test.ts` covering both directions (fresh resume routes to the fork; a resume with an existing care space or draft does not). `npm run typecheck`, `npm test` (13 suites/145 tests, up from 143) and `npm run validate` (web export included) all pass.

The self/someone-else fork and the existing multi-person relationship carousel remain two independent, coexisting entry points into the same onboarding stage machine, as required - this fix only corrects which one a resumed session lands on.

## 10 September 2026 - Phase 9 everyday add and record management

Implemented by Claude per a written product-owner brief (`fork.txt`), the first phase-implementation task done directly by this session rather than committed on Codex's behalf. Codex is offline for approximately a week from this date.

Before writing any code, traced the current repository line by line (not assumption, not prior session memory) and established a `PRE_PHASE_9_BASELINE`: clean working tree at `46c1125` (already in sync with `origin/master`), TypeScript clean, 12 Jest suites/135 tests, 152 pgTAP assertions across 5 files, clean database lint, clean secret scan, clean web export.

The trace found that the brief's central deliverable - a category-gateway → list → add → edit → save workflow, with wheel date/time pickers and keyboard-safe compact editors - was **already fully implemented**, as part of the 10 September 2026 "Structured Category And Wheel-Picker Correction", and was already reachable from Home's everyday `Add` action (`onAddSomething` in `App.tsx` already routed to the `firstThing` stage regardless of setup status). It also found that a Phase 8 "occurrence" is a derived projection computed automatically from existing `LilicaRecord` fields, both locally and via a server-side trigger - there is no separate occurrence-mutation API for the UI to call, so editing through the existing save path was already canonical.

Given that, the actual Phase 9 work was narrower than the brief's full scope, and scoped down deliberately rather than reorganising what already worked:

- `src/screens/FirstThingScreen.tsx` gained an `everyday` prop (`App.tsx` passes `currentSpace?.setupStatus === 'ready'`), changing only the heading (`"[Name]'s records"` instead of `"Let's get [Name] organised."`), supporting text, and empty-category footer button (`"Back to Home"` instead of `"I'll add things later"`). The category-gateway/list/add/edit architecture itself is identical in both modes.
- `src/types.ts` gained one new optional field, `assignedMembershipId?: string`, on `FirstItem`/`LilicaRecord`.
- `src/components/RecordEditor.tsx` gained an `Assigned to: Unassigned / You` segmented control for appointment/task/bill/home-or-car-matter records, rendered only when an `activeMembershipId` prop is supplied (so nothing fabricates a populated care circle where none exists). `You` stores the organiser's own `care_space_membership.id` (threaded from `currentSpace.membershipId`, already loaded locally) - never a display name or email. The existing free-text `responsiblePerson` field ("Who's taking them"/"Who's dealing with it") is completely unchanged and independent of this control; both are saved together.
- No migration was created: `assignedMembershipId` rides inside the existing generic `record_data` JSONB already written by `apply_record_mutation(...)`, so no new schema, RPC, sync surface or RLS test was needed. The separate Phase 8 `assignments` table remains uncalled by any client code - deliberately deferred rather than speculatively wired up with only one possible assignee to exercise it against today.
- New tests: `tests/phase9-record-management.test.tsx` (8 tests) covering the everyday-copy variant, the Unassigned/You control storing a stable ID rather than a name, the legacy text field's independence, and that no Assigned-to control renders for record types that don't support it or when no membership is available.

Not changed: `bootstrap_supported_people`, `apply_record_mutation`, `apply_occurrence_mutation`, `PersonSwitcher.tsx`, `Wordmark.tsx`, `Screen.tsx`, `HomeScreen.tsx`, `records.ts`, `CareForkScreen.tsx`, any theme token, any migration, any RLS policy. `git diff --stat` after implementation: 4 files changed, 63 insertions, 4 deletions, plus 1 new test file - no unexpected files.

`PRE_PHASE_9_BASELINE` vs `POST_PHASE_9`: TypeScript clean both; Jest 12 suites/135 tests -> 13 suites/143 tests (net +1 suite/+8 tests, nothing disappeared); pgTAP 152 assertions unchanged (no migration); database lint clean both; secret scan clean both (127 -> 128 files); web export clean both.

Database contract preservation, individually verified: Phase 7 record IDs unchanged; `care_space_id` immutability untouched; Phase 8 occurrence IDs/derivation unchanged; recurrence history untouched; past-appointment `past_awaiting_outcome` semantics untouched (no code path touches appointment-passed logic); legacy responsibility text still never identity-linked; assignment still never grants visibility (no RLS policy was touched, and `assignedMembershipId` carries no access grant); no new cross-space access path exists (the field is scoped exactly like every other record field, per-care-space, via the same RLS `apply_record_mutation` boundary); no existing RLS test was weakened or removed (152 assertions, same 5 files); no already-deployed migration was rewritten.

Physical-device QA required before approval: see `docs/PHASE_9_QA.md`. Not committed or pushed pending that review, per the implementation brief.

## 10 September 2026 - Phase 8 physical-device QA passed

The product owner completed and confirmed the `docs/PHASE_8_QA.md` checklist on physical Android and iPhone devices. Phase 8 (committed as `019b88b`) is now physically approved, alongside Phases 5-7. Codex is offline for approximately one week; this session committed and pushed Codex's completed Phase 8 work on explicit product-owner instruction, and is now the sole active agent on this repository until Codex returns. Phase 9 still requires its own separate, explicit, bounded implementation prompt before any work begins - the passed QA checklist is not that authorisation.

## 10 September 2026 - Phase 8 core Record and Occurrence engine

- Added stable, care-space-owned canonical occurrences beneath existing Phase 7 records, with explicit date-only, local date/time and instant shapes.
- Added immutable occurrence snapshots, stable recurrence series, versioned recurrence rules and idempotent server mutation receipts. Explicit recurring completion creates exactly one deterministic next occurrence.
- Mapped the existing record editor model behind the current UI. Missing or malformed dates remain unknown; no historical occurrence is fabricated.
- Extended the existing account/care-space record cache and sync pass with occurrence projections and pull cursors. No second outbox or sync engine was introduced.
- Added server-only stable assignment and external-contact foundations. Membership UUID is identity; display name is a snapshot; legacy responsibility text never auto-links; assignments never participate in access policies.
- Preserved Claude's approved Home redesign and self/someone-else fork without UI changes. Calendar, To Do, assignment controls, collaboration, invitations and notifications remain deferred.
- Added focused domain/cache tests and 57 Phase 8 pgTAP assertions. `npm run validate:all` passes 12 Jest suites/135 tests and 152 database assertions with clean lint. A one-migration linked preview was applied to `lilica-development`; linked tests/lint pass and migration histories match. Phase 8 physical-device QA remains outstanding; work is intentionally uncommitted pending review.

## 10 September 2026 - Self/someone-else onboarding fork

Bounded implementation authorised via a written product-owner brief (`fork.txt`) after a full line-by-line trace of Codex's Phase 6/7 implementation confirmed no self-care semantic existed anywhere in the app: the `Relationship` type, the relationship-wheel copy, and the `care_space_memberships_relationship_type` database CHECK constraint were all a closed set of third-person relationships only (`Mum, Dad, Partner, Child, Grandparent, Other relative, Someone else`) with no way to represent the organiser's own care space. Confirmed as the situation the brief calls "D" (a genuine architectural gap, not a hidden or unexposed existing semantic) before writing any code.

The smallest addition that integrates with the existing architecture rather than replacing it:

- Added `'Myself'` as a new value to the existing `Relationship` type (`src/types.ts`) and the `relationships` option list (`src/data/options.ts`) - not a new type, not a parallel identity system.
- New screen `src/screens/CareForkScreen.tsx`: `Whose wellbeing are you looking to support with Lilica?`, two `ChoiceTile`s (an existing, previously-unused component matching the app's visual language exactly), Myself / Someone else.
- New onboarding stage `'careFork'`, inserted into `App.tsx`'s `stageOrder` between `aboutYou` and `relationship`. It only ever governs the very first pass through person setup (`initialPersonStage()` checks whether any care space or in-progress draft already exists); `startAddPerson()`, the existing "add another person" path, is untouched and always goes straight to the relationship wheel.
- Choosing **Myself** creates a person draft with `relationshipType: 'Myself'` and `displayName` prefilled from the organiser's own `About you` profile name (never re-asked), then feeds it into the exact same `relationshipSummary` → identity-pass → `peopleReview` → `bootstrap_supported_people` pipeline any other relationship choice already uses - so back navigation, edit, add-another and provisioning retry/idempotency are all the existing, already-tested behaviour, not new code paths.
- Choosing **Someone else** continues into the existing relationship wheel unchanged. `Myself` also appears there as a normal option (for adding yourself after starting with someone else), but is hidden once it is already represented - in the current draft or an already-provisioned care space (`RelationshipScreen`'s new optional `selfAlreadyUsed` prop) - so it can never be picked twice.
- `NameScreen.tsx`: fixed the title shown when editing/confirming a Myself draft (`Just to confirm, what should we call you?`) - the existing generic title would otherwise have read "What's your myself's name?".
- New migration `supabase/migrations/20260910160000_myself_relationship_type.sql`: extends the existing `care_space_memberships_relationship_type` CHECK constraint to accept `'Myself'`, and adds a partial unique index (`care_space_memberships_one_self_per_user`) so at most one `Myself` membership can exist per user - enforced at the database, not only client UI. No new table, no change to `bootstrap_supported_people`'s logic (it already accepted any string satisfying the constraint). Applied to `lilica-development` via `supabase db push --linked --dry-run` then `--linked` push; the dry-run listed exactly this one migration.
- New pgTAP file `supabase/tests/database/myself_relationship.test.sql` (6 assertions): Myself provisions through the existing RPC, the stored value is exactly `'Myself'`, a second Myself for the same user is rejected (`23505`) with no partial care space left behind, and the one-per-user rule is per-user rather than global.
- New Jest file `tests/care-fork.test.tsx` (6 tests) covering `CareForkScreen`'s two routes, `validatePersonDraft` accepting a Myself draft without a custom label, the wheel offering and then hiding Myself, and `NameScreen`'s corrected copy.

Not changed: `bootstrap_supported_people`'s PL/pgSQL body, `PersonSwitcher.tsx`, `careSpaceState.ts`'s provisioning/reconciliation functions, Phase 7 record/cache/sync/RLS, and Home (verified Home's existing copy - `Everything for [Name], in one place.` - already reads naturally for a Myself care space using the organiser's own name; no correction was needed there).

Verification: `npm run validate` (TypeScript, 12 Jest suites/129 tests, secret scan, dependency/config checks, web export) and `npm run validate:backend` (local database rebuild, 95 pgTAP assertions, schema lint) both pass; the same 95 assertions and a clean lint were re-run against the linked `lilica-development` database after the migration was applied.

Follow-up, same day, product-owner approved: `InterestsScreen` gained an optional `isSelf` prop (`App.tsx` passes `currentSpace?.relationshipType === 'Myself'`, the same existing semantic, no new concept) so `What do you help [Name] with?` becomes `What would you like help staying on top of?` for a Myself care space. `FirstThingScreen`'s `Let's get [Name] organised.`/`Go to [Name]'s Home` were checked and left as-is - they already read naturally using the organiser's own name. The privacy declaration's wording was deliberately left unchanged: it is protected, versioned legal copy (`privacy-basis-v2`), its existing generic phrasing ("the person you support") already parses correctly for a Myself space, and editing it would need a version bump and legal/product sign-off outside this task. Added `tests/care-fork.test.tsx` coverage for both the self and non-self interests copy. Full `npm run validate` re-run: 12 suites/130 tests, all pass.

## 10 September 2026 - Home presentation redesign

A browser mockup was reviewed and approved by the product owner, then authorised by GPT (development oversight) with an explicit architectural boundary: no new domain/aggregation semantics in `src/records.ts`, no permanent "To Do" record-category rule, and everything contained to `src/screens/HomeScreen.tsx`.

Changes, all inside `HomeScreen.tsx`:

- Header now leads with the `Wordmark` component (reused, not recreated) as the dominant element.
- The old dismissible "Everything for [Name], in one place" banner and the small "[Name]'s week ⌄" switcher trigger are both replaced by one persistent, tappable card (no Dismiss action) that opens the existing `PersonSwitcher` unchanged. It's shown regardless of setup status, preserving the same always-reachable switcher access the old header trigger had.
- New horizontal-scroll snapshot row beneath it: To do / Bills / Home matters / Appointments, each a truthful count of not-completed records of that type, computed locally with the existing `deriveRecordState()` - no new selector was added to `records.ts`. Hidden entirely when there are no records, per the existing documented rule to omit empty sections rather than show zero counts. The caption is the single word "open" everywhere, since that is the only word truthful for every category under the current record model; "To do" is a display label for today's `task` type only, not a claim about the future Phase 8 actionable-occurrence projection.
- Section item cards (Needs attention/Today/Coming up/Latest - `sectionFor()`'s own four-way classification is unchanged) moved from a single-column list with one uniform accent bar to a two-column grid, each card carrying a small category-tonal icon and an explicit "Overdue" label shown only when the existing `deriveRecordState(item).overdue` is true.
- Category icons for all eight record types are drawn from plain `View`/border shapes (the same technique already used by `Wordmark`'s leaf mark and `PersonSwitcher`'s tick) - no icon-library dependency was added.
- Category eyebrow/snapshot labels are looked up from the existing `firstItemOptions` list in `src/data/options.ts`, not a separate invented label set.

Not changed: `sectionFor()` classification, `PersonSwitcher.tsx`, `Wordmark.tsx`, `records.ts`, `Screen.tsx`, `TabBar.tsx`, `colors.canvas` or any other theme token, dependencies, and no Phase 7/8 code.

Verification: `npm run validate` - TypeScript, 11 Jest suites/123 tests (including the existing characterization tests asserting Home's empty-state and Needs-attention classification, unmodified and passing), secret scan, dependency/config checks and web export all pass.

Physical-device QA still required: long person names and long record titles in the new cards, and the horizontal snapshot row on a narrow phone.

## 10 September 2026 - Phase 7 record persistence, cache, sync and migration

- Added care-space-owned Supabase records, generated domain/sensitivity classification, active-membership RLS, protected audit/version fields, tombstone deletion and idempotent mutation receipts.
- Added an account/care-space-scoped AsyncStorage record cache with stable identity mappings, migration checkpoints, semantic outbox, capped retry, durable conflict/rejection state and change cursors.
- Preserved valid UUIDs and deterministically mapped historical non-UUID IDs; migrated responsibility as descriptive text/provenance only with no identity inference.
- Added explicit-base reconciliation: non-overlapping stale fields merge, overlapping edits conflict, and stale deletes cannot erase newer work.
- Kept attachment bytes and URIs local while persisting non-URI metadata; no Storage bucket, OCR or sharing was added.
- Added fresh-device care-space discovery without inferring local privacy/setup completion, plus focused application and pgTAP coverage.
- Added `docs/PHASE_7_ARCHITECTURE.md` and `docs/PHASE_7_QA.md`. The product owner subsequently completed and approved the Phase 7 physical-device checklist on Android and iOS. Phase 8 was not started.
- `npm run validate:all` passed TypeScript, 11 Jest suites/123 tests, secret scanning, Expo dependency/config checks, web export, a clean local database rebuild, 89 pgTAP assertions and database lint.
- Sync ordering now prevents later operations from overtaking an unresolved mutation for the same record; unrelated remote records continue reconciling past a conflict without advancing the durable cursor beyond that conflict.
- A linked dry run listed only `20260910150000_phase7_records.sql`; that migration was applied to `lilica-development`. All 89 linked pgTAP assertions and linked database lint then passed, and local/remote migration histories matched.
- No production project, Auth configuration, Storage resource, seed or role configuration was changed. Phase 7 is approved for its completed repository checkpoint.
- The passing responsive Jest suite emitted an asynchronous React `VirtualizedList`/`act(...)` warning. It is recorded as an out-of-scope test-harness issue and was not altered in Phase 7.

## 10 September 2026 - Assignment and collaboration roadmap locked

- Made stable assignment identity and independently server-enforced visibility an authoritative product invariant.
- Assigned legacy responsibility migration to Phase 7, assignment entities/defaults/occurrence overrides to Phase 8, editor controls to Phase 9, To Do projections to Phase 12, and real member population/permissions/revocation to Phase 15.
- Locked no name/email auto-matching, no implicit permission from assignment, active-membership-only assignment, separate zero-access external contacts, non-mandatory acceptance at initial launch, and historical attribution after revocation.
- Added role/capability/domain boundaries and a roadmap-level acceptance matrix covering identity changes, cross-space isolation, legacy text, permissions, pending invitations, helper restrictions, revocation and stale offline replay.
- No application code, schema or Supabase resource was changed for this architecture decision. The present free-text field remains until its approved replacement phase.
- Product-owner physical-device testing also confirmed the corrected keyboard behavior and password, verification-code, check-email and password-reset paths before the Phase 6 checkpoint.
- The checkpoint passed `npm run validate:all`: 10 Jest suites/110 tests, secret scanning, Expo dependency/config checks, web export, local database reset, 49 pgTAP assertions and database lint.

## 10 September 2026 - Home and car category expansion

- Expanded the user-facing `Home matter` category to `Home or car matter` across Welcome, interests, structured record entry and record-management actions.
- Renamed the interest to `Home, car & bills` and clarified that it includes repairs, servicing and renewals.
- Added MOT, vehicle service and maintenance examples while retaining the distinction between matters and bills/renewals.
- Preserved the internal `homeMatter` identifier so existing stored records and migrations remain compatible.
- Updated exact-copy regression coverage and current product/handoff documentation.

## 10 September 2026 - Keyboard-avoidance corrective task: Password and CTA trapped behind the keyboard

Physical-device testing after the prior keyboard correction still showed the Password field (and, once that was fixed, the Create Account/Log in CTA) clipped or hidden behind the open keyboard on Android, and initially on iOS too. Root-caused and fixed in three passes rather than patched with fixed offsets:

- **Reveal math replaced.** `KeyboardAwareScrollView`'s legacy `scrollResponderScrollNativeHandleToKeyboard` compared a field's position (relative to the ScrollView) against the keyboard's reported screen position (relative to the original, pre-resize window) - two different coordinate spaces that disagree once Android's `softwareKeyboardLayoutMode: resize` shrinks the window, so the computed offset collapsed to zero and nothing scrolled. Replaced with two chained `UIManager.measureInWindow` calls (container, then field) in the same coordinate space. The actual overflow arithmetic was extracted into a pure, directly-testable function, `src/keyboardReveal.ts`.
- **Async race fixed.** The first replacement still failed identically on iOS and Android: the container measurement was written by its own independently-timed native call, and `reveal()` only waited one `requestAnimationFrame` before reading it - not the same as waiting for that native round-trip to land - so it kept reading a stale, pre-keyboard (full-height) container size. Fixed by chaining the native callbacks together instead of guessing timing, plus a corrective re-measure shortly after focus for a still-animating keyboard.
- **Footer/CTA co-visibility added.** The product owner clarified the requirement: field and CTA should both remain visible together as the keyboard opens, standard mobile behaviour, not merely reachable by a further manual scroll. `KeyboardAwareScrollView` now accepts an optional ref to trailing content (the screen's footer), and `Screen` wires its own footer through automatically. The reveal target now spans from the focused field down through the footer's bottom, with the field's own top as a hard ceiling so it is never scrolled off-screen chasing a CTA that cannot fit alongside it on an extremely short viewport.
- **Android's real root cause found.** After the above, iOS worked correctly but Android's screen stayed completely static as the keyboard opened - no resize at all, confirmed by direct visual observation on-device. `app.json`'s `android.softwareKeyboardLayoutMode: resize` is native `AndroidManifest.xml` configuration; Expo Go is a pre-built generic host app with its own fixed manifest and never applies a loaded project's native Android config - only a custom dev client or a standalone/production build does. `src/keyboard.ts` now detects Expo Go at runtime via `expo-constants`'s `executionEnvironment` and gives Android the same JS-driven `KeyboardAvoidingView` behaviour (`'height'`) iOS already had, only inside Expo Go; a real dev-client/standalone build keeps Android's behaviour `undefined` so the native `resize` config is not double-compensated once it actually applies.

Files changed: `src/keyboard.ts`, `src/components/KeyboardAwareScrollView.tsx`, `src/components/Screen.tsx`, `src/keyboardReveal.ts` (new), `package.json`/`package-lock.json` (added `expo-constants` as a direct dependency), `tests/keyboard-reveal.test.ts` (new), `tests/keyboard.test.ts` (new). No other screen, dependency, backend/Supabase resource or Phase 7 work was touched.

Verification: `npm run validate` passed - TypeScript, 10 Jest suites/110 tests (108 prior plus 6 new keyboard-reveal cases and 3 new Expo-Go-detection cases), secret scan, `npx expo install --check`, `npx expo config --type public` and web export. Confirmed by the product owner on physical Android and iOS devices in fresh Expo Go sessions: Password (and any other field) and the footer CTA both become visible together as the keyboard opens, and Android now visibly resizes/pads instead of staying static.

## 10 September 2026 - Recovery OTP and cross-platform keyboard correction

- Added the approved password-recovery sequence: `Check your email`, six-cell code entry and new-password form.
- Added Supabase `recovery` OTP verification and retained legacy callback compatibility.
- Added and narrowly pushed the development recovery email OTP template without changing 19 undeclared hosted settings.
- Fixed the shared keyboard root cause: form content and CTA now participate in one scroll region rather than a fixed footer compressing/covering fields.
- Added focus-aware input revealing on focus and after `keyboardDidShow` for shared screens and record-sheet editors.
- Kept Android `softwareKeyboardLayoutMode: resize` without double height avoidance; retained iOS padding behavior and native safe areas.
- Forwarded text-input refs so Email Next moves directly to Password while the keyboard remains open.
- Confirmed the fix covers Login, Create account, About You, name/custom relationship, signup/recovery code, recovery request/new password, item forms and record editing.
- Validation passed: TypeScript, 8 Jest suites/98 tests, focused auth/responsive tests, Expo dependency/config checks and `git diff --check`.
- Physical Android/iOS acceptance remains the immediate next step; automated tests do not claim device proof.

## 9 September 2026 - Phase 5 authentication and organiser profile

- Replaced placeholder account entry with Supabase email/password signup, mandatory verification, login and recovery.
- Added persisted React Native sessions, foreground refresh, native auth callbacks and safe unauthenticated loading gates.
- Added a separate organiser `About you` profile backed by owner-only `public.profiles` RLS.
- Added sign-out without deleting or uploading existing device-local supported-person data.
- Deferred profile photos because private cloud storage is outside Phase 5.
- Added focused Phase 5 UI/provider tests and a physical-device QA checklist.
- Follow-up device QA moved Phase 5 forms to top-led responsive composition, added Android keyboard resize/avoidance, and replaced signup links with a six-digit verification-code screen and Lilica email template.
- Activated verified-domain Resend SMTP for development auth email and reset the product owner's test account for fresh signup QA.

## 2026-09-09: Phase 4 Supabase Environment And Security Foundation

Reason:

Create controlled, isolated backend infrastructure and prove the first private account/profile boundary before real authentication or user-data migration.

Changes:

- Confirmed the Luxford Interactive organisation contained no Lilica project and did not reuse the existing Goalbuddy, Tandemly, or Waddl infrastructure.
- Created and linked the dedicated non-production `lilica-development` project in West Europe (London) on `micro` compute; no Lilica production project was created.
- Added a pinned Supabase CLI, local Docker workflow, version-controlled migration and empty synthetic seed.
- Added `public.profiles`, keyed one-to-one to `auth.users`, with required display name, optional private avatar path, server timestamps and database constraints.
- Enabled and forced RLS; removed anonymous access; allowed authenticated owners to select, insert and update only their own profile; deliberately omitted delete access.
- Added 17 transactional pgTAP assertions covering owner success and anonymous/non-owner/ownership-change/delete denial.
- Added placeholder-only public client configuration, environment ignores, repository credential scanning and a credential-free CI database validation job.
- Added `docs/SUPABASE_OPERATIONS.md` with environment, migration, access, secrets, reset, backup and recovery boundaries.

Scope guard:

- No Supabase runtime client, real authentication UI, organiser-profile UI, supported people, care spaces, records, documents, sync or production infrastructure was added.
- Existing AsyncStorage data and attachment files remain untouched and authoritative for the running application.

Verification:

- A clean local database reset recreated the schema from migrations.
- All 17 database/RLS tests passed against both local Postgres and hosted `lilica-development`; hosted synthetic data rolled back.
- Local and hosted schema lint passed, and local/remote migration histories match.
- `npm run secrets:check` found no credential pattern in tracked or unignored repository files.

## 2026-09-09: Phase 3 Android Responsive QA Correction

Reason:

Physical Android screenshots showed excessive dead space, dense content, bottom navigation collisions and a First Thing stack centred too far below its introduction.

Changes:

- Replaced the shared legacy safe-area boundary with `react-native-safe-area-context` for Android/iOS top and bottom insets.
- Balanced Email, Relationship and Interests through elastic content regions while preserving scroll fallback.
- Let the existing Interests carousel use the full screen gutters.
- Positioned the First Thing stack below its introduction and derived trailing snap space from measured height.
- Added six responsive composition tests.

Verification:

- The product owner checked the live Expo Go correction with Android and iPhone connected and confirmed it was much better.
- `npm run validate` passed TypeScript, 62 Jest tests, Expo dependency/configuration checks and web export.

## 2026-09-09: Phase 3 Safety Harness And Executable Domain Contract

Reason:

Protect the completed Phase 1 experience and make approved future record rules executable before authentication, backend, persistence or record-architecture work begins.

Changes:

- Added Expo-compatible Jest/`jest-expo`, Jest types and React Native Testing Library as development-only tooling.
- Added characterization tests for current date logic, AsyncStorage migration, protected onboarding surfaces, structured record entry, retained drafts, document entry points and real-record-only Home.
- Added six historical JSON migration fixtures covering `records[]`, legacy `firstItem`, completion, recurrence, local attachments, missing optional fields and partially malformed legacy state.
- Added an unwired, framework-independent `src/domain` module and executable examples for approved lifecycle, occurrence, recurrence, link, responsibility, confirmation, activity, projection, timezone and semantic-conflict rules.
- Added `npm test`, `npm run test:watch` and the complete `npm run validate` gate.
- Added a minimal GitHub Actions validation workflow without deployment automation or credentials.
- Added `docs/PHASE_3_QA_BASELINE.md` with the protected baseline, known limitations and repeatable physical-device checklist.

Scope guard:

- No business/domain behaviour, navigation sequence, `src/records.ts`, `src/storage.ts`, authentication or backend was changed by the original safety-harness work.
- Known Home/date and recurrence limitations are characterized, not fixed.
- The new future domain module has no runtime imports and does not migrate data.

Verification:

- `npm run validate` passed after physical-device correction: TypeScript, 62 Jest tests, Expo dependency compatibility, public Expo configuration and web export.
- Jest reports the existing React Native `SafeAreaView` deprecation warning from the protected Welcome screen; Phase 3 does not refactor it.
- Android baseline screenshots and product-owner Android/iPhone recheck are recorded in `docs/PHASE_3_QA_BASELINE.md`; the remainder of the physical checklist stays manual.

## 2026-09-09: Structured Record Onboarding And Document Capture

Reason:

The post-interest `What would help today?` screen repeated a question the user had already answered and did not collect enough structured information to power Lilica's Home experience. Its initial inline forms were also disproportionately large and clipped awkwardly around the keyboard.

Changes:

- Replaced the old category-choice screen with `Let's get [Name] organised`.
- Added an interest-prioritised vertical snapping stack for Appointment, Something to do, Bill or renewal, Home matter, Important document, Contact, Care information and Update.
- Added reusable structured records with event dates, due dates, expiry dates, status, responsibility, recurrence, completion, confirmations and audit timestamps.
- Added deterministic date parsing and derived due-today, overdue, upcoming, unresolved, completed, recently-updated and next-recurring-date state.
- Migrated legacy `firstItem` storage into a records array without creating fake content.
- Updated Home to group real records into populated Needs attention, Today, Coming up and Latest sections.
- Moved record forms from inline expanded cards into a compact animated bottom sheet.
- Added dismissal through Done, backdrop, system back and downward swipe from anywhere on the sheet while content is at the top.
- Kept unsaved draft values in the parent screen when the sheet is temporarily dismissed.
- Added file upload and rear-camera capture to Important document.
- Added local durable attachment copies and attachment metadata using Expo Document Picker, Image Picker and File System.
- Added native permission configuration for document and camera access.

Known limits:

- Unsaved drafts are retained only while the record-onboarding screen remains mounted.
- Camera capture does not provide document edge detection, PDF assembly or OCR.
- Attachments are local and do not synchronise without future backend storage.
- Authentication remains a local placeholder and no organiser profile/care-circle membership model exists yet.
- Physical-device gesture, keyboard and camera QA remains manual because browser automation was unavailable.

Verification:

- `npm run typecheck` passed.
- `npx expo config --type public` passed.
- `npx expo export --platform web` passed.
- The local preview returned HTTP 200 at `http://localhost:8084`.

## 2026-09-09: Interest Selection Carousel

Changes:

- Replaced the tall interest option list with a compact horizontal chip carousel.
- Added touch scrolling and accessible previous/next arrow controls.
- Added clear selected styling and a live selected-count message.
- Preserved multi-selection, skipping and later interest-based record ordering.

## 2026-09-09: Premium Onboarding Redesign

Reason:

The previous onboarding was visually crowded and presented too much copy at once. The new direction uses Yuka as a benchmark for calm pacing, strong colour, restrained typography and one clear idea at a time.

Changes:

- Replaced the separate Welcome and How Lilica Works presentation with one three-page horizontal swipe journey.
- Added a small three-position indicator used only within the intro and a tappable swipe cue for people who do not swipe.
- Reduced intro copy to short, natural sentences and moved Get started to the final page.
- Refocused the final intro slide from the repetitive `Know what needs doing` message to controlled collaboration: bringing in family or helpers, sharing updates and responsibilities, clear ownership, optional later invitations and explicit solo-user value.
- Replaced the final slide's generic progress-path visual with two helpers connected through a shared, confirmed responsibility.
- Refined the Lilica wordmark and created a distinct visual composition for each intro page.
- Reduced heading and body sizes across the shared type system.
- Restyled buttons, option rows, headers and screen spacing for a quieter, more consistent flow.
- Simplified the account, email, relationship, name, interests and first-item screens.
- Removed `Save your place and come back anytime.` from Create your account and grouped the heading with its three authentication buttons and Log in action using responsive in-content spacing rather than a detached fixed footer.
- Reworked privacy information into three numbered, plain-English points covering permission, involvement and UK GDPR-aligned privacy; simplified the declaration and advanced its stored version to `privacy-basis-v2` while preserving the active checkbox gate and acceptance time.
- Removed technical and development wording from user-facing first-item and foundation screens.
- Removed the development reset action from Home.
- Preserved onboarding resume, back navigation, skip paths, saved selections and the first real item on Home.

Verification:

- `npm run typecheck` passed.
- `npx expo install --check` reported that dependencies are up to date.
- `npx expo export --platform web` passed and exported `dist`.
- Static flow checks confirmed the three-page deck, paging, tap fallback, old `how` stage recovery, privacy gate/version/time, onboarding resume and first-item preservation.
- Metro started successfully at `http://localhost:8083`.

Rendered QA limitation:

The managed browser was available, but its navigation policy blocked both `localhost` and `127.0.0.1`, so an automated rendered screenshot could not be captured. The app remains runnable for manual mobile-size and Expo Go inspection.

## 2026-09-08: Controlled Phase 1 Visual And Flow Pass

Reason:

The previous rendered app did not meet the requested Yuka-level visual benchmark. The Welcome headline was too large, How Lilica Works read like a text page, the email auth flow lacked a focused CTA step, and Home used too much paragraph copy.

Changes:

- Added a recoverable `emailAuth` onboarding stage.
- Added `src/screens/EmailAuthScreen.tsx`.
- Changed `AuthScreen` so it is only the account-method choice screen.
- Changed Continue with email to lead to `What's your email?`.
- Reduced the Welcome hero image and headline scale.
- Kept the approved Welcome headline and supporting copy intact.
- Changed How Lilica Works into a numbered explanation screen.
- Adjusted progress dots from 8 to 9 onboarding moments.
- Centred and simplified the main onboarding question screens.
- Revised Home so it leads with a compact supported-person header and visible real item content rather than large product paragraphs.
- Preserved the first item when users return to add something and then skip.

Verification:

- `npm run typecheck` passed.
- `npx expo export --platform web` passed.
- Expo web server was started on `http://localhost:8082`.
- Expo Go URL shown by Metro: `exp://192.168.1.178:8082`.

Known limitation:

Browser automation was unavailable, so no automated rendered screenshot could be captured from this environment. The user should inspect the revised screens manually in browser or Expo Go.

Outcome:

The user manually inspected the revised app and rejected the visual result. The UI is still too cluttered, still does not follow the Yuka benchmark closely enough, and still does not present text with a professional premium feel.

Additional feedback:

- Welcome should follow the Yuka structure more exactly.
- The supporting paragraph beginning "Keep appointments, care..." should not live on Welcome.
- The progress dots/slider at the bottom of onboarding screens should be removed.
- Home should not show paragraph-led explanation.
- A more capable visual design pass is required.

## 2026-09-08: Documentation Handoff Update

Reason:

The user asked for all project docs to be updated with a full debrief, project context, failure analysis, reference material and a prompt for Lumen, the incoming AI OpenClaw agent.

Changes:

- Updated `README.md` to state that the current visual design is rejected.
- Updated `docs/PROJECT_BRIEF.md` with a current design status section.
- Updated `docs/LUMEN_HANDOFF.md` with the latest rejection, failure analysis and stronger Lumen instructions.
- Updated this revision log with the outcome of the failed visual pass.

Status:

No further visual implementation should be attempted from the existing composition without first rethinking the screen structure against the Yuka references.

## 2026-09-09: Android Layout Consistency Pass

Reason:

Physical-device QA found several remaining composition inconsistencies: active onboarding content sat too low on tall Android screens, `Skip for now` wrapped near the system-navigation area, and development overlay chrome appeared over Home's compact `Add` action.

Changes:

- Standardised active non-Welcome authentication and onboarding content on a top-led responsive layout.
- Applied top and bottom safe-area handling at the shared app shell.
- Kept shared full-width command labels to one line with bounded font fitting for larger system text.
- Preserved intrinsic width for intentional compact Home and record-editor actions.
- Added responsive regression coverage for the top-led screens and footer command labels.
- Documented that the grey Android cog is Expo Go/device development UI, not part of Lilica.

Scope:

No wording, onboarding order, state, domain behaviour, carousel behaviour, record behaviour, or approved Welcome design was changed. Phase 6 was not started.

## 2026-09-09: Authenticated Local-State Isolation

Physical QA found that a newly created account inherited the device-wide completed onboarding state and opened directly onto Maggie. The cause was the Phase 5 auth gate continuing to use Phase 1's single unscoped AsyncStorage key.

Authenticated onboarding and record state is now stored under a Supabase-user-specific local key. Session transitions block rendering and saving until the correct account namespace has loaded. Existing legacy device data is retained without being assigned silently to a new account. Regression coverage proves that a new user does not adopt Maggie from the legacy key. No cloud record sync or Phase 6 work was introduced.

After the development test user was deleted, Expo Go retained its cached token and reopened at `About you`. Startup now verifies restored identities with Supabase and clears a locally cached session only when the server rejects that identity as unauthorised or deleted. This restores Welcome for a clean signup retry without treating an ordinary network outage as account deletion.

## 2026-09-10: Structured Category And Wheel-Picker Correction

The structured category UI implicitly selected the first record of a type, despite `records[]` supporting several stable IDs. Categories are now clean gateways. Populated categories open a reusable compact record list; empty categories retain the fast direct-editor path. Add, edit and confirmed remove operations append, update or delete only the intended record ID, then return to the category list. Top-level cards contain no counts or `Add another` management controls.

Free-text structured date/time fields were replaced with reusable Lilica wheel selectors. Dedicated bottom sheets dim the unchanged form and keep coherent Cancel, title and Done controls with centred Day/Month/Year or Hour/Minute wheels. Date logic handles month lengths and leap years, displays UK format, and writes only on confirmation. Time supports every minute from `00:00` through `23:59`. Existing records and IDs require no migration. Phase 6 was not started.

## 2026-09-10: Phase 6A And Phase 6 Multi-Person Kernel

The supported-person journey now creates a reviewed roster rather than one global relationship/name. Every draft has a stable UUID; duplicate relationships remain distinct. Identity is collected for everyone first, then one person is chosen for full setup. Other people remain visible with independent incomplete setup state.

Supabase now contains one care space and one supported person per roster entry, plus a membership that stores the organiser-relative relationship. The authenticated bootstrap RPC creates the roster transactionally and reuses stable draft IDs on retry. RLS grants reads only through membership and denies anonymous, cross-user and direct membership writes.

AsyncStorage migration version 2 partitions privacy, interests, records, attachments and setup state by care space. Existing single-person data migrates deterministically without changing record or attachment IDs. Home projects only the active space and exposes a minimal switcher/add-person route. Records remain local; Phase 7 sync was not started.
