# Lilica Context

Follow `AGENTS.md` as the working contract. Read `docs/PROJECT_BRIEF.md`, `docs/LUMEN_HANDOFF.md` and `docs/REVISION_LOG.md` before changing the product.

The current Welcome work is intentionally retained. Phase 5 authentication, the Phase 6 multi-person care-space ownership kernel, the Phase 7 cloud-record/cache/outbox foundation and Phase 8's canonical Record -> Occurrence, recurrence/history and server-side assignment-identity foundation are all implemented and physically approved. Phase 9 (everyday add/record management, and a stable-identity Assigned-to control), Calendar (canonical roadmap Phase 11 — committed under the working label "Phase 10"; see `docs/PHASE_10_ARCHITECTURE.md`), Phase 12 (To Do: actionable-work projection grouped into Overdue/Today/Upcoming with Mine/Unassigned/All assignment filters, sharing the same completion transition the record editor itself uses), Phase 13 (Person: a durable-knowledge projection of contacts/care/home/documents/bills, with no medical or legal inference), Phase 14 (local-only reminders/notifications, strictly separate from record truth; server/push delivery deferred) and Phase 15 (real care-circle invitations to Contributor/Viewer roles, explicit per-domain permission grants, server-enforced assignment-visibility, immediate-effect removal, sole-organiser safety; migration deployed to `lilica-development`) are implemented and validated, awaiting physical QA (Phase 14 needs a development build, not Expo Go; Phase 15 needs two genuine Supabase accounts, not one pretending to be two — see `docs/PHASE_15_QA.md`). Read `docs/PHASE_8_ARCHITECTURE.md`, `docs/PHASE_9_ARCHITECTURE.md`, `docs/PHASE_10_ARCHITECTURE.md`, `docs/PHASE_12_ARCHITECTURE.md`, `docs/PHASE_13_ARCHITECTURE.md`, `docs/PHASE_14_ARCHITECTURE.md`, `docs/PHASE_15_ARCHITECTURE.md` and the canonical current state in `docs/LUMEN_HANDOFF.md`; do not represent push/email invitation delivery, cloud attachment bytes, push delivery or production infrastructure as complete.

Phase 15's Hard Start Gate (product-owner confirmation of Phase 14 physical QA) was explicitly waived by the product owner on 11 September 2026 before Phase 15 began — do not reopen that question. Do not begin Phase 16+ without its own separate, explicit, bounded implementation prompt.

Preserve the shared safe-area and keyboard-aware form architecture. Read `docs/SUPABASE_OPERATIONS.md` before any hosted operation, use only `lilica-development`, and never run a blind full Auth config push.
