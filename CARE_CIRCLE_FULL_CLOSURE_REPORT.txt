================================================================
LILICA - CARE CIRCLE SECURITY + INVITATION DELIVERY - FULL REPORT
================================================================
Report date: 16 September 2026


################################################################
PERMISSION CLOSURE
################################################################

1. FINAL EIGHT-TYPE MAPPING
   general:    appointment, task, contact, update
   health:     careNote
   financial:  bill
   home:       homeMatter
   documents:  document
   Exhaustive - the records_type CHECK constraint permits no others.

2. EVERYDAY-COPY CORRECTION
   Before: "...plus anything else not covered by the categories below."
   After:  "Appointments, tasks, contacts and everyday updates."
   (src/careCircle.ts, DOMAIN_DESCRIPTIONS.general)

3. FAIL-CLOSED FUTURE-TYPE CHANGE
   record_domain_for_type() rewritten to explicitly branch on all eight
   real types; any other value raises (errcode 22023) instead of
   defaulting to 'general'. A third, even earlier defence was found
   while testing: apply_record_mutation()'s own whitelist check rejects
   an unmapped type before this function or the CHECK constraint are
   ever reached.

4. ASSIGNMENT-POLICY DEFECT BEFORE
   assignments' SELECT policy checked blanket 'general' access,
   ignoring the specific record/occurrence's own domain.

5. ASSIGNMENT-POLICY FIX
   Now resolves the actual owning record's domain (via record_id
   directly, or occurrence_id -> occurrences.record_id).

6. ASSIGNMENT pgTAP RESULT
   9/9 new assertions pass (local and linked): general-only access
   denied for financial/health/home/documents assignments, granted once
   the matching domain is added, organiser and mutation authority
   unaffected.


################################################################
INVITATION DELIVERY
################################################################

7. PREVIOUS INVITATION ARCHITECTURE
   care_space_invitations already had: a server-owned gen_random_uuid()
   id, status enum (pending/accepted/declined/expired/revoked), a
   14-day expiry (lazily enforced), one-pending-per-email uniqueness,
   and fully-working invite_member()/accept_invitation()/
   decline_invitation()/revoke_invitation()/list_my_invitations() RPCs.
   Nothing about accept/decline/expiry/revoke needed to change.

8. EMAIL PROVIDER SELECTED
   Resend - the SAME account already sending Lilica's Supabase Auth
   verification/password-reset emails (the product owner's own explicit
   direction: reuse, don't introduce a second provider).

9. SERVER-SIDE EMAIL ARCHITECTURE
   New Edge Function `send-invitation-email` (supabase/functions/send-
   invitation-email/index.ts + email-content.ts). Uses a USER-scoped
   Supabase client (the caller's own forwarded Authorization header) --
   never service-role. The invitation row is read through that client,
   so the existing care_space_invitations_select_organiser RLS policy
   is the entire authority check: if the row doesn't come back, the
   caller was never allowed to send it. Deployed to `lilica-development`.

10. SECRETS/ENV CONFIGURATION
    RESEND_API_KEY set via `npx supabase secrets set` on the
    lilica-development project. Never written to any file in this
    repository, never logged, confirmed absent by the secrets:check
    scan (340 files inspected, clean).

11. EXACT EMAIL CONTENT
    Subject: "[Inviter] has invited you to help with [Person]'s care"
    Body: "[Inviter] has invited you to join [Person]'s Care Circle.
    Lilica helps families organise the things involved in supporting
    someone. [Join button] If you weren't expecting this invitation,
    you can ignore this email."
    Contains ONLY the inviter's display name and the supported person's
    FIRST NAME -- proven by a dedicated test asserting the combined
    subject/text/html never contains any of: appointment, task, bill,
    invoice, prescription, diagnosis, document, financial, balance, £, $.

12. SECURE TOKEN DESIGN
    No new column. care_space_invitations.id itself (gen_random_uuid(),
    122 bits of randomness, server-owned, tied to one row, naturally
    single-use via its own status transitions) already satisfies every
    stated requirement. Documented explicitly in src/invitationLinks.ts.

13. DEEP-LINK DESIGN
    src/invitationLinks.ts: the one canonical link format
    (https://lilica.co.uk/invite/<id> and lilica://invite/<id>) and
    parser, shared by email, Share, and deep-link handling.
    src/invitationDeepLink.ts: a second, independent Linking listener
    (RN's Linking emitter calls every subscriber -- proven not to
    interfere with AuthProvider's own existing listener by a dedicated
    test). A signed-in user tapping either link form jumps straight to
    the Invitations review screen.

14. NOT-INSTALLED JOURNEY
    public/invite/index.html: a static, HONEST landing page. Shows
    "Open in Lilica" (using the lilica:// scheme) and explicit, truthful
    text that Lilica isn't in the App Store/Google Play yet -- NO fake
    or placeholder store buttons, because no real store listing exists
    (no EAS build has ever been done for Lilica). This page is written
    but NOT yet deployed/hosted anywhere -- see item 42.

15. MANUAL-LOGIN FALLBACK
    Unchanged and confirmed still correct: list_my_invitations() already
    runs on every signed-in app bootstrap and auto-opens the Invitations
    screen the first time it finds anything pending, regardless of
    whether a link was ever tapped.

16. NORMAL ONBOARDING UNCHANGED
    No new "Have you been invited?" step was added anywhere. Confirmed
    by the full, unchanged, still-passing onboarding test suite.

17. INVITED-NEW-USER ONBOARDING BRANCH
    Not built as a distinct branch -- deliberately relies on the
    existing fallback (item 15), which already handles this case
    correctly once the new user signs up with the invited email.

18. EXISTING-USER INVITATION BRANCH
    A signed-in user tapping a link now jumps straight to review
    (item 13) instead of waiting for the next natural refresh.

19. INVITATION REVIEW UI
    InvitationsScreen.tsx already showed "You've been invited," the
    inviter, the role, and Accept/Decline -- this pass added the full
    DOMAIN_DESCRIPTIONS text for each granted domain (previously only
    the bare label was shown).

20. PERMISSION-DESCRIPTION REUSE
    Confirmed: InvitationsScreen.tsx imports the SAME DOMAIN_DESCRIPTIONS
    constant CareCircleScreen.tsx uses when the organiser selects what
    to grant -- no second copy exists anywhere.

21. ACCEPT BEHAVIOUR
    Unchanged (already correct) -- uses the invitation's own stored
    care_space_id/role/granted_domains exclusively; the invitee cannot
    alter any of them from the client.

22. DECLINE BEHAVIOUR
    Unchanged (already correct) -- creates no membership, marks the
    invitation declined, never nags again.

23. EXPIRY
    Unchanged (already correct, 14 days, lazily enforced on read/accept).

24. RESEND
    No new RPC needed -- "resend" is simply calling sendInvitationEmail()
    again for the SAME existing invitation id (proven directly by a test:
    inviteMember is never called a second time).

25. REVOKE
    Unchanged (already correct, revoke_invitation()) -- confirmed a
    revoked invitation's own id can no longer be used (accept_
    invitation() checks status = 'pending' first).

26. EMAIL FAILURE UX
    "Invitation created" (already true) is shown separately from
    "Invitation emailed" (only after Resend's API genuinely accepts the
    send) -- a failure shows "The invitation was created, but the email
    couldn't be sent" plus a "Retry email" button, never a fabricated
    success. Proven directly by test.

27. NATIVE SHARE BEHAVIOUR
    Share.share() with inviter name, supported person's first name, and
    the https://lilica.co.uk/invite/<id> link -- proven directly by test
    to contain exactly these and nothing else.


################################################################
VALIDATION
################################################################

28. KNOWN CLOCK-TEST FIX
    tests/home-strip-navigation.test.tsx's real-clock-dependent test now
    uses jest.useFakeTimers()/setSystemTime(), matching this file's own
    sibling blocks. Test-only change.

29. FULL JEST RESULT
    84 suites / 748 tests. ALL GREEN.

30. TYPESCRIPT
    npm run typecheck: CLEAN.

31. SECRETS CHECK
    "Secret scan passed (340 repository files inspected)."

32. EXPO CONFIG
    npx expo config --type public: resolves cleanly (exit 0).

33. WEB EXPORT
    npx expo export --platform web: succeeded, dist/ produced, no errors.

34. pgTAP LOCAL
    17 files / 415 assertions, all pass.

35. pgTAP LINKED
    Identical: 17 files / 415 assertions, all pass against
    lilica-development.

36. DB LINT
    Local and linked both show only the same single pre-existing,
    unrelated finding present before this entire closure began
    (delete_care_space's own unread caller_membership variable).

37. MIGRATIONS
    One additive migration this closure
    (20260916090000_fix_assignments_domain_scoped_read.sql). No schema
    change was needed for the invitation-delivery work itself -- the
    existing table already had everything required.

38. GIT DIFF --CHECK
    Clean (only expected LF/CRLF advisory warnings on Windows).


################################################################
EXTERNAL SETUP
################################################################

39. EXACT PROVIDER ACCOUNT SETUP NEEDED
    NONE REMAINING -- already completed this session: lilica.co.uk was
    added to the existing Resend account and shows "verified" (confirmed
    directly via the Resend API, not just visually), and a scoped API
    key was generated and set as RESEND_API_KEY.

40. DNS/DOMAIN SETUP NEEDED
    Resend's own domain-verification DNS records: already added and
    verified. STILL NEEDED, separately: DNS/hosting for the actual
    landing page content (public/invite/index.html) -- lilica.co.uk's
    DNS currently only points at Resend's mail-sending records. Choose a
    static host (Netlify/Vercel/GitHub Pages all work) and point
    lilica.co.uk (or a subpath) at it; the page's own header comment
    documents the exact rewrite rule each option needs for /invite/*.

41. SUPABASE SECRETS NEEDED
    NONE REMAINING -- RESEND_API_KEY is already set.

42. DEEP-LINK/STORE CONFIGURATION NEEDED
    Real universal/app link verification needs: (a) Lilica's real Apple
    Developer Team ID, to replace the placeholder in
    public/.well-known/apple-app-site-association; (b) Lilica's real
    Android app-signing SHA256 certificate fingerprint, to replace the
    placeholder in public/.well-known/assetlinks.json. Neither exists
    yet -- both require Lilica to have gone through a real EAS build
    (a separate, much larger prerequisite requiring the product owner's
    own explicit, express consent per this project's own standing rule,
    and outside this closure's own scope). Real App Store/Google Play
    URLs for the landing page's own "not installed" messaging need the
    same prerequisite.

43. ANYTHING PRODUCT OWNER MUST DO BEFORE PHYSICAL TEST
    Nothing further for EMAIL delivery -- already tested for real (item
    45). For deep-link/landing-page testing specifically: host
    public/invite/index.html somewhere (item 40) before a browser-based
    "not installed" journey can be tested; the lilica:// custom-scheme
    link already works today on a device with Lilica installed via Expo
    Go/a dev build, no further setup needed for that part.


################################################################
QA
################################################################

44. PHYSICAL QA CHECKLIST
    A. In the app, invite a test email address with a limited domain
       selection; confirm "Invitation created" appears with Send by
       email / Share invitation.
    B. Tap "Send by email"; confirm "Invitation emailed." appears (not a
       raw error) and a real email arrives at the invited address.
    C. Open the received email; confirm it names the real inviter and
       supported person, has a working Join button, and contains no
       record/task/bill/health/document content.
    D. With Lilica already installed on a device, open the link the
       email sent (or generated by Share) and confirm it opens Lilica
       directly via the lilica:// scheme once tapped from within a
       context that supports it (e.g. Messages/Notes -- browser-opened
       https:// links will fall through to the NOT-yet-hosted landing
       page until item 40 is done).
    E. Tap "Share invitation"; confirm the native share sheet appears
       with the expected text and a working link.
    F. As a different organiser account, confirm "Pending invitations"
       shows the same invitation with its own Send by email/Share/Cancel
       actions, and that Cancel invitation genuinely revokes it.
    G. Accept the invitation as the invitee; confirm the reviewing
       screen shows the real inviter, role, and per-domain descriptions
       before accepting, and that access afterward exactly matches what
       was granted -- nothing more.
    H. Confirm declining creates no membership and the organiser sees
       the invitation is no longer pending.

45. PHYSICAL EMAIL DELIVERY: PASS.
    A genuine test email was sent via the Resend API directly to
    dtm35@cam.ac.uk and Resend's own API confirmed
    "last_event":"delivered" before this report was written. The
    product owner separately confirmed receiving it in their own inbox
    during this same session.

46. REAL dtm35@cam.ac.uk RESULT
    Delivered and received, confirmed by both Resend's API
    (`last_event: delivered`) and the product owner's own direct
    confirmation ("Received the test email you sent").


################################################################
CLOSURE
################################################################

47. FILES CHANGED (this Part B pass; Part A's own files are listed in
    the earlier CARE_CIRCLE_SECURITY_CLOSURE_PART_A_REPORT.txt)
    supabase/functions/send-invitation-email/index.ts (new)
    supabase/functions/send-invitation-email/email-content.ts (new)
    src/careCircle.ts (sendInvitationEmail(); inviteMember() now returns
      the invitation id)
    src/invitationLinks.ts (new)
    src/invitationDeepLink.ts (new)
    src/screens/CareCircleScreen.tsx (DeliveryActions: Send by email /
      Share, for both a just-created and an existing pending invitation)
    src/screens/InvitationsScreen.tsx (full DOMAIN_DESCRIPTIONS shown)
    App.tsx (useInvitationDeepLink wiring; inviterDisplayName prop)
    app.json (ios.associatedDomains, android.intentFilters)
    public/.well-known/apple-app-site-association (new, placeholder Team ID)
    public/.well-known/assetlinks.json (new, placeholder fingerprint)
    public/invite/index.html (new, honest landing page)
    tests/invitation-email-content.test.ts (new)
    tests/invitation-links.test.ts (new)
    tests/invitation-deep-link.test.tsx (new)
    tests/care-circle-invitation-delivery.test.tsx (new)
    tests/phase15-invitations-screen.test.tsx (updated for the new
      full-description display)
    tests/care-circle-domain-explanations.test.tsx (mock updated for
      inviteMember()'s new return shape)
    docs/REVISION_LOG.md (two new entries -- Part A and Part B)

48. DIFF CLASSIFICATION
    Security/consistency fix: assignments policy + record_domain_for_
      type() hardening + their pgTAP (Part A).
    UX correction: Everyday copy (Part A).
    Test-only fix: clock-dependent test (Part A).
    New feature, server: send-invitation-email Edge Function.
    New feature, client: delivery UI, deep-link hook, link helpers.
    New feature, config: app.json, public/ well-known + landing page.
    Documentation: REVISION_LOG.md.
    No unexpected files.

49. GIT STATUS
    Branch: prephase22-remove-supported-person-faq-help.
    HEAD: 87a427e -- unchanged; nothing from this closure (Part A or
    Part B) has been committed.
    origin/master: a67c40f -- unchanged throughout.

50. CONFIRMATION NOTHING COMMITTED OR PUSHED
    Confirmed.

51. CONFIRMATION PHASE 22 NOT RESUMED
    Confirmed. docs/PHASE_22_VISUAL_AUDIT.md untouched.


================================================================
SUMMARY FOR THE MORNING
================================================================

Real invitation emails now work end to end -- verified by both Resend's
own delivery confirmation and your own inbox. Care Circle domain
permissions are hardened and fail closed. The one remaining piece from
the original brief that could NOT be finished tonight is the "not
installed" universal-link/landing-page journey, and it isn't a code gap
-- it's genuinely blocked on Lilica never having been through a real
App Store/Google Play build, which needs its own separate, explicit
decision from you before it can even start. Everything else in this
report is done, tested, and waiting only on your own review.

DO NOT COMMIT. DO NOT PUSH. DO NOT START PHASE 22.
================================================================
