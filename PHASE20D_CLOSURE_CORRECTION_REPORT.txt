================================================================
LILICA - PHASE 20D FINAL CLOSURE CORRECTION - REPORT
================================================================
Report date: 15 September 2026


1. EXACT FILES CHANGED BY THIS CORRECTION
   - src/components/RemoveCareSpaceConfirm.tsx (new `requiresAllOrganisers`
     prop; subscription disclosure added to both wording paths)
   - src/screens/ManageCareScreen.tsx (wires `requiresAllOrganisers`;
     approval card gained its own permanence + disclosure line)
   - tests/remove-care-space.test.tsx (2 new tests on the component
     directly)
   - tests/manage-care-screen.test.tsx (2 tests extended/added covering
     the request and approval flows end to end)
   - docs/PHASE_20D_CARE_SPACE_LIFECYCLE_PROPOSAL.md (closure-correction
     addendum)
   - docs/REVISION_LOG.md (new entry)

   A SECOND, unrelated fix was also made this same pass after further
   direct product-owner confirmation that the Janet-avatar bug survived a
   genuinely fresh Expo Go session (see section 13):
   - src/careSpaceState.ts (integrateReconnectedCareSpaces() now prunes a
     genuinely deleted/revoked care space on reconnect)
   - tests/reconnect-prunes-deleted-care-space.test.ts (new, 4 tests)

2. EXACT REQUEST-CONFIRMATION COPY (multi-organiser, `requiresAllOrganisers`)
   "Because {careSpaceName}'s care has more than one organiser, all active
   organisers must agree before it can be permanently removed."
   "Once every organiser agrees, this cannot be undone."
   "Requesting removal does not itself cancel an App Store or Google Play
   subscription -- manage or cancel it directly in your App Store/Google
   Play account settings."
   (Sole-organiser wording, corrected to carry the same disclosure for the
   first time: "This cannot be undone." / "Removing does not itself
   cancel an App Store or Google Play subscription -- manage or cancel it
   directly in your App Store/Google Play account settings.")

3. EXACT APPROVAL COPY
   Shown only to an organiser who has not yet approved (never repeated to
   the requester, who already saw the fuller request confirmation, and
   never repeated to someone who has already approved):
   "Approving is permanent once every organiser agrees, and does not
   itself cancel an App Store or Google Play subscription."

4. TESTS ADDED/CHANGED
   - `RemoveCareSpaceConfirm`: sole-organiser default shows the original
     wording plus the disclosure, and never shows the "all organisers"
     line; `requiresAllOrganisers` shows all three required points.
   - `ManageCareScreen`: the sole-organiser Remove flow shows "This cannot
     be undone." and its disclosure line unchanged; the multi-organiser
     request flow shows all of (A) permanence, (B) agreement requirement,
     (C) subscription disclosure; the approval card shows (D) its own
     permanence + disclosure line to the approver only, never to the
     requester.
   - `integrateReconnectedCareSpaces`: the exact reported scenario (both
     care spaces deleted, still shows Janet -> now correctly cleared);
     only the removed space is pruned, an untouched synced space
     survives; a local-only space is never pruned; the original
     "never wipe a space still returned by the server" guarantee holds.

5. FULL JEST RESULT
   79 suites / 716 tests, ALL PASSING (up from 78/709 before this
   correction). Zero regressions.

6. TYPESCRIPT RESULT
   `npm run typecheck`: CLEAN.

7. SECRETS RESULT
   `npm run secrets:check`: "Secret scan passed (320 repository files
   inspected)."

8. EXPO CONFIG RESULT
   `npx expo config --type public`: resolves cleanly (exit 0).

9. WEB EXPORT RESULT
   `npx expo export --platform web`: succeeded, produced `dist/` with no
   errors.

10. GIT DIFF --CHECK RESULT
    Clean (only expected LF/CRLF advisory warnings on Windows, no real
    whitespace errors or conflict markers).

11. CONFIRMATION NO SCHEMA/DATABASE CHANGE
    Confirmed. No migration file was created or modified by this
    correction. `request_care_space_deletion()`, `approve_care_space_
    deletion()`, `decline_care_space_deletion()`, `cancel_care_space_
    deletion()`, `delete_care_space()`, organiser counting, approval
    rules, reason metadata, storage cleanup, commercial ownership, and
    subscription architecture are all completely untouched - this was a
    UX/copy closure correction plus one unrelated client-side reducer fix,
    exactly as scoped.

12. DOCUMENTATION CORRECTION
    `docs/PHASE_20D_CARE_SPACE_LIFECYCLE_PROPOSAL.md` gained a closure-
    correction addendum recording that the disclosure is no longer
    deferred, and honestly noting that the earlier report's claim about
    the sole-organiser flow already having it was mistaken. `docs/
    REVISION_LOG.md` gained a matching new entry, including the second,
    separately-found reconnect-pruning fix. No earlier phase's history
    was rewritten - both proposal documents' original analysis remains
    exactly as written below their own status banners.

13. CONFIRMATION ALL PREVIOUS PHASE 20D IMPLEMENTATION REMAINS INTACT
    Confirmed by the full, unchanged, still-passing suite: Archive/
    Restore, archived-care server enforcement, ArchivedGate, Archived
    care screen, organiser promotion/handoff, commercial-owner leave
    protection, multi-organiser deletion consensus, sole-organiser
    permanent deletion, Manage [Name]'s care, Documents, the final
    Settings drawer structure, Help, the existing Phase 20C work, and the
    two previously-documented unrelated live bug fixes are all present
    and unmodified except where this correction explicitly touched them
    (items 1 above). One of those two previously-documented bug fixes
    (the stale Janet avatar) turned out to have a SECOND, deeper cause
    beyond the first fix already reported - both causes are now fixed;
    see the REVISION_LOG entry for the full explanation of why the first
    fix alone was insufficient (it corrected the LAST-space-removed case
    within a single reducer call, but not the separate, ongoing
    reconnect-on-every-launch case that kept re-merging the stale entry
    back in).

14. PHYSICAL QA CHECKLIST
    (Unchanged from the structural-closure report - reproduced here per
    this brief's own request that it be returned prominently)

    A. SETTINGS DRAWER - scroll the entire drawer; confirm [Name]'s care
       contains Care Summary, Recent Activity, Documents, Manage [Name]'s
       care; confirm Account and Help sections remain correct.
    B. DOCUMENTS - open a person with documents; check newest-first
       presentation; check parent-record context; open a document; check
       the empty state for a person without documents.
    C. MANAGE [NAME]'S CARE - inspect the overall hierarchy; confirm
       ordinary management, Archive, and Permanent removal are visually
       distinct.
    D. ARCHIVE - archive a TEST care space; confirm it disappears from
       active person switching; confirm historical information remains
       available; attempt an edit/add and confirm ArchivedGate appears.
    E. RESTORE - restore the same care space; confirm it returns to
       active switching; confirm normal editing works again.
    F. ORGANISER HANDOFF - promote another active Care Circle member to
       organiser; verify the role change; verify the original organiser
       can leave once responsibility and commercial ownership are
       appropriately resolved.
    G. SOLE-ORGANISER REMOVE - use a TEST care space; confirm the existing
       strong checkbox confirmation; confirm the subscription disclosure
       now genuinely appears; confirm permanent deletion works.
    H. MULTI-ORGANISER REMOVE - use a TEST care space with two organisers;
       organiser A requests removal; confirm the permanent-removal
       wording, the all-organiser-agreement wording, and the subscription
       disclosure all appear; confirm the care space is NOT immediately
       deleted; organiser B sees the pending request with Approve/Decline
       and the same disclosure; approve; confirm deletion occurs only
       after consensus.
    I. PERSON SWITCHING - with two active supported people, switch between
       them; confirm Documents and Manage [Name]'s care always show the
       correct person's name/data.
    J. RECONNECT FIX (new, from this correction) - as an organiser, delete
       a TEST supported person; fully close and reopen the app several
       times; confirm the deleted person never reappears on Home or
       People, on any subsequent launch, not just the first one.

    DO NOT perform destructive physical QA against genuine care data -
    use disposable/test care spaces throughout.

    AUTOMATED PASS: confirmed via items 5-11 above, plus the existing
    396/396 pgTAP (unaffected by this correction, re-confirmed via the
    full Jest suite covering the client-side logic these RPCs back).

    15. PHYSICAL QA NOT YET RUN.

16. FINAL GIT STATUS
    Branch: prephase22-remove-supported-person-faq-help.
    HEAD: f6a311a - unchanged; nothing from this correction (or the
    structural-closure work before it) has been committed.
    origin/master: a67c40f - unchanged throughout.

17. CONFIRMATION NOTHING COMMITTED OR PUSHED
    Confirmed. No `git commit` or `git push` has been run.

18. CONFIRMATION PHASE 22 NOT STARTED
    Confirmed. `docs/PHASE_22_VISUAL_AUDIT.md` was not opened, read, or
    modified by this correction.

DO NOT COMMIT. DO NOT PUSH. DO NOT START PHASE 22.
================================================================
